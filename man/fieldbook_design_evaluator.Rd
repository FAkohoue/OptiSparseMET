% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fieldbook_design_evaluator.R
\name{fieldbook_design_evaluator}
\alias{fieldbook_design_evaluator}
\title{Build a joint allocation-replication-layout evaluator}
\usage{
fieldbook_design_evaluator(
  env_design_specs,
  treatment_info = NULL,
  common_treatments = NULL,
  seed_info = NULL,
  seed_required_per_plot = NULL,
  minimum_seed_buffer = 0,
  information_models = NULL,
  cost_per_plot = 1,
  seed = NULL,
  plan_args = list()
)
}
\arguments{
\item{env_design_specs}{Named local-design specifications, as in
\code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.}

\item{treatment_info, common_treatments, seed_info, seed_required_per_plot}{Inputs passed to \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.}

\item{minimum_seed_buffer}{Seed-reserve input passed to
\code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.}

\item{information_models}{Optional named list, one element per environment,
of arguments for \code{\link[=local_treatment_information]{local_treatment_information()}} other than \code{field_book},
\code{treatments}, and \code{check_treatments}. Unspecified environments use the
defaults of that function.}

\item{cost_per_plot}{One finite non-negative plot cost, or a named value per
environment, returned to the design objective.}

\item{seed}{Reproducible local-layout seed.}

\item{plan_args}{Additional named arguments for \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.
Core candidate-specific arguments cannot be overridden.}
}
\value{
A function suitable for \code{optimize_design(design_evaluator = ...)}.
Calling it returns \code{reps}, \code{local_information}, \code{fieldbooks}, \code{metadata},
\code{cost_per_plot}, and \code{feasible}.
}
\description{
Creates the callback consumed by \code{\link[=optimize_design]{optimize_design()}} to turn each candidate
allocation into actual environment fieldbooks, count its realised integer
replication, and derive full local treatment-information matrices. This is
the package's joint optimisation bridge: allocation moves are accepted or
rejected using the plantable layouts they produce, rather than an assumed
scalar site efficiency.
}
