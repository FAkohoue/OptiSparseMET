% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envirotype_features.R
\name{envirotype_features}
\alias{envirotype_features}
\title{Temporal envirotyping features from a daily weather series}
\usage{
envirotype_features(
  daily,
  windows = 1,
  stats = "mean",
  thresholds = NULL,
  gdd = NULL,
  functional = NULL,
  stress = NULL,
  spells = NULL,
  phenology = NULL,
  env_col = "environment",
  day_col = "day",
  date_col = "date"
)
}
\arguments{
\item{daily}{A data frame with an environment column, a day column (numeric
day-after-start; if absent it is derived from row order within each
environment), and one column per weather parameter. Long format (many rows
per environment).}

\item{windows}{Integer \code{K}, numeric cut points, or a named list of day ranges
(see Details). Default 1 (whole season).}

\item{stats}{Character vector of per-window statistics. Default \code{"mean"}.}

\item{thresholds}{Named list of stress-day definitions (see Details).}

\item{gdd}{Named list of growing-degree-day definitions (see Details).}

\item{functional}{Named list of functional (spline) specifications (see
Details).}

\item{stress}{Named list of cumulative stress-severity definitions. Each uses
\code{par} and either \code{above} or \code{below}; severity is the accumulated distance
beyond the threshold rather than only a count of stress days.}

\item{spells}{Named list of consecutive-event definitions using \code{par} and
\code{above} or \code{below}. Both the longest run and number of runs are returned.}

\item{phenology}{Optional data frame with one row per environment-stage and
columns \code{environment}, \code{stage}, plus either \code{start_day}/\code{end_day} or
\code{start_date}/\code{end_date}. These observed or breeder-specified stages replace
the common \code{windows} for the corresponding environment.}

\item{env_col, day_col, date_col}{Column names for environment id, day index,
and calendar date. When day is absent but date is available, the day index
is computed from actual dates after sorting. When both are unavailable,
row order is used within environment and one consolidated warning names
every affected environment.}
}
\value{
An environment-by-feature numeric matrix (row names = environments).
}
\description{
Collapsing a season to one mean per site discards the within-season variation,
extremes, and \emph{timing} that often drive genotype-by-environment interaction.
\code{envirotype_features()} keeps that information: from a daily (or sub-daily)
weather series per environment it builds a rich, fixed-length feature vector
per environment -- by time window / crop stage, with variability and stress
statistics, growing degree days, and an optional functional (spline)
representation of the whole trajectory. The resulting environment-by-feature
matrix plugs straight into \code{\link[=build_environment_relationship]{build_environment_relationship()}} (or
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}} as \verb{weather =}), so the environmental kinship
reflects how sites differ in their temporal dynamics, not just their means.
}
\details{
All features are computed on \emph{phenological time} -- each series is indexed by
day-after-start, so sites with different season lengths are compared on a
common footing (this is why dynamic time warping is not used: warping would
erase the timing signal we want to keep).
\describe{
\item{Windows / stages}{\code{windows} may be a single integer \code{K} (split each
season into \code{K} equal intervals -- no crop knowledge needed), a numeric
vector of day-after-start cut points (e.g. \code{c(30, 60, 90)}), or a named
list of day ranges from crop physiology
(e.g. \code{list(vegetative = c(0, 40), flowering = c(40, 70), grain_fill = c(70, 120))}). Expert stages give the most interpretable
features; equal intervals or thermal-time cut points are the
knowledge-free alternatives.}
\item{Statistics}{Each \code{stats} (e.g. \code{"mean"}, \code{"sd"}, \code{"cv"}, \code{"sum"},
\code{"min"}, \code{"max"}, \code{"median"}) is computed per window per parameter; \code{"sd"}
and \code{"cv"} capture the day-to-day variability the mean hides.}
\item{Stress days}{\code{thresholds} counts days meeting a condition per window,
e.g. \code{list(heat_days = list(par = "T2M_MAX", above = 35))}.}
\item{Growing degree days}{\code{gdd} accumulates degree days per window, e.g.
\code{list(gdd = list(par = "T2M", base = 10, upper = 30))}.}
\item{Functional}{\code{functional} fits a B-spline basis over normalised season
time and returns the coefficients as shape features, e.g.
\code{list(temp_shape = list(par = "T2M", df = 4))}.}
}
}
\examples{
set.seed(1)
mk <- function(e, len, base) data.frame(
  environment = e, day = 0:(len - 1),
  T2M = base + sin((0:(len - 1)) / 10) * 3 + rnorm(len, 0, 0.5),
  T2M_MAX = base + 6 + rnorm(len, 0, 0.5),
  PRECTOTCORR = pmax(0, rnorm(len, 3, 3)))
daily <- rbind(mk("E1", 90, 24), mk("E2", 80, 28))
W <- envirotype_features(
  daily, windows = list(veg = c(0, 40), flower = c(40, 70), fill = c(70, 120)),
  stats = c("mean", "sd"),
  thresholds = list(heat_days = list(par = "T2M_MAX", above = 33)),
  gdd = list(gdd = list(par = "T2M", base = 10)),
  functional = list(temp_shape = list(par = "T2M", df = 4)))
dim(W)
}
\references{
Costa-Neto, G., Fritsche-Neto, R., & Crossa, J. (2021). Nonlinear kernels,
dominance, and envirotyping data increase prediction accuracy... \emph{Heredity}.
Jarquin, D. et al. (2014). A reaction norm model for genomic selection using
high-dimensional genomic and environmental data. \emph{TAG} 127, 595-607.
}
\seealso{
\code{\link[=fetch_weather_series]{fetch_weather_series()}}, \code{\link[=build_environment_relationship]{build_environment_relationship()}},
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}.
}
