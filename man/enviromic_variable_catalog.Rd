% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enviromic_variable_catalog.R
\name{enviromic_variable_catalog}
\alias{enviromic_variable_catalog}
\title{Catalogue of enviromic variable names, descriptions and units}
\usage{
enviromic_variable_catalog(source = c("all", "weather", "soil"))
}
\arguments{
\item{source}{Which variables to list: \code{"all"} (default), \code{"weather"}, or
\code{"soil"}.}
}
\value{
A data frame with columns \code{variable} (the exact column name to use),
\code{source}, \code{description}, and \code{units} (conventional units after the
package's SoilGrids conversion factors are applied).
}
\description{
The weather (NASA POWER) and soil (SoilGrids) fetchers return columns with
fixed but not always self-explanatory names (e.g. \code{phh2o}, \code{bdod},
\code{total_precip}). A breeder choosing the \emph{limiting variables} for
\code{\link[=build_environment_relationship]{build_environment_relationship()}} needs to know which names are available
and what they mean. \code{enviromic_variable_catalog()} returns that reference
table, so the exact strings to pass to the \code{variables} argument can be read
off directly. The assembled matrix's own \code{colnames()} always lists what is
actually present for a given fetch; this catalogue explains each code.
}
\examples{
enviromic_variable_catalog("soil")
# pick limiting variables by their catalogued names:
# build_environment_relationship(X, source = "enviromic",
#   variables = c("mean_temp", "total_precip", "clay", "phh2o"))
}
\seealso{
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}, \code{\link[=fetch_soilgrids]{fetch_soilgrids()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}.
}
