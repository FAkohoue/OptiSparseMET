% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/met_information_mt.R
\name{met_information_mt}
\alias{met_information_mt}
\title{Exact multi-trait MET information matrix (trait-covariance Kronecker term)}
\usage{
met_information_mt(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  Sigma_T,
  R_T = NULL,
  index_weights = NULL,
  sigma_e2 = 1,
  reps = NULL,
  env_efficiency = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocation_matrix}{Genotype-by-environment 0/1 (or replication) matrix,
shared across traits (every plot measures all traits).}

\item{G}{Genomic relationship matrix (row/column names).}

\item{Sigma_E}{Environment covariance (defaults to identity).}

\item{Sigma_T}{Trait \strong{genetic} (co)variance matrix (\verb{T x T}, dimnames
optional).}

\item{R_T}{Trait \strong{residual} (co)variance matrix (\verb{T x T}); defaults to
\code{sigma_e2 * I}.}

\item{index_weights}{Optional index weight vector \code{w} (length \code{T}), typically
from \code{\link[=desired_gain_weights]{desired_gain_weights()}}. At least one weight must be non-zero. If
\code{NULL}, per-trait reliabilities are returned but no index summary.}

\item{sigma_e2}{Residual scale used only when \code{R_T} is \code{NULL}. Default 1.}

\item{reps, env_efficiency, max_dim}{Passed to \code{\link[=met_information]{met_information()}}.}
}
\value{
A list with \code{CDmean_index} (mean index reliability), \code{rel_index_per_line},
\code{PEV_index_per_line}, \code{mean_PEV_index}, \code{CD_per_trait} (a \code{T}-vector of mean
per-trait reliabilities), \code{sigma_index} (\eqn{\sqrt{w'\Sigma_T w}}),
\code{lambda} (canonical genetic variances), and \code{Q} (canonical transform).
}
\description{
The exact multi-trait extension of \code{\link[=met_information]{met_information()}}. The additive genetic
values form a genotype-by-environment-by-trait array with the separable
covariance \eqn{\Sigma_T \otimes \Sigma_E \otimes G}, and the within-plot
residuals have trait covariance \eqn{R_T}. Rather than approximating a
multi-trait index by the single-trait reliability (as
\code{\link[=expected_genetic_gain]{expected_genetic_gain()}} does by default), \code{met_information_mt()} solves the
full multi-trait mixed-model equations and returns the exact prediction-error
variance and reliability of a desired-gain / economic index.
}
\details{
When every plot measures all traits (the usual case), the multi-trait model
with separable structure decouples exactly by the \strong{canonical transformation}
(Hayes & Hill 1981; Thompson): simultaneously diagonalising \eqn{\Sigma_T} and
\eqn{R_T} turns the problem into \code{T} independent single-trait analyses, each
solved by \code{\link[=met_information]{met_information()}} at the corresponding canonical genetic variance
\eqn{\lambda_k} (with unit residual variance). The canonical
prediction-error variances are recombined into the original-scale
trait PEV matrix per genotype, and hence the reliability of any linear index
\eqn{H = w'g}. This is exact (verified against a direct multi-trait MME solve),
not an approximation, and reuses the fast single-trait engine \code{T} times.
}
\examples{
set.seed(1)
G <- crossprod(matrix(rnorm(8 * 20), 20, 8)) / 20 + diag(8) * 0.4
dimnames(G) <- list(paste0("L", 1:8), paste0("L", 1:8))
SigE <- matrix(c(1, .5, .5, 1), 2); dimnames(SigE) <- list(c("E1","E2"), c("E1","E2"))
SigT <- matrix(c(1, .3, .3, .6), 2)
M <- matrix(0L, 8, 2, dimnames = list(rownames(G), colnames(SigE)))
for (e in 1:2) M[sample(8, 5), e] <- 1L
met_information_mt(M, G, SigE, Sigma_T = SigT, index_weights = c(1, -0.5))$CDmean_index
}
\references{
Hayes, J. F., & Hill, W. G. (1981). Modification of estimates of parameters in
the construction of genetic selection indices ('bending'). \emph{Biometrics}, 37,
483-493. Thompson, R. (1976). The estimation of maternal genetic variances.
\emph{Biometrics}, 32, 903-917.
}
\seealso{
\code{\link[=met_information]{met_information()}}, \code{\link[=desired_gain_weights]{desired_gain_weights()}}, \code{\link[=expected_genetic_gain]{expected_genetic_gain()}}.
}
