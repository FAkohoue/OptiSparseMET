% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_environments.R
\name{infer_mega_environments}
\alias{infer_mega_environments}
\title{Infer stable mega-environments without specifying their number}
\usage{
infer_mega_environments(
  D,
  relationships = NULL,
  relationship_groups = NULL,
  Sigma_E = NULL,
  k_range = NULL,
  methods = c("hclust", "kmeans"),
  min_cluster_size = 2L,
  min_silhouette = 0.25,
  min_stability = 0.7,
  stability_quantile = 0.1,
  n_boot = 200L,
  seed = 1L
)
}
\arguments{
\item{D}{Central square, finite, named environment similarity matrix.}

\item{relationships}{Optional named list of aligned environment relationship
matrices representing distinct modalities, years, or defensible
uncertainty draws. These are used to assess grouping stability, not to
estimate genetic covariance or user weights.}

\item{relationship_groups}{Optional named character vector assigning each
relationship matrix to an evidence block, such as all yearly weather
matrices to \code{"weather"}. Agreement is first aggregated within blocks, and
bootstrap sampling draws within every block, so a modality with many years
cannot dominate modalities represented by one matrix.}

\item{Sigma_E}{Optional covariance matrix returned within inferred groups.
It does not affect group selection and defaults to \code{D}.}

\item{k_range}{Optional integer candidate counts. The default is
\code{2:min(8, floor(n_environment / min_cluster_size))}.}

\item{methods}{Any non-empty subset of \code{"hclust"} and \code{"kmeans"}.}

\item{min_cluster_size}{Smallest admissible group size. The default of two
prevents an unsupported singleton from being called a mega-environment.}

\item{min_silhouette}{Minimum average silhouette required for a candidate.}

\item{min_stability}{Minimum lower stability quantile required. Stability is
measured by the adjusted Rand index against alternative methods,
relationship matrices, and relationship-bootstrap consensuses.}

\item{stability_quantile}{Lower quantile used for the stability requirement.}

\item{n_boot}{Number of relationship-level bootstrap draws. Bootstrap is
performed when at least one evidence block contains repeated matrices.}

\item{seed}{Integer seed for reproducible k-means and bootstrap sampling.}
}
\value{
A list containing \code{membership}, \code{clusters}, \code{Sigma_E}, inferred
\code{n_clusters}, selected \code{method}, \code{status} (\code{"stable"}, \code{"provisional"}, or
\code{"unstable"}), \code{hard_groups}, a human-readable \code{reason}, candidate
\code{diagnostics}, and resampling metadata. When status is \code{"unstable"},
\code{membership} contains one group and \code{hard_groups} is \code{FALSE}.
}
\description{
\code{infer_mega_environments()} chooses both the number of mega-environments and
the clustering algorithm from the environmental evidence. It compares
hierarchical clustering with k-means across a defensible candidate range,
requires adequate average silhouette separation, rejects clusters smaller
than \code{min_cluster_size}, and evaluates reproducibility across supplied
modality-, year-, or bootstrap-specific relationship matrices. Bootstrap
draws resample whole relationship matrices and recompute their rank-median
consensus; they do not estimate or apply arbitrary modality weights.
}
\details{
Candidate solutions first have to pass the separation and stability
thresholds. Selection is then lexicographic: largest silhouette, largest
lower stability quantile, fewer groups, and finally method order. Thus no
user-defined weighted score determines the answer. With only one
relationship matrix a well-separated, algorithm-reproducible solution is
labelled \code{"provisional"}. With multiple independent evidence blocks it can
be labelled \code{"stable"}. If no candidate is supported, the function returns one
unpartitioned group with status \code{"unstable"} rather than forcing a
biologically fragile classification.

K-means uses a finite-descent Hartigan relocation algorithm. It starts from
non-empty partitions and accepts only moves that strictly reduce
within-cluster sum of squares. Because the number of partitions is finite,
every start terminates at a valid local optimum without an iteration-limit
failure. Multiple starts and deterministic seed handling are retained.

These groups are descriptive environmental strata. They become genetic
mega-environments only when historical MET response data support the
corresponding genotype-by-environment structure.
}
\seealso{
\code{\link[=cluster_environments]{cluster_environments()}}, \code{\link[=consensus_environment_kernels]{consensus_environment_kernels()}},
\code{\link[=assess_envirotype_stability]{assess_envirotype_stability()}}.
}
