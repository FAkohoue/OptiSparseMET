% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend_replication.R
\name{recommend_replication}
\alias{recommend_replication}
\title{Recommend a replication level from accuracy, cost, and seed constraints (decision 4)}
\usage{
recommend_replication(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  replication_levels = c(1, 1.5, 2, 3),
  n_sim = 30L,
  min_gain = 0.02,
  seed_available = NULL,
  seed_required_per_plot = NULL,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocation_matrix}{A \eqn{J \times E} 0/1 incidence matrix (lines as
rows, environments as columns) with row and column names. Typically
\code{alloc$allocation_matrix} from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{G}{Genomic (or pedigree) relationship matrix with row/column names
covering the lines in \code{allocation_matrix}.}

\item{Sigma_E}{Optional \eqn{E \times E} environment covariance (genetic
correlations across environments). Defaults to the identity (independent
environments, unit variance).}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{replication_levels}{Numeric vector of replication levels to evaluate.}

\item{n_sim}{Number of independent simulation replicates.}

\item{min_gain}{Minimum accuracy improvement (per unit replication) required
to justify the next level; the recommendation is the largest level whose
marginal accuracy gain still meets \code{min_gain}, else the smallest level.}

\item{seed_available}{Optional total number of seeds available per line
(scalar) for a feasibility flag.}

\item{seed_required_per_plot}{Optional seeds needed per plot (scalar).}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A list with \code{table} (a data frame: replication, accuracy_mean,
gain_mean, mean_PEV, feasible) and \code{recommended} (the chosen level).
}
\description{
\code{recommend_replication()} sweeps candidate replication levels for a given
allocation and reports the realized accuracy, genetic gain, and design-based
PEV at each level (via \code{\link[=simulate_met]{simulate_met()}}), then recommends the level at the
point of diminishing returns. It is the resource-aware form of decision 4 in
Colmant et al. (2026): it evaluates replication at the program's own spatial
noise (\code{sigma_e2}), relationship matrix, and -- optionally -- seed budget,
and it varies replication with the number of unique entries held fixed, so
the two are not confounded (as they were in the paper's field-size grid).
}
\details{
A replication level \code{r} scales the effective plots per tested cell
(\code{reps = allocation_matrix * r}), so fractional (p-rep) levels are allowed.
If \code{seed_available} (a per-line seed budget) and \code{seed_required_per_plot} are
supplied, a level is flagged infeasible when total required seed exceeds the
budget.
}
\seealso{
\code{\link[=simulate_met]{simulate_met()}}, \code{\link[=assign_replication_by_seed]{assign_replication_by_seed()}}.
}
