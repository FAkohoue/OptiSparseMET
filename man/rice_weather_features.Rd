% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envirotype_features.R
\name{rice_weather_features}
\alias{rice_weather_features}
\title{Rice-focused weather and crop-stress envirotyping}
\usage{
rice_weather_features(
  daily,
  windows = list(vegetative = c(0, 40), reproductive = c(40, 75), grain_fill = c(75,
    120)),
  phenology = NULL,
  time_scale = c("calendar", "thermal"),
  base_temperature = 10,
  stats = c("mean", "sd", "min", "max", "sum"),
  heat_threshold = 35,
  hot_night_threshold = 24,
  cold_threshold = 15,
  dry_day_threshold = 1,
  wet_day_threshold = 20,
  root_moisture_threshold = 0.3,
  env_col = "environment",
  day_col = "day",
  date_col = "date"
)
}
\arguments{
\item{daily}{Long daily weather data with NASA POWER-style names.}

\item{windows}{Stage windows used when \code{phenology} is absent.}

\item{phenology}{Optional observed environment-stage table; see
\code{\link[=envirotype_features]{envirotype_features()}}.}

\item{time_scale}{\code{"calendar"} or \code{"thermal"}. Thermal time accumulates GDD
from \code{base_temperature} and interprets \code{windows} on that scale.}

\item{base_temperature}{Base temperature for rice thermal time.}

\item{stats}{Per-stage summary statistics.}

\item{heat_threshold, hot_night_threshold, cold_threshold}{Temperature stress
thresholds in degrees C.}

\item{dry_day_threshold, wet_day_threshold}{Rain thresholds in mm/day.}

\item{root_moisture_threshold}{Threshold for \code{GWETROOT} when present.}

\item{env_col, day_col, date_col}{Column names.}
}
\value{
An environment-by-feature numeric matrix. Attributes include the
derived-variable definitions.
}
\description{
Adds physiologically interpretable rice weather variables before calling
\code{\link[=envirotype_features]{envirotype_features()}}: vapour-pressure deficit, diurnal temperature range,
precipitation-minus-evapotranspiration water balance, heat/cold severity,
hot nights, dry/hot/wet spells, root-zone moisture deficit, solar radiation,
and stage-specific summaries. Stages may come from observed phenology,
breeder-specified calendar windows, or thermal-time windows.
}
