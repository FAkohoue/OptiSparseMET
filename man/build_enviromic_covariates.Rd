% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_enviromic_covariates.R
\name{build_enviromic_covariates}
\alias{build_enviromic_covariates}
\title{Assemble enviromic covariates for sites (weather, soil, management)}
\usage{
build_enviromic_covariates(
  sites,
  management = NULL,
  weather = NULL,
  soil = NULL,
  fetch_weather = FALSE,
  fetch_soil = FALSE,
  weather_pars = NULL,
  weather_stats = NULL,
  weather_temporal = "daily",
  soil_backend = c("wcs", "webdav", "rest", "local"),
  soil_properties = c("bdod", "cec", "cfvo", "clay", "nitrogen", "ocd", "phh2o", "sand",
    "silt", "soc"),
  soil_depth = "0-5cm",
  soil_quantile = "mean",
  soil_local_paths = NULL,
  standardize = TRUE,
  scale_dummies = TRUE,
  min_coverage = 0.8,
  missing_action = c("impute", "warn", "error"),
  impute = c("median", "none")
)
}
\arguments{
\item{sites}{Data frame with columns \code{environment}, \code{latitude}, \code{longitude},
and (for weather fetching) \code{start_date}, \code{end_date} (the growing window).}

\item{management}{Optional data frame with an \code{environment} column plus
management covariates (numeric or categorical). Categorical columns are
one-hot encoded. Management \strong{doses} (fertiliser, pesticide, irrigation
amount) should be nested within their type first with
\code{\link[=nest_dose_within_type]{nest_dose_within_type()}} -- a raw dose is not comparable across products
(100 kg NPK is not 100 kg urea). Use \code{\link[=add_management_interactions]{add_management_interactions()}} for
other interactions.}

\item{weather, soil}{Optional pre-fetched matrices/data frames (one row per
environment) to use instead of fetching.}

\item{fetch_weather, fetch_soil}{Logical; fetch from coordinates if \code{TRUE}.
Fetched variables are \strong{merged} with any \code{weather}/\code{soil} you supply -- your
own data takes precedence and only the columns you are missing are fetched,
so you can complete a partly-collected dataset automatically.}

\item{weather_pars}{Character vector of NASA POWER parameter codes to fetch
(e.g. \code{c("T2M", "PRECTOTCORR", "WS2M", "GWETROOT", "EVPTRNS")}). Defaults to
the six core variables. See \code{\link[=enviromic_variable_catalog]{enviromic_variable_catalog()}} for codes and
meanings.}

\item{weather_stats}{Optional named vector giving how to aggregate each
requested parameter over the window: \code{"mean"} (default), \code{"sum"} (e.g.
precipitation, evapotranspiration), \code{"min"}, \code{"max"}, \code{"sd"}, or \code{"median"}.}

\item{weather_temporal}{NASA POWER temporal resolution to pull before
aggregating: \code{"daily"} (default), \code{"monthly"}, or \code{"climatology"}
(long-term). The covariate matrix always holds one aggregated value per
site; for stage-specific covariates, set each site's \code{start_date}/\code{end_date}
to the stage of interest and merge the results.}

\item{soil_backend}{Backend used when \code{fetch_soil = TRUE}: \code{"wcs"} (default),
\code{"webdav"}, \code{"rest"}, or \code{"local"}. See \code{\link[=fetch_soilgrids]{fetch_soilgrids()}}.}

\item{soil_properties, soil_depth, soil_quantile}{SoilGrids property names,
depth interval, and prediction statistic to retrieve (see
\code{\link[=fetch_soilgrids]{fetch_soilgrids()}}).}

\item{soil_local_paths}{Named list mapping properties to local raster files,
required when \code{soil_backend = "local"}.}

\item{standardize}{Logical; scale the assembled numeric covariates. Default
\code{TRUE}.}

\item{scale_dummies}{Logical; when \code{standardize = TRUE}, also centre/scale the
0/1 dummy columns (one-hot management, indicators). Default \code{TRUE}; set
\code{FALSE} to leave dummies as 0/1 while still scaling the continuous
weather/soil/dose covariates.}

\item{min_coverage}{Minimum acceptable observed fraction per source-variable.}

\item{missing_action}{\code{"impute"} (default), \code{"warn"}, or \code{"error"}.}

\item{impute}{Explicit missing-data rule: \code{"median"} or \code{"none"}. Imputation
and coverage are returned as matrix attributes rather than being silent.}
}
\value{
An environment-by-covariate numeric matrix (row names = environment),
ready for \code{\link[=build_environment_relationship]{build_environment_relationship()}} with \code{source = "enviromic"}.
Attributes \code{environment_audit}, \code{environment_provenance},
\code{environment_imputation}, and \code{environment_blocks} preserve the source
quality-control record.
}
\description{
Turns site information into the environment-by-covariate matrix that
\code{\link[=build_environment_relationship]{build_environment_relationship()}} needs. It merges three sources:
\describe{
\item{Weather}{Optionally fetched from site GPS coordinates over a growing
window (via the \pkg{nasapower} POWER API) and summarised to per-site
features, or supplied directly.}
\item{Soil}{Optionally fetched from coordinates via ISRIC SoilGrids using a
robust backend (WCS or WebDAV; see \code{\link[=fetch_soilgrids]{fetch_soilgrids()}}), or supplied
directly.}
\item{Management}{User-supplied per-site management such as planting system,
fertilisation, and irrigation. Numeric columns are kept; categorical
columns are one-hot encoded.}
}

Fetching requires internet access and the relevant optional packages; it is
never triggered by examples or tests. When a source cannot be fetched the
function warns and continues with whatever is available.
}
\examples{
mgmt <- data.frame(environment = c("E1", "E2"),
                   planting_system = c("conventional", "no_till"),
                   fertN = c(120, 80))
wx <- data.frame(environment = c("E1", "E2"),
                 mean_temp = c(21.5, 24.0), total_precip = c(410, 300))
X <- build_enviromic_covariates(
  sites = data.frame(environment = c("E1", "E2"),
                     latitude = c(-1.1, 9.0), longitude = c(37.0, 7.5)),
  management = mgmt, weather = wx)
\dontrun{
# Fetch weather and soil directly from the coordinates (needs internet):
X <- build_enviromic_covariates(sites, management = mgmt,
                                fetch_weather = TRUE, fetch_soil = TRUE)
}
}
\seealso{
\code{\link[=build_environment_relationship]{build_environment_relationship()}}, \code{\link[=cluster_environments]{cluster_environments()}}.
}
