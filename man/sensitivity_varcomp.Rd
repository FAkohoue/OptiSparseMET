% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_varcomp.R
\name{sensitivity_varcomp}
\alias{sensitivity_varcomp}
\title{Variance-component sensitivity of a MET design (P3.5)}
\usage{
sensitivity_varcomp(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  lambda_grid = c(0.25, 0.5, 1, 2, 4, 8),
  reps = NULL,
  env_efficiency = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocation_matrix}{A \eqn{J \times E} 0/1 incidence matrix (lines as
rows, environments as columns) with row and column names. Typically
\code{alloc$allocation_matrix} from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{G}{Genomic (or pedigree) relationship matrix with row/column names
covering the lines in \code{allocation_matrix}.}

\item{Sigma_E}{Optional \eqn{E \times E} environment covariance (genetic
correlations across environments). Defaults to the identity (independent
environments, unit variance).}

\item{lambda_grid}{Numeric vector of \eqn{\sigma_e^2/\sigma_g^2} ratios to
evaluate (with \code{sigma_g2} fixed at 1).}

\item{reps}{Optional \eqn{J \times E} matrix of plot counts per cell.
Defaults to \code{allocation_matrix} (one plot per present cell).}

\item{env_efficiency}{Optional length-\eqn{E} vector of within-environment
design efficiency factors \eqn{\epsilon_e \in (0,1]} (the coupling to the
local design). Defaults to 1 for every environment.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A data frame with columns \code{lambda}, \code{mean_PEV}, and \code{CDmean}.
}
\description{
Design optimality is conditional on the assumed variance components. A design
recommended under one variance ratio may not be best under another.
\code{sensitivity_varcomp()} sweeps the residual-to-genetic variance ratio
\eqn{\lambda = \sigma_e^2/\sigma_g^2} for a fixed allocation and reports the
across-TPE mean PEV and CDmean at each value (via \code{\link[=met_information]{met_information()}}), so a
program can judge whether its design choice is robust to variance
misspecification.
}
\seealso{
\code{\link[=met_information]{met_information()}}, \code{\link[=simulate_met]{simulate_met()}}.
}
