% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmarking.R
\name{benchmark_environment_missingness}
\alias{benchmark_environment_missingness}
\title{Stress-test environmental kernels under missing covariates}
\usage{
benchmark_environment_missingness(
  weather = NULL,
  soil = NULL,
  management = NULL,
  geography = NULL,
  missing_rates = c(0, 0.05, 0.1, 0.2),
  n_repeats = 20L,
  target = NULL,
  historical = NULL,
  truth = NULL,
  build_args = list(missing_action = "impute", impute = "median"),
  benchmark_args = list(n_boot = 0L),
  seed = 1L
)
}
\arguments{
\item{weather, soil, management, geography}{Environmental blocks accepted by
\code{\link[=build_environment_kernels]{build_environment_kernels()}}.}

\item{missing_rates}{Fractions of eligible covariate cells masked completely
at random. Include zero for the unperturbed reference.}

\item{n_repeats}{Number of masks at each missingness level.}

\item{target, historical, truth}{Evidence inputs passed to
\code{\link[=benchmark_environment_models]{benchmark_environment_models()}}.}

\item{build_args}{Named list of additional arguments passed to
\code{\link[=build_environment_kernels]{build_environment_kernels()}}.}

\item{benchmark_args}{Named list of additional arguments passed to
\code{\link[=benchmark_environment_models]{benchmark_environment_models()}}. The default disables bootstrapping inside
each perturbation.}

\item{seed}{Integer random seed.}
}
\value{
A list with run-level model scores, missingness-level summaries,
masking diagnostics, a kernel-rebuild warning ledger, and the rebuilt
kernel objects. Expected quality-control warnings caused by deliberate
masking are captured in the ledger rather than emitted to the caller.
}
\description{
\code{benchmark_environment_missingness()} repeatedly masks observed weather,
soil, management, and geographic cells, rebuilds the kernels through the
package quality-control and imputation workflow, and scores the resulting
covariance candidates against response evidence or a known truth.
}
