% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_design.R
\name{robust_design_score}
\alias{robust_design_score}
\title{Robust (Bayesian / maximin / CVaR) score of a design over a prior}
\usage{
robust_design_score(
  allocation_matrix,
  G,
  Sigma_E,
  scenarios,
  aggregate = c("mean", "cvar", "min"),
  cvar_alpha = 0.25,
  ...
)
}
\arguments{
\item{allocation_matrix, G, Sigma_E}{The design and relationship inputs.}

\item{scenarios}{A list from \code{\link[=robust_scenarios]{robust_scenarios()}}.}

\item{aggregate}{\code{"mean"} (Bayes), \code{"min"} (maximin), or \code{"cvar"}.}

\item{cvar_alpha}{Tail fraction for \code{"cvar"} (e.g. 0.25 = worst quarter).}

\item{...}{Further arguments passed to \code{\link[=design_objective]{design_objective()}} (e.g. \code{weights},
\code{ref}, \code{prop}, \code{cost_per_plot}, \code{budget}, \code{trait_weights}).}
}
\value{
A list with \code{score} (aggregated), \code{aggregate}, and per-scenario
\code{scenario_scores}, \code{scenario_probs}, and covariance-candidate labels.
}
\description{
Evaluates \code{\link[=design_objective]{design_objective()}} under every scenario from \code{\link[=robust_scenarios]{robust_scenarios()}}
and aggregates the scores into one robust value. \code{"mean"} gives the
Bayes-optimal expected objective over the prior; \code{"min"} gives the worst-case
(maximin) objective; \code{"cvar"} gives the conditional value-at-risk -- the mean
of the worst \code{cvar_alpha} fraction of scenarios -- a tunable risk-averse
middle ground.
}
\seealso{
\code{\link[=robust_scenarios]{robust_scenarios()}}, \code{\link[=optimize_design]{optimize_design()}}.
}
