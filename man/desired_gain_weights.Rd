% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/desired_gain_index.R
\name{desired_gain_weights}
\alias{desired_gain_weights}
\title{Multi-trait index weights from desired genetic gains}
\usage{
desired_gain_weights(
  desired_gain,
  gen_cov,
  lower_is_better = NULL,
  method = c("pesek_baker", "dgqgsi"),
  dgqgsi_result = NULL,
  weights_field = NULL
)
}
\arguments{
\item{desired_gain}{Named (recommended) numeric vector of desired gain
magnitudes, one per trait.}

\item{gen_cov}{Trait genetic (co)variance matrix; if it has dimnames it is
aligned to \code{names(desired_gain)}.}

\item{lower_is_better}{Optional trait names (or indices) for which a decrease
is the improvement; their desired gain is negated.}

\item{method}{\code{"pesek_baker"} (default) or \code{"dgqgsi"}.}

\item{dgqgsi_result}{For \code{method = "dgqgsi"}: a result object/list from the
\pkg{DGQGSI} package.}

\item{weights_field}{Optional name of the element in \code{dgqgsi_result} holding
the numeric weight vector, if auto-detection fails.}
}
\value{
A list with \code{weights} (the index weights \eqn{b}), \code{sigma_index}
(\eqn{\sqrt{b'Gb}}, the index genetic SD), \code{desired_gain} (signed),
\code{gen_cov} (aligned), and \code{method}.
}
\description{
Economic weights are hard to elicit and subjective. A more intuitive
alternative is to state \emph{desired genetic gains} directly -- "raise yield by
1.5 units while lowering disease score by 1 unit" -- and derive the index
weights that deliver gains in those proportions. \code{desired_gain_weights()}
returns those weights, ready to use as the multi-trait objective in
\code{\link[=optimize_design]{optimize_design()}} / \code{\link[=design_objective]{design_objective()}} / \code{\link[=expected_genetic_gain]{expected_genetic_gain()}} (pass
them as \code{trait_weights} with \code{trait_gencov = gen_cov}).
}
\details{
Two sources are supported:
\describe{
\item{\code{"pesek_baker"} (default, built-in)}{The classical desired-gain index
of Pesek & Baker (1969): with genetic (co)variance matrix \eqn{G} and
signed desired gains \eqn{d}, the weights are \eqn{b = G^{-1} d}. Because
the expected genetic gains from index selection are proportional to
\eqn{G b}, this makes the realised gains proportional to \eqn{d} exactly.
Deterministic and dependency-free.}
\item{\code{"dgqgsi"}}{Extracts the optimised index-weight vector from a result
produced by the \pkg{DGQGSI} package (Akohoue 2026;
\code{run_desired_gain_index_Joukhadar2024()} / \code{run_dgsi_qgsi_pipeline()}),
which optimises desired-gain indices (and a quadratic genomic variant)
from breeding data. Pass the result as \code{dgqgsi_result}; if the weight
vector is stored under a non-standard name, give it with \code{weights_field}.}
}
Desired gains are supplied as positive magnitudes; traits named in
\code{lower_is_better} have their sign flipped internally (the same convention as
\pkg{DGQGSI}), so a smaller value counts as a gain.
}
\examples{
G <- matrix(c(1, .3, -.2, .3, .8, .1, -.2, .1, .5), 3,
            dimnames = list(c("YLD", "DIS", "MOI"), c("YLD", "DIS", "MOI")))
dg <- c(YLD = 1.5, DIS = 1.0, MOI = 1.0)
w <- desired_gain_weights(dg, gen_cov = G, lower_is_better = "DIS")
w$weights
}
\references{
Pesek, J., & Baker, R. J. (1969). Desired improvement in relation to selection
indices. \emph{Canadian Journal of Plant Science}, 49, 803-804.

Akohoue, F. (2026). DGQGSI: Desired-Gain Selection Index and Quadratic Genomic
Selection Index tools for breeding programs. R package.
}
\seealso{
\code{\link[=expected_genetic_gain]{expected_genetic_gain()}}, \code{\link[=design_objective]{design_objective()}}, \code{\link[=optimize_design]{optimize_design()}}.
}
