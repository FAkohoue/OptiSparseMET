% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_relationship_matrix.R
\name{kinship_pca}
\alias{kinship_pca}
\title{Principal components of a kinship/relationship matrix}
\usage{
kinship_pca(K, ncp = NULL)
}
\arguments{
\item{K}{A kinship/relationship matrix (ideally a tuned, PD one).}

\item{ncp}{Number of principal components to return. Default: all.}
}
\value{
A list with \code{values} (eigenvalues), \code{scores} (individual PC scores),
and \code{prop_var} (proportion of variance per PC). When \pkg{ASRgenomics} is
used its native object is returned instead.
}
\description{
Population-structure principal components from a relationship matrix, useful
for structure-aware allocation grouping (\code{\link[=derive_allocation_groups]{derive_allocation_groups()}}) and
diagnostics. Uses \code{ASRgenomics::kinship.pca()} when available, otherwise a
symmetric eigen-decomposition fallback.
}
\examples{
set.seed(1)
M <- matrix(sample(0:2, 30 * 200, TRUE), 30, 200); rownames(M) <- paste0("L", 1:30)
G <- tune_relationship_matrix(build_relationship_matrix(markers = M, type = "genomic"))
pcs <- kinship_pca(G, ncp = 5)
}
\seealso{
\code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}}, \code{\link[=derive_allocation_groups]{derive_allocation_groups()}}.
}
