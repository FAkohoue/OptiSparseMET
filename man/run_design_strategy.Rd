% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_design_strategy.R
\name{run_design_strategy}
\alias{run_design_strategy}
\title{Run the sparse-MET decision-point framework end to end}
\usage{
run_design_strategy(
  treatments,
  environments = NULL,
  n_test_entries_per_environment,
  G = NULL,
  Sigma_E = NULL,
  env_relationship = NULL,
  n_environments = NULL,
  env_select_method = "representative",
  n_individuals = NULL,
  individual_criterion = "cdmean",
  allocation_method = "random_balanced",
  target_replications = NULL,
  common_treatments = NULL,
  balance = "none",
  refine_gxe = FALSE,
  recommend_reps = FALSE,
  replication_levels = c(1, 1.5, 2),
  seed_available = NULL,
  seed_required_per_plot = NULL,
  evaluate = TRUE,
  n_sim = 30L,
  sigma_g2 = 1,
  sigma_e2 = 1,
  bv_target = "across_tpe",
  target_envs = NULL,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{treatments}{Character vector of all candidate line IDs.}

\item{environments}{Character vector of environment names. If \code{NULL}, they are
chosen from \code{env_relationship} (decision 1).}

\item{n_test_entries_per_environment}{Entries per environment (decision 3).}

\item{G, Sigma_E}{Genomic relationship and environment covariance, required for
individual selection, G x E refinement, and evaluation.}

\item{env_relationship, n_environments, env_select_method}{Decision 1: choose
\code{n_environments} from the \code{env_relationship} matrix
(see \code{\link[=build_environment_relationship]{build_environment_relationship()}} and \code{\link[=select_environments]{select_environments()}}).}

\item{n_individuals, individual_criterion}{Decision 2: choose \code{n_individuals}
tested lines from \code{treatments} by a training-set criterion
(see \code{\link[=select_individuals]{select_individuals()}}). If \code{NULL}, all \code{treatments} are used.}

\item{allocation_method, target_replications, common_treatments, balance}{Decision 3: passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{refine_gxe}{Logical. If \code{TRUE} (and \code{G}/\code{Sigma_E} supplied), refine the
allocation with \code{\link[=optimize_allocation_gxe]{optimize_allocation_gxe()}}.}

\item{recommend_reps}{Logical. If \code{TRUE}, run \code{\link[=recommend_replication]{recommend_replication()}}
(decision 4) on the allocation.}

\item{replication_levels, seed_available, seed_required_per_plot}{Passed to
\code{\link[=recommend_replication]{recommend_replication()}}.}

\item{evaluate}{Logical. If \code{TRUE} (and \code{G}/\code{Sigma_E} supplied), evaluate the
final allocation with \code{\link[=met_information]{met_information()}} and \code{\link[=simulate_met]{simulate_met()}}.}

\item{n_sim, sigma_g2, sigma_e2, bv_target, target_envs}{Evaluation settings
passed to \code{\link[=simulate_met]{simulate_met()}}.}

\item{seed, max_dim}{Reproducibility and size controls.}
}
\value{
A list with \code{decisions} (chosen \code{environments}, \code{individuals},
\code{allocation_method}), \code{allocation_matrix} (final, after any refinement),
\code{replication} (from \code{\link[=recommend_replication]{recommend_replication()}} or \code{NULL}), and \code{evaluation}
(\code{met_information} summary and \code{simulate_met} outcomes, or \code{NULL}).
}
\description{
\code{run_design_strategy()} chains the Colmant et al. (2026) decision points into
a single call so a strategy can be specified and evaluated in one place:
(1) choose environments, (2) choose the tested individuals, (3) allocate
entries across environments (optionally refined for G x E prediction), (4)
recommend replication, and finally evaluate the result. Each stage is
optional -- supply only the pieces relevant to the decision you are testing --
and each underlying tool remains usable on its own. For the plantable field
book, pass the returned allocation to \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.
}
\seealso{
\code{\link[=select_environments]{select_environments()}}, \code{\link[=select_individuals]{select_individuals()}},
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, \code{\link[=optimize_allocation_gxe]{optimize_allocation_gxe()}},
\code{\link[=recommend_replication]{recommend_replication()}}, \code{\link[=simulate_met]{simulate_met()}}, \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}}.
}
