% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/allocate_sparse_met.R
\name{allocate_sparse_met}
\alias{allocate_sparse_met}
\title{Allocate test treatments across environments for sparse MET designs}
\usage{
allocate_sparse_met(
  treatments,
  environments,
  allocation_method = c("random_balanced", "balanced_incomplete", "M3", "M4"),
  n_test_entries_per_environment,
  target_replications = NULL,
  common_treatments = NULL,
  allocation_group_source = c("none", "Family", "GRM", "A"),
  treatment_info = NULL,
  GRM = NULL,
  A = NULL,
  id_map = NULL,
  group_method = c("kmeans", "hclust"),
  group_seed = 1,
  group_attempts = 25,
  n_pcs_use = Inf,
  min_groups_per_environment = NULL,
  min_env_per_group = NULL,
  balance_groups_across_env = TRUE,
  force_group_connectivity = TRUE,
  allow_approximate = FALSE,
  seed = NULL
)
}
\arguments{
\item{treatments}{Character vector of test treatment IDs to allocate across
environments. Check treatments should not be included here; they are
handled separately within the within-environment design functions. Duplicate
values are silently removed.}

\item{environments}{Character vector of environment names. Must contain at
least two elements. Duplicate values are silently removed.}

\item{allocation_method}{Character scalar. Sparse allocation strategy.
Accepted values are \code{"random_balanced"} (M3-type stochastic allocation)
and \code{"balanced_incomplete"} (M4-type BIBD-inspired allocation). The
aliases \code{"M3"} and \code{"M4"} are also accepted and translated internally to
their canonical names before any further processing.}

\item{n_test_entries_per_environment}{Integer scalar or integer vector. Total
number of test treatments per environment including common treatments. If a
scalar, it applies uniformly to all environments. If a vector, its length
must equal the number of environments. All values must be positive and must
be at least \code{length(common_treatments)}. Use \code{\link[=suggest_safe_k]{suggest_safe_k()}} to choose
a value that guarantees full treatment coverage.}

\item{target_replications}{Optional positive integer. Target number of
environments in which each non-common treatment should appear. For
\code{"random_balanced"}, this is a soft target that guides the phase-two
filling. For \code{"balanced_incomplete"}, this is the strict replication level
required for an exact balanced solution. If \code{NULL}, the function infers a
value as \code{floor(total_sparse_slots / n_sparse_treatments)}, with a minimum
of 1.}

\item{common_treatments}{Optional character vector of treatment IDs to assign
to all environments. Placed before sparse allocation begins and excluded
from the sparse pool. Values not present in \code{treatments} are silently
dropped.}

\item{allocation_group_source}{Character scalar. Controls whether and how
genetic group structure guides allocation. \code{"none"} disables group-guided
allocation. \code{"Family"} uses \code{treatment_info$Family}. \code{"GRM"} derives
clusters from \code{GRM}. \code{"A"} derives clusters from \code{A}. Active in both
allocation phases when set to anything other than \code{"none"}.}

\item{treatment_info}{Optional data frame. Required when
\code{allocation_group_source = "Family"}. Must contain columns \code{Treatment} and
\code{Family}. When \code{allocation_group_source \%in\% c("GRM", "A")}, this argument
is optional but used to anchor the number of clusters when supplied.}

\item{GRM}{Optional numeric matrix. Genomic relationship matrix. Required
when \code{allocation_group_source = "GRM"}. Row and column names must match
treatment IDs or be reachable through \code{id_map}.}

\item{A}{Optional numeric matrix. Pedigree-based numerator relationship
matrix. Required when \code{allocation_group_source = "A"}. Same naming
requirements as \code{GRM}.}

\item{id_map}{Optional data frame with columns \code{Treatment} and \code{LineID}.
Required only when \code{allocation_group_source \%in\% c("GRM", "A")} and
treatment IDs do not match the row/column names of the relationship matrix.}

\item{group_method}{Character scalar. Clustering algorithm applied to the
principal components of \code{GRM} or \code{A}. \code{"kmeans"} or \code{"hclust"}. Ignored
when \code{allocation_group_source \%in\% c("none", "Family")}.}

\item{group_seed}{Integer. Seed for k-means initialization. Active only when
\code{allocation_group_source \%in\% c("GRM", "A")} and
\code{group_method = "kmeans"}.}

\item{group_attempts}{Integer. Number of random restarts for k-means.
Active only when \code{allocation_group_source \%in\% c("GRM", "A")} and
\code{group_method = "kmeans"}.}

\item{n_pcs_use}{Integer or \code{Inf}. Number of leading principal components
retained for matrix-based clustering. \code{Inf} retains all components
corresponding to positive eigenvalues. Ignored when
\code{allocation_group_source \%in\% c("none", "Family")}.}

\item{min_groups_per_environment}{Optional positive integer. Minimum number
of allocation groups that should be represented in each environment, where
feasible given available treatments. Active in phase two when
\code{allocation_group_source} is not \code{"none"}.}

\item{min_env_per_group}{Optional positive integer. Minimum number of
environments in which each allocation group should appear, where feasible.
Used in phase two when \code{force_group_connectivity = TRUE}.}

\item{balance_groups_across_env}{Logical. If \code{TRUE}, phase two preferentially
assigns treatments from groups whose current allocation count is below
their proportional target. Active when \code{allocation_group_source} is not
\code{"none"}.}

\item{force_group_connectivity}{Logical. If \code{TRUE}, phase two preferentially
assigns treatments from groups that have not yet appeared in
\code{min_env_per_group} environments. Active when \code{allocation_group_source} is
not \code{"none"} and \code{min_env_per_group} is not \code{NULL}.}

\item{allow_approximate}{Logical, default \code{FALSE}. When \code{FALSE}, an
infeasible exact balanced incomplete allocation triggers an error. When
\code{TRUE}, the function falls back to the closest feasible approximate
allocation. For \code{"random_balanced"}, this argument has no effect on the
error behaviour since that strategy never requires exact balance.}

\item{seed}{Optional integer. Random seed for reproducibility. Controls
the random order in which sparse treatments are processed in phase one
and the stochastic tie-breaking in phase two under \code{"random_balanced"},
as well as tie-breaking in the strict exact constructor. If \code{NULL}, no
seed is set and results may differ across runs; the seed used internally
is returned as \code{seed_used}.}
}
\value{
A named list with the following components:
\describe{
\item{\code{allocation_matrix}}{Binary integer matrix of dimension
\verb{n_treatments x n_environments} with \code{dimnames} set to \code{treatments} and
\code{environments}. Entry \verb{[i, e]} is \code{1L} if treatment \code{i} is assigned to
environment \code{e}, and \code{0L} otherwise. Every non-common treatment is
guaranteed to have row sum at least 1.}
\item{\code{allocation_long}}{Long-format data frame with one row per
treatment-by-environment combination. Columns: \code{Treatment},
\code{Environment}, \code{Assigned} (integer 0/1), \code{IsCommonTreatment} (logical),
and \code{AllocationGroup} (character, present when
\code{allocation_group_source} is not \code{"none"}).}
\item{\code{overlap_matrix}}{Square integer matrix of dimension
\verb{n_environments x n_environments} giving the number of treatments shared
between each pair of environments. Diagonal entries give the total
treatment count per environment.}
\item{\code{line_replications}}{Named integer vector of length \code{n_treatments}.
Row sums of \code{allocation_matrix}: the number of environments each
treatment enters.}
\item{\code{environment_sizes}}{Named integer vector of length \code{n_environments}.
Column sums of \code{allocation_matrix}: the number of treatments assigned to
each environment.}
\item{\code{group_assignment}}{Data frame with columns \code{Treatment} and
\code{AllocationGroup}, one row per treatment. \code{NULL} when
\code{allocation_group_source = "none"}.}
\item{\code{group_by_environment}}{Data frame summarizing the count of assigned
treatments from each allocation group in each environment. Columns:
\code{Environment}, \code{AllocationGroup}, \code{n_treatments}. \code{NULL} when
\code{allocation_group_source = "none"}.}
\item{\code{group_overlap_matrix}}{Square integer matrix of dimension
\verb{n_environments x n_environments} giving the number of shared allocation
groups between each pair of environments. \code{NULL} when
\code{allocation_group_source = "none"}.}
\item{\code{summary}}{Named list with allocation metadata: \code{allocation_method},
\code{allocation_group_source}, \code{target_replications}, \code{n_treatments_total},
\code{n_sparse_treatments}, \code{n_common_treatments}, \code{total_sparse_slots},
\code{environment_sizes}, \code{min_replication}, \code{max_replication},
\code{mean_replication}, \code{min_sparse_replication}, \code{max_sparse_replication},
\code{mean_sparse_replication}, \code{min_common_replication},
\code{max_common_replication}, \code{mean_common_replication}, and \code{n_groups}.
The overall replication summaries include common treatments, whereas the
sparse-only summaries reflect the quantities relevant for strict M4
equality checks.}
\item{\code{seed_used}}{The integer seed passed to \code{set.seed()} internally, or
\code{NULL} if no seed was supplied.}
}
}
\description{
Two allocation strategies are available. \code{"random_balanced"} implements an
M3-type stochastic allocation that approximates balance without requiring
exact BIBD parameters to be satisfiable — appropriate when environment
capacities differ or when the trial dimensions do not admit an exact balanced
solution. \code{"balanced_incomplete"} implements an M4-type allocation following
BIBD principles at the MET level. When \code{allow_approximate = FALSE} and the
slot identity is satisfied, the function uses an exact constructor that
enforces equal replication of all non-common treatments. When
\code{allow_approximate = TRUE}, it relaxes exact balance and constructs the
closest feasible allocation.

Allocation proceeds in two phases for \code{"random_balanced"} and approximate
\code{"balanced_incomplete"}. Phase one guarantees that every non-common
treatment appears in at least one environment, distributing treatments across
environments with awareness of genetic group structure when
\code{allocation_group_source} is not \code{"none"}. Phase two fills the remaining
capacity in each environment up to \code{n_test_entries_per_environment}, guided
by replication targets and group-balance penalties. For strict
\code{"balanced_incomplete"} (\code{allow_approximate = FALSE}), allocation is carried
out by a separate exact constructor rather than the two-phase heuristic.

Before allocation begins, the function calls
\code{.check_full_coverage_feasibility()} to verify that the total number of
sparse slots across all environments is sufficient to assign every non-common
treatment at least once. If it is not, the function stops with an informative
error naming the deficit and the minimum capacity that would resolve it. Use
\code{\link[=suggest_safe_k]{suggest_safe_k()}} or \code{\link[=min_k_for_full_coverage]{min_k_for_full_coverage()}} to choose a feasible
\code{n_test_entries_per_environment} before calling this function.
}
\details{
\code{allocate_sparse_met()} constructs the treatment-by-environment incidence
structure for a sparse multi-environment trial. Given a set of candidate
lines and a set of environments, the function determines which lines enter
which environments subject to replication targets, capacity constraints,
optional common treatment requirements, and optional genetic structure
constraints. The function guarantees that every non-common treatment is
assigned to at least one environment before attempting to fill additional
replication slots.
\subsection{Relation to sparse testing theory}{

The allocation strategies implemented here correspond to M3 and M4 in
Montesinos-López et al. (2023). The underlying resource identity is:

\deqn{N = J \times r = I \times k}

where \eqn{J} is the number of lines, \eqn{I} is the number of environments,
\eqn{k} is the number of lines per environment, \eqn{r} is the number of
environments each line enters, and \eqn{N} is the total number of
line-by-environment observations. This identity makes the tradeoff between
coverage (\eqn{k}) and replication depth (\eqn{r}) explicit given fixed
\eqn{N}.
}

\subsection{Common treatments}{

Treatments in \code{common_treatments} are assigned to every environment before
sparse allocation begins and are excluded from the sparse allocation pool.
The per-environment sparse capacity is:

\deqn{k_e^* = k_e - C}

where \eqn{C} is the number of common treatments and \eqn{k_e} is the total
capacity of environment \eqn{e}. Common treatments establish direct
cross-environment connectivity that does not depend on the relationship
structure in the covariance model, and provide stable references for
estimating environment-level effects. They are most important when
environments are weakly correlated.
}

\subsection{Two-phase allocation}{

For \code{"random_balanced"} and approximate \code{"balanced_incomplete"}, phase one
iterates over sparse treatments in random order and assigns each to one
environment, preferring environments where the treatment's genetic group is
not yet represented when \code{allocation_group_source} is active. Phase two
iterates over environments in decreasing order of remaining capacity and
fills each to its target, guided by line-level replication deficit scores and
group-balance penalties.
}

\subsection{Allocation groups}{

When \code{allocation_group_source} is not \code{"none"}, both phases use genetic
group membership to guide assignments. Family-based grouping reads labels
from \code{treatment_info$Family}. GRM-based and A-based grouping derive clusters
from the leading principal components of the respective relationship matrix.
In phase two, the allocator penalizes assignments that concentrate a group
in too few environments (\code{balance_groups_across_env}) and gives preference to
groups that have not yet reached \code{min_env_per_group}
(\code{force_group_connectivity}). Line-level replication targets are preserved
within these group-level constraints.
}

\subsection{Feasibility of balanced incomplete allocation}{

For a fully balanced incomplete allocation, the total number of sparse slots
must satisfy:

\deqn{\sum_{e=1}^{I} k_e^* = J^* \times r}

where \eqn{J^*} is the number of non-common treatments and \eqn{r} is the
target replication. When this equality does not hold and
\code{allow_approximate = FALSE}, the function stops. When the equality holds and
\code{allow_approximate = FALSE}, the function attempts an exact constructor that
enforces equal replication for all non-common treatments. When
\code{allow_approximate = TRUE}, the function constructs the closest feasible
allocation, accepting minor deviations from perfect balance.
}
}
\examples{
treatments <- paste0("L", sprintf("\%03d", 1:120))
envs       <- c("Env1", "Env2", "Env3", "Env4")
fam        <- rep(paste0("F", 1:6), each = 20)

treatment_info <- data.frame(
  Treatment = treatments,
  Family    = fam,
  stringsAsFactors = FALSE
)

## Check a safe per-environment capacity before running allocation
suggest_safe_k(treatments, envs, buffer = 3)  # 33

## Example 1: random balanced allocation with family-guided group spreading
out1 <- allocate_sparse_met(
  treatments                     = treatments,
  environments                   = envs,
  allocation_method              = "random_balanced",
  n_test_entries_per_environment = 33,
  target_replications            = 1,
  allocation_group_source        = "Family",
  treatment_info                 = treatment_info,
  min_groups_per_environment     = 4,
  min_env_per_group              = 2,
  seed                           = 123
)

out1$summary
head(out1$allocation_long)
head(out1$group_by_environment)

## Example 2: strict balanced incomplete allocation with common treatments.
## Here the sparse slot totals are exactly feasible, so all non-common
## treatments are replicated exactly `target_replications` times.
out2 <- allocate_sparse_met(
  treatments                     = treatments,
  environments                   = envs,
  allocation_method              = "balanced_incomplete",
  n_test_entries_per_environment = 65,
  target_replications            = 2,
  common_treatments              = treatments[1:10],
  allow_approximate              = FALSE,
  seed                           = 123
)

out2$summary

}
\references{
Montesinos-López, O. A., Mosqueda-González, B. A., Salinas-Ruiz, J.,
Montesinos-López, A., & Crossa, J. (2023). Sparse multi-trait genomic
prediction under balanced incomplete block design. \emph{The Plant Genome},
16, e20305.
}
\seealso{
\code{\link[=suggest_safe_k]{suggest_safe_k()}} and \code{\link[=min_k_for_full_coverage]{min_k_for_full_coverage()}} for choosing a feasible
\code{n_test_entries_per_environment} before calling this function.
\code{\link[=check_balanced_incomplete_feasibility]{check_balanced_incomplete_feasibility()}} for verifying the slot identity
before attempting a \code{"balanced_incomplete"} allocation.
\code{\link[=derive_allocation_groups]{derive_allocation_groups()}} for inspecting the group structure that guides
allocation when \code{allocation_group_source} is not \code{"none"}.
}
