% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparse_met_design.R
\name{sparse_met_design}
\alias{sparse_met_design}
\title{Create a validated sparse-MET design object}
\usage{
sparse_met_design(
  allocation_matrix,
  reps = NULL,
  fieldbooks = NULL,
  G = NULL,
  Sigma_E = NULL,
  tpe_weights = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  diagnostics = list(),
  provenance = list(),
  validate = TRUE
)
}
\arguments{
\item{allocation_matrix}{Named treatment-by-environment 0/1 matrix.}

\item{reps}{Optional named integer plot-count matrix. Defaults to one plot per
allocated cell.}

\item{fieldbooks}{Optional named list of environment fieldbooks, each with a
\code{Treatment} column. Treatments not in the allocation (for example checks)
are allowed, but allocated treatments must reconcile exactly with \code{reps}.}

\item{G, Sigma_E}{Optional genetic and environment covariance matrices.}

\item{tpe_weights}{Optional target-population weights.}

\item{sigma_g2, sigma_e2}{Variance assumptions.}

\item{diagnostics, provenance}{Named metadata lists.}

\item{validate}{Logical; run \code{\link[=validate_sparse_met_design]{validate_sparse_met_design()}} immediately.}
}
\value{
An object of class \code{sparse_met_design}.
}
\description{
Packages the allocation, realised integer replication, fieldbooks, covariance
assumptions, and optimisation provenance into one versioned object. The
constructor runs cross-component QA so a design cannot silently disagree
with its plantable fieldbooks.
}
