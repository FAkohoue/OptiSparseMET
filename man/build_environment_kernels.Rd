% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment_kernels.R
\name{build_environment_kernels}
\alias{build_environment_kernels}
\title{Build separate environmental kernels by information modality}
\usage{
build_environment_kernels(
  weather = NULL,
  soil = NULL,
  management = NULL,
  geography = NULL,
  environments = NULL,
  kernels = NULL,
  bandwidth_multipliers = c(0.5, 1, 2),
  min_coverage = 0.8,
  missing_action = c("impute", "warn", "error"),
  impute = c("median", "none"),
  include_interactions = FALSE,
  interaction_terms = NULL,
  interaction_mode = c("anova", "product"),
  variable_interactions = NULL,
  variable_interaction_control = list()
)
}
\arguments{
\item{weather, soil, management, geography}{Optional environment-by-variable
matrices/data frames. Supply row names or an \code{environment} column.
\code{geography} needs latitude and longitude.}

\item{environments}{Optional required environment order. By default the union
of environment names across supplied blocks is used.}

\item{kernels}{Optional named character vector choosing \code{"gaussian"} or
\code{"correlation"} per non-geographic block.}

\item{bandwidth_multipliers}{Positive Gaussian bandwidth multipliers around
the median pairwise distance. Averaging several bandwidths reduces
sensitivity in small environment networks.}

\item{min_coverage}{Minimum source coverage passed to
\code{\link[=qc_environmental_data]{qc_environmental_data()}}.}

\item{missing_action, impute}{Missing-data policy passed to
\code{\link[=qc_environmental_data]{qc_environmental_data()}}.}

\item{include_interactions}{Logical. When \code{TRUE}, add all available pairwise
weather-by-soil, weather-by-management, and soil-by-management kernels.}

\item{interaction_terms}{Optional named list defining an exact set of
interactions, for example
\code{list(weather_x_soil_x_management = c("weather", "soil", "management"))}.
Supplying this argument also enables interaction construction.}

\item{interaction_mode}{\code{"anova"} (default) constructs functional-ANOVA
interactions from separately centred parent kernels, reducing main-effect
leakage. \code{"product"} retains the legacy raw Hadamard product.}

\item{variable_interactions}{Optional dedicated variable-level hypotheses
passed to \code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}}. Use these for a short,
pre-specified set of interactions that must be represented and audited
separately, such as temperature by root-zone bulk density. The broad
modality interactions controlled by \code{include_interactions} remain distinct
omnibus uncertainty structures.}

\item{variable_interaction_control}{Optional named list of advanced arguments
passed to \code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}}, excluding \code{covariates}
and \code{interactions}. This keeps the main interface compact while allowing
control of bandwidths, residualisation, hierarchy, and tensor dimension.}
}
\value{
A list with \code{kernels}, aligned \code{covariates}, \code{audit}, \code{provenance},
\code{imputation}, realised \code{bandwidths}, and dedicated variable-interaction
diagnostics when requested.
}
\description{
Constructs weather, soil, management, and geographic kernels separately.
This prevents a high-dimensional source (usually weather) from receiving
accidental extra weight merely because it has more columns. Continuous
blocks use an ensemble of Gaussian bandwidths by default. Categorical
management is one-hot encoded within its own block. Every block is aligned,
quality controlled, imputed explicitly, and normalised before combination.
}
