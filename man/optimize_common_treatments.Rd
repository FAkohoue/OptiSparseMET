% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_common_treatments.R
\name{optimize_common_treatments}
\alias{optimize_common_treatments}
\title{Robustly optimise a common-treatment set}
\usage{
optimize_common_treatments(
  G,
  Sigma_E,
  treatment_info,
  seed_info,
  seed_required_per_plot,
  entry_capacities,
  n_common = NULL,
  minimum_seed_buffer = 0,
  scenarios = NULL,
  aggregate = c("cvar", "mean", "min"),
  cvar_alpha = 0.25,
  env_efficiency = 1,
  target_se = 0.15,
  pair_aggregate = c("maximin", "cvar", "mean"),
  pair_cvar_alpha = 0.25,
  min_per_family = 1L,
  n_groups = NULL,
  objective_weights = c(reliability = 0.4, connectivity = 0.3, genetic_diversity = 0.15,
    testing_breadth = 0.15),
  require_full_coverage = TRUE,
  max_count_evaluations = 15L,
  seed = NULL
)
}
\arguments{
\item{G}{Named genomic or pedigree relationship matrix.}

\item{Sigma_E}{Named environment covariance matrix.}

\item{treatment_info}{Data frame with columns \code{Treatment} and \code{Family}.}

\item{seed_info}{Data frame with columns \code{Treatment} and \code{SeedAvailable}.}

\item{seed_required_per_plot}{Positive environment-specific mandatory seed
debit for one selected common presence, as a named vector or a data frame
with columns \code{Environment} and \code{SeedRequiredPerPlot}. Supply the per-plot
requirement when one plot is mandatory; multiply it by the mandatory local
plot count when the local design requires more than one plot.}

\item{entry_capacities}{Named positive integer vector giving the number of
distinct candidate treatments accommodated in each environment.}

\item{n_common}{Optional single common-set size. If supplied, the size is
fixed but the function still selects the member identities. If \code{NULL}, a
nested grid of feasible sizes and their candidate identities is optimised.}

\item{minimum_seed_buffer}{Non-negative seed reserve retained for every
treatment after placing one plot of a selected common treatment in every
environment. Default 0.}

\item{scenarios}{Prior from \code{\link[=robust_scenarios]{robust_scenarios()}}. Defaults to residual
variances \code{c(0.5, 1, 2)} and environment-covariance shrinkage
\code{c(0, 0.25, 0.5)}.}

\item{aggregate}{Risk aggregation: \code{"cvar"} (default), \code{"mean"}, or \code{"min"}.}

\item{cvar_alpha}{Lower-tail probability used by CVaR.}

\item{env_efficiency}{Named vector of local-design efficiencies in \verb{(0, 1]}.}

\item{target_se}{Target standard error for pairwise environment correlations.}

\item{pair_aggregate}{How to protect connectivity across environment pairs.
\code{"maximin"} (default) maximises the weakest pair, \code{"cvar"} maximises the
lower-tail mean, and \code{"mean"} maximises average pairwise attainment.}

\item{pair_cvar_alpha}{Lower-tail fraction used only when
\code{pair_aggregate = "cvar"}.}

\item{min_per_family}{Minimum number of common treatments per family.}

\item{n_groups}{Number of genomic groups to cover; defaults to the number of
families, bounded by the feasible candidate pool.}

\item{objective_weights}{Named non-negative weights for \code{reliability},
\code{connectivity}, \code{genetic_diversity}, and \code{testing_breadth}.}

\item{require_full_coverage}{If \code{TRUE}, reject common-set sizes that make it
arithmetically impossible to test every treatment at least once.}

\item{max_count_evaluations}{Maximum number of automatically generated
common-set sizes. The grid spans the entire feasible interval.}

\item{seed}{Optional integer controlling genomic clustering.}
}
\value{
A list containing \code{selected}, \code{n_common}, \code{common_presence} (a binary
common-treatment by environment matrix), \code{comparison}, \code{pareto},
\code{candidate_sets}, \code{selection_diagnostics}, \code{pairwise_connectivity},
\code{seed_ledger}, and \code{rationale}.
}
\description{
Selects the size and composition of the global connectivity set in a sparse
multi-environment trial (MET). A common treatment is required to occur in
every environment. This function decides presence only; local plot
replication is a separate, downstream seed-feasible precision decision.

The set is a design-stage decision, not a quantity that changes during field
deployment. When \code{n_common = NULL}, the function searches feasible sizes and
identities. Supplying \code{n_common} fixes only the number to be selected; the
function still selects the identities. Once the returned \code{selected} vector
is adopted as \code{common_treatments} in allocation or planning, those identities
are fixed as present in every environment of that released design.

Candidate common-set sizes are compared by a multi-criterion, risk-averse
objective combining:
\itemize{
\item prediction reliability of the target-population-of-environments mean;
\item precision of pairwise environment connectivity;
\item genetic effective sample size of the common set; and
\item testing breadth retained for non-common treatments.
}
Reliability is integrated over a prior of variance-component and
environment-covariance scenarios. Pairwise connectivity is the number of
distinct shared treatments. Genetic effective sample size is retained as a
separate diversity criterion, not substituted for the observed
shared-treatment count. Correlation-dependent targets are
calculated separately for every environment pair. By default, the global
common set is chosen with maximin protection of the least-connected pair;
lower-tail CVaR and mean pairwise aggregation are available explicitly.
Family and genomic-group coverage, entry capacities, and one network-wide
seed inventory are enforced.

Replication never increases the pairwise connectivity count: one genotype
shared by two environments remains one shared genotype whether it has one
plot or several plots at either environment. Use
\code{\link[=assign_replication_by_seed]{assign_replication_by_seed()}} or \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}} after allocation
to assign feasible local replication from the remaining seed inventory.
}
\details{
For a proposed common set with relationship submatrix \eqn{G_C}, its genetic
effective sample size is
\deqn{n_{\mathrm{eff}} =
  \{\operatorname{tr}(G_C)\}^2 / \operatorname{tr}(G_C^2).}
Thus duplicated or strongly related anchors reduce the diversity score
without altering the number of distinct shared identities. For environments
\eqn{a,b}, let \eqn{S_{ab}} be the set of distinct genotypes present in both
environments. Connectivity is \eqn{|S_{ab}|}; repeating a member does not
change it. The shared-treatment count is compared with the conservative
correlation-precision
requirement
\eqn{\max_s\{(1-\rho_{ab,s}^2)/\mathrm{target\_se}\}^2} across prior
scenarios \eqn{s}.

Within a scenario, the planning-stage posterior covariance for common
treatment \eqn{j}, using one presence record per environment, is computed
from
\deqn{\left[
  \{\sigma_{g,s}^2 G_{jj}\Sigma_{E,s}\}^{-1} +
  \operatorname{diag}(\epsilon/\sigma_{e,s}^2)
  \right]^{-1}.}
Its TPE-mean PEV is the mean of that matrix. Scenario reliabilities are then
aggregated by their prior mean, worst case, or lower-tail CVaR. These compact
calculations make the common-set search scalable; the delivered whole-trial
design should still be evaluated with \code{\link[=met_information]{met_information()}} and
\code{\link[=simulate_met]{simulate_met()}}.
}
\seealso{
\code{\link[=suggest_common_treatments]{suggest_common_treatments()}}, \code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}},
\code{\link[=robust_scenarios]{robust_scenarios()}}
}
