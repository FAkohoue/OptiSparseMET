% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pareto_designs.R
\name{pareto_designs}
\alias{pareto_designs}
\title{Trade-off (Pareto) frontier of designs over benefit vs cost}
\usage{
pareto_designs(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  cost_weights = seq(0, 1, by = 0.25),
  benefit = c("gain", "reliability"),
  preserve = "none",
  objective = list(),
  ...
)
}
\arguments{
\item{allocation_matrix}{Starting allocation (dimnames required).}

\item{G, Sigma_E}{Relationship inputs.}

\item{cost_weights}{Vector of cost weights to sweep (benefit weight fixed at
1). Larger values buy cheaper, smaller designs. Default \code{seq(0, 1, 0.25)}.}

\item{benefit}{\code{"gain"} (default) or \code{"reliability"} -- the benefit objective
traded against cost.}

\item{preserve}{Move set for \code{\link[=optimize_design]{optimize_design()}}; \code{"none"} (default) lets plot
count vary so cost is actually optimised.}

\item{objective}{Base objective settings (see \code{\link[=optimize_design]{optimize_design()}}); the
benefit and cost weights are set by the sweep.}

\item{...}{Further arguments passed to \code{\link[=optimize_design]{optimize_design()}} (e.g. \code{sigma_g2},
\code{robust}, \code{n_starts}, \code{iters}, \code{seed}).}
}
\value{
A list with \code{frontier} (a data frame: \code{cost_weight}, \code{reliability},
\code{gain}, \code{plots}, \code{cost}, \code{score}, \code{pareto}) and \code{designs} (the list of
allocation matrices, one per cost weight).
}
\description{
There is rarely a single best design once resources matter: a bigger design
predicts better but costs more plots. \code{pareto_designs()} maps that trade-off
by re-running \code{\link[=optimize_design]{optimize_design()}} across a range of cost weights (an
\eqn{\varepsilon}-constraint-style sweep), then flags the \strong{non-dominated}
(Pareto-optimal) designs -- those for which no other design is both cheaper
and at least as good. The breeder reads the frontier and picks the point that
matches their budget, rather than committing to one weighting up front. The
sweep can itself be robust by passing \code{\link[=robust_scenarios]{robust_scenarios()}}.
}
\examples{
set.seed(1)
G <- crossprod(matrix(rnorm(24 * 60), 60, 24)) / 60 + diag(24) * 0.3
dimnames(G) <- list(paste0("L", 1:24), paste0("L", 1:24))
SigE <- diag(3) * 0.5 + 0.5
dimnames(SigE) <- list(paste0("E", 1:3), paste0("E", 1:3))
M <- matrix(0L, 24, 3, dimnames = list(rownames(G), colnames(SigE)))
for (e in 1:3) M[sample(24, 12), e] <- 1L
pf <- pareto_designs(M, G, SigE, cost_weights = c(0, 0.5, 1),
                     n_starts = 1, iters = 30, seed = 1)
pf$frontier
}
\seealso{
\code{\link[=optimize_design]{optimize_design()}}, \code{\link[=design_objective]{design_objective()}}.
}
