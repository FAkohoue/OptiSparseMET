% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consensus_relationship.R
\name{consensus_relationship}
\alias{consensus_relationship}
\title{Robust consensus of several relationship matrices}
\usage{
consensus_relationship(
  matrices,
  method = c("rv_weighted", "geometric", "median", "mean"),
  eps = 1e-08,
  use_off_diagonal = TRUE,
  center = TRUE
)
}
\arguments{
\item{matrices}{A list of relationship matrices over the same individuals.}

\item{method}{\code{"rv_weighted"}, \code{"geometric"}, \code{"median"}, or \code{"mean"}.}

\item{eps}{Eigenvalue floor for the SPD operations. Default 1e-8.}

\item{use_off_diagonal}{For STATIS weighting, compare only unique
off-diagonal relationships. This prevents a common unit diagonal from
artificially inflating agreement.}

\item{center}{Centre the vectorised relationships before computing the
STATIS/RV agreement. Default \code{TRUE}.}
}
\value{
The consensus matrix. For \code{"rv_weighted"} the per-matrix weights are
attached as \code{attr(., "weights")}.
}
\description{
Given the same relationship matrix estimated over several years (or replicates,
data sources), a \emph{consensus} summarises them into one. The naive choice -- the
arithmetic (Euclidean) mean -- is neither robust to an anomalous year nor
geometrically appropriate for covariance/relationship matrices, which live on
the manifold of symmetric positive-definite (SPD) matrices. \code{consensus_relationship()}
offers better options:
\describe{
\item{\code{"rv_weighted"} (default)}{A STATIS-style consensus: each matrix is
weighted by how much it agrees with the others (the leading eigenvector of
the between-matrix RV-coefficient matrix), so an outlier year is
automatically down-weighted. The \strong{robust} choice.}
\item{\code{"geometric"}}{The log-Euclidean mean \eqn{\exp(\overline{\log D_i})},
the principled mean on the SPD manifold (Arsigny et al. 2007); avoids the
variance "swelling" of Euclidean averaging.}
\item{\code{"median"}}{Element-wise median across matrices (resistant to
outliers), projected to the nearest positive-definite matrix.}
\item{\code{"mean"}}{The plain arithmetic mean (Euclidean), provided for
comparison.}
}
}
\examples{
set.seed(1)
mk <- function() { A <- matrix(rnorm(5 * 8), 5); tcrossprod(A) / 8 + diag(5) }
Ds <- list(mk(), mk(), mk())
Cw <- consensus_relationship(Ds, "rv_weighted")
attr(Cw, "weights")
}
\references{
Arsigny, V., Fillard, P., Pennec, X., Ayache, N. (2007). Geometric means in a
novel vector space structure on symmetric positive-definite matrices.
Lavit, C. et al. (1994). The ACT (STATIS method).
}
\seealso{
\code{\link[=assess_envirotype_stability]{assess_envirotype_stability()}}, \code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}}.
}
