% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variable_interaction_kernels.R
\name{assess_variable_interactions}
\alias{assess_variable_interactions}
\title{Assess dedicated environmental interactions with historical MET evidence}
\usage{
assess_variable_interactions(
  kernels,
  target = NULL,
  historical = NULL,
  genotype_col = "genotype",
  environment_col = "environment",
  value_col = "value",
  year_col = NULL,
  interaction_kernels = NULL,
  alpha = 0.05,
  p_adjust = "holm",
  n_boot = 200L,
  seed = NULL
)
}
\arguments{
\item{kernels}{Named list containing main and dedicated interaction kernels,
usually the combined output of \code{\link[=build_environment_kernels]{build_environment_kernels()}} and
\code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}}.}

\item{target}{Optional empirical environment genetic-correlation matrix or
list of year-specific matrices.}

\item{historical}{Optional long data frame of historical adjusted genetic
values. Supply only one of \code{target} and \code{historical}.}

\item{genotype_col, environment_col, value_col}{Column names in \code{historical}.}

\item{year_col}{Optional year column. When present, a correlation target is
constructed for each year and leave-year-out validation is preferred.}

\item{interaction_kernels}{Optional character vector naming the dedicated
interactions. By default, kernels carrying
\code{interaction_class = "variable_level"} are selected.}

\item{alpha}{Family-wise significance level for interaction support.}

\item{p_adjust}{Multiplicity adjustment passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.
Holm's procedure is the default. Use \code{"none"} only for a pre-registered
single hypothesis or an explicitly exploratory analysis.}

\item{n_boot}{Number of pair-bootstrap calibration draws used to report
component inclusion stability.}

\item{seed}{Optional reproducibility seed.}
}
\value{
A list with an interaction-level \code{evidence} table, character vectors
\code{supported} and \code{structural_uncertainty}, the fitted single-interaction
models, and the historical targets used for validation. A supported
interaction has positive mean blocked-validation improvement, an adjusted
one-sided sign-test probability below \code{alpha}, and a positive fitted
covariance contribution.
}
\description{
Evaluates each dedicated variable-level interaction separately against a
common main-effect model. The comparison uses the blocked validation
machinery in \code{\link[=calibrate_environment_covariance]{calibrate_environment_covariance()}}. Leave-target-out folds
are used when several year-specific targets are available; otherwise
leave-environment-out folds are used. One-sided paired sign tests determine
whether adding an interaction reduces prediction error consistently.
Multiplicity is controlled across the assessed interactions by Holm's method
by default.

Without valid historical multi-environment trial (MET) responses, no
interaction is declared supported or unsupported. Every interaction is
labelled \code{structural_uncertainty} and should remain a separate, unweighted
maximin scenario.
}
\section{Interpretation}{

Statistical support means that the dedicated kernel improved prediction of
historical genetic environment correlations under the available blocked
validation. It is not causal evidence. An unsupported interaction is
excluded from the central calibrated covariance but remains a legitimate
structural sensitivity scenario.
}

\examples{
\dontrun{
evidence <- assess_variable_interactions(
  kernels = c(environment_kernels, variable_kernels),
  historical = adjusted_historical_met,
  year_col = "year",
  n_boot = 500,
  seed = 2026
)
evidence$evidence
}
}
