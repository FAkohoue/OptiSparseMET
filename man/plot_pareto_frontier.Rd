% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_pareto_frontier.R
\name{plot_pareto_frontier}
\alias{plot_pareto_frontier}
\title{Plot a cost-minimisation Pareto frontier}
\usage{
plot_pareto_frontier(
  pf,
  x = c("cost", "plots"),
  highlight_pareto = TRUE,
  col_gain = "#0C4C80",
  col_acc = "#087E8B",
  metric = NULL,
  reference_cost = NULL,
  col_dominated = "#B7BDC6",
  label_designs = FALSE,
  ...
)
}
\arguments{
\item{pf}{A \code{\link[=pareto_designs]{pareto_designs()}} result, its \code{frontier} data frame, a
\code{\link[=benchmark_met_designs]{benchmark_met_designs()}} result, or its \code{design_summary} data frame.}

\item{x}{Real resource column: \code{"cost"} (default) or \code{"plots"}.}

\item{highlight_pareto}{Logical. Highlight and join non-dominated designs.}

\item{col_gain, col_acc}{Colours retained for backwards compatibility.
\code{col_gain} is used for gain and \code{col_acc} for accuracy or reliability.}

\item{metric}{One endpoint: \code{"accuracy"}, \code{"gain"}, or \code{"reliability"}.
The default selects accuracy when available, then reliability, then gain.}

\item{reference_cost}{Optional two-element vector \code{c(minimum, maximum)} that
defines the normalisation and the top axis. By default the observed range
is used.}

\item{col_dominated}{Colour for dominated designs.}

\item{label_designs}{Logical. Label points when a \code{design} column is present.}

\item{...}{Passed to \code{\link[graphics:plot]{graphics::plot()}}.}
}
\value{
The frontier data frame ordered by real cost, invisibly. Added
columns record \code{cost_minimisation_percent},
\code{performance_maximisation_percent}, \code{frontier_eligible}, and
\code{pareto_plotted}.
}
\description{
Plots one statistical endpoint against normalised cost minimisation. The
bottom axis runs from 0\% (the most expensive observed design) to 100\% (the
least expensive observed design). A secondary top axis reports the
corresponding real cost in decreasing order. Therefore, a movement to the
right always means stronger cost minimisation, whereas a movement upwards
always means stronger statistical performance.

Only feasible non-dominated designs are joined, using straight segments
between the observed design points. When a \code{feasible} column is present,
infeasible designs remain visible as grey crosses but cannot define the
frontier. Dominated feasible designs remain visible as grey circles. Genetic
gain is expressed as a percentage of the maximum observed gain; accuracy
and reliability are expressed directly as percentages. Gain and accuracy
are never overlaid on different vertical scales.
}
\examples{
fr <- data.frame(
  design = c("Full", "M4", "M3"),
  accuracy = c(0.82, 0.79, 0.72),
  cost = c(10, 6, 1)
)
fr$pareto_accuracy <- c(TRUE, TRUE, TRUE)
plot_pareto_frontier(fr, metric = "accuracy")
}
\seealso{
\code{\link[=pareto_designs]{pareto_designs()}}, \code{\link[=benchmark_met_designs]{benchmark_met_designs()}}.
}
