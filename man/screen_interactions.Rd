% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_selection.R
\name{screen_interactions}
\alias{screen_interactions}
\title{Screen environmental covariates/interactions by variance explained in G x E}
\usage{
screen_interactions(
  gxe,
  env_covariates,
  method = c("anova", "rf"),
  n_scores = 2L,
  top = NULL
)
}
\arguments{
\item{gxe}{Genotype-by-environment numeric matrix of adjusted means
(BLUEs/BLUPs); row names genotypes, column names environments.}

\item{env_covariates}{Environment-by-covariate matrix (row names
environments), typically including candidate interaction columns already
built with \code{\link[=add_management_interactions]{add_management_interactions()}}.}

\item{method}{\code{"anova"} (default; R^2 of the covariate against the leading
G x E scores, no extra dependency) or \code{"rf"} (random-forest importance, needs
\pkg{ranger}).}

\item{n_scores}{Number of leading environment G x E scores to explain.
Default 2.}

\item{top}{Optional number of top covariates to return; default all.}
}
\value{
A data frame sorted by \code{importance} (descending): \code{covariate},
\code{importance}, and \code{method}.
}
\description{
The principled way to decide which covariates or interactions to keep is to
ask which of them explain the observed genotype-by-environment interaction
(G x E) in historical multi-environment data. \code{screen_interactions()} extracts
the environment-level G x E signal from a genotype-by-environment matrix
(double-centring to remove genotype and environment main effects, then an SVD
to get the leading environment G x E scores) and ranks each environmental
covariate by how well it explains those scores. Add only the top few -- the
rest are noise, which is what overfits a kinship built from many interactions.
}
\examples{
set.seed(1)
envs <- paste0("E", 1:6); x <- c(-2, -1, 0, 1, 2, 3)
slopes <- rnorm(20)
gxe <- outer(slopes, x) + matrix(rnorm(120, 0, 0.1), 20)
dimnames(gxe) <- list(paste0("L", 1:20), envs)
cov <- cbind(x = x, noise = rnorm(6)); rownames(cov) <- envs
screen_interactions(gxe, cov)
}
\seealso{
\code{\link[=list_candidate_interactions]{list_candidate_interactions()}},
\code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}},
\code{\link[=assess_variable_interactions]{assess_variable_interactions()}}, \code{\link[=add_management_interactions]{add_management_interactions()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}.
}
