% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch_soil.R
\name{fetch_soilgrids}
\alias{fetch_soilgrids}
\title{Retrieve SoilGrids soil properties at site coordinates}
\usage{
fetch_soilgrids(
  sites,
  backend = c("wcs", "webdav", "rest", "local"),
  properties = c("bdod", "cec", "cfvo", "clay", "nitrogen", "ocd", "phh2o", "sand",
    "silt", "soc"),
  depth = "0-5cm",
  quantile = "mean",
  local_paths = NULL,
  buffer_m = 1000,
  resolution_m = 250,
  apply_conversion = TRUE,
  spatial_summary = c("point", "mean_sd")
)
}
\arguments{
\item{sites}{A data frame with columns \code{environment}, \code{latitude}, \code{longitude}
(WGS84 decimal degrees).}

\item{backend}{One of \code{"wcs"} (default; OGC Web Coverage Service, needs
\pkg{terra}), \code{"webdav"} (crop the global VRT over \verb{/vsicurl} with
\pkg{gdalUtilities} then read locally; falls back to \code{"wcs"} if that
package is absent), \code{"rest"} (legacy beta properties/query endpoint, needs
\pkg{jsonlite}), or \code{"local"} (read from local raster files).}

\item{properties}{Character vector of SoilGrids property names to retrieve.}

\item{depth}{One or more of the six standard depth intervals.}

\item{quantile}{One or more prediction statistics: \code{"mean"}, \code{"Q0.05"},
\code{"Q0.5"} or \code{"Q0.95"}. Retrieving Q0.05/Q0.5/Q0.95 makes model uncertainty
available to \code{\link[=soil_profile_features]{soil_profile_features()}}.}

\item{local_paths}{Named list/vector mapping each property to a local raster
file, required when \code{backend = "local"}.}

\item{buffer_m}{Half-width (metres) of the window requested around each site
on the 250 m Homolosine grid (WCS \code{GetCoverage} subset, or WebDAV
\code{gdal_translate} crop). Default 1000.}

\item{resolution_m}{Output cell size (metres) for the WebDAV crop. Default
250 (SoilGrids native resolution).}

\item{apply_conversion}{Logical; divide raw values by the SoilGrids
conversion factor to obtain conventional units. Default \code{TRUE}.}

\item{spatial_summary}{\code{"point"} extracts the cell containing the coordinate;
\code{"mean_sd"} returns the neighbourhood mean and SD from the requested
buffer, representing local spatial heterogeneity.}
}
\value{
A data frame with one row per site (row names = \code{environment}) and one
column per property for scalar point requests. Profile/uncertainty requests
use \code{property__depth__quantile__summary} names. Returns \code{NULL} if nothing
usable was retrieved (with a
warning naming the cause). The \code{"rest"}, \code{"wcs"}, \code{"webdav"} and \code{"local"}
raster backends require the \pkg{terra} package (except \code{"rest"}, which
requires \pkg{jsonlite}).
}
\description{
Production-grade access to ISRIC SoilGrids predictions at a set of site
coordinates. The public REST "properties/query" endpoint is a beta service
that is rate-limited and intermittently returns only null values, so it is
fragile for a pipeline. ISRIC instead recommends the OGC \strong{Web Coverage
Service (WCS)} for extracting spatial subsets, and \strong{WebDAV} for direct
access to the global VRT layers. \code{fetch_soilgrids()} supports both of those
robust backends (plus the legacy REST endpoint and purely local files) behind
a single interface.
}
\details{
SoilGrids stores every property as scaled integers to save space; the raw
values must be divided by a property-specific conversion factor to obtain
conventional units (e.g. clay is stored in g/kg and divided by 10 to reach
g/100g = \\%). \code{fetch_soilgrids()} applies these factors automatically unless
\code{apply_conversion = FALSE}. The conversion factors are:
\tabular{lll}{
Property \tab Mapped units \tab Factor \cr
bdod \tab cg/cm3 \tab 100 \cr
nitrogen \tab cg/kg \tab 100 \cr
cec, cfvo, clay, sand, silt, soc, ocd, ocs, phh2o \tab various \tab 10 \cr
wv0010, wv0033, wv1500 \tab 10^-3 \tab 10
}

Note that SoilGrids is a single \emph{modelled snapshot}, not a time series. Some
soil properties change slowly (texture, bulk density) while others change with
time and management (organic carbon, nitrogen, pH, nutrients, salinity), and
soil moisture changes daily. For time-resolved soil use: the daily NASA POWER
\verb{GWET*} soil-wetness variables (via \code{\link[=fetch_weather_series]{fetch_weather_series()}}), satellite soil
moisture (SMAP, ESA-CCI, Copernicus), point profiles (ISRIC WoSIS), or -- best
-- your own dated field measurements, which you can pass directly as the
\code{soil} argument of \code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}} (including one set per year).

Predictions exist for six standard depth intervals
(\verb{0-5cm}, \verb{5-15cm}, \verb{15-30cm}, \verb{30-60cm}, \verb{60-100cm}, \verb{100-200cm}) and four
prediction statistics (\code{mean}, \code{Q0.05}, \code{Q0.5}, \code{Q0.95}). SoilGrids native
data are on the Interrupted Goode Homolosine grid (EPSG:152160) at 250 m
resolution; the WCS and WebDAV backends reproject site coordinates onto that
grid with \code{\link[terra:project]{terra::project()}} and read only the cells needed via GDAL's
\verb{/vsicurl} virtual file system, so no full-grid download occurs.
}
\examples{
\dontrun{
sites <- data.frame(environment = c("E1", "E2"),
                    latitude = c(9.06, -1.29), longitude = c(7.49, 36.82))
# Robust OGC Web Coverage Service backend (default), topsoil mean values:
soil <- fetch_soilgrids(sites, backend = "wcs",
                        properties = c("clay", "sand", "phh2o", "soc"),
                        depth = "0-5cm", quantile = "mean")
# Direct VRT access over WebDAV instead:
soil <- fetch_soilgrids(sites, backend = "webdav", properties = "clay")
}
}
\references{
ISRIC SoilGrids FAQ and WCS/WebDAV access guides, \url{https://www.isric.org/},
\url{https://maps.isric.org/}, \url{https://files.isric.org/soilgrids/latest/data/}.
}
\seealso{
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}, \code{\link[=build_environment_relationship]{build_environment_relationship()}}.
}
