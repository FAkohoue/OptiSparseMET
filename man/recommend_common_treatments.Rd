% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend_common_treatments.R
\name{recommend_common_treatments}
\alias{recommend_common_treatments}
\title{Recommend the number of common treatments from environmental kinship}
\usage{
recommend_common_treatments(
  env_relationship,
  n_test_entries_per_environment,
  n_treatments,
  n_environments = NULL,
  sparse_allocation = c("random", "disjoint"),
  target_se = 0.15,
  n_points = 8L,
  is_covariance = TRUE,
  evaluate = FALSE,
  G = NULL,
  Sigma_E = NULL,
  ...
)
}
\arguments{
\item{env_relationship}{Environment covariance \code{Sigma_E} (default) or an
environment correlation/relationship matrix (\code{is_covariance = FALSE}).}

\item{n_test_entries_per_environment, n_treatments}{Passed to
\code{\link[=suggest_common_range]{suggest_common_range()}}; the per-environment capacity and total treatment
pool.}

\item{n_environments}{Number of environments; defaults to \code{nrow(env_relationship)}.}

\item{sparse_allocation}{\code{"random"} or \code{"disjoint"} (see
\code{\link[=suggest_common_range]{suggest_common_range()}}).}

\item{target_se, n_points}{Passed to \code{\link[=suggest_common_range]{suggest_common_range()}}.}

\item{is_covariance}{If \code{TRUE} (default) \code{env_relationship} is a covariance and
is converted to a correlation before averaging; set \code{FALSE} if it is already
a correlation/similarity in [-1, 1].}

\item{evaluate}{If \code{TRUE} and \code{G}/\code{Sigma_E} are given, also run
\code{\link[=tune_common_treatments]{tune_common_treatments()}} over the suggested range and return the simulated
optimum.}

\item{G, Sigma_E}{Genomic relationship and environment covariance for the
optional simulation (\code{evaluate = TRUE}). Treatments/environments are taken
from their dimnames.}

\item{...}{Further arguments passed to \code{\link[=tune_common_treatments]{tune_common_treatments()}}.}
}
\value{
A list with \code{rho} (typical environment correlation), \code{common_range}
(suggested counts), \code{suggest} (the full \code{\link[=suggest_common_range]{suggest_common_range()}} object),
and, when evaluated, \code{tune} (the \code{\link[=tune_common_treatments]{tune_common_treatments()}} object) and
\code{recommended} (its \code{best_estimated} optimum).
}
\description{
Common (check-like) treatments tested in every environment provide the
cross-environment connectivity that lets a sparse MET be analysed jointly.
How many are needed depends on how correlated the environments are: strongly
correlated sites borrow information freely and need few, weakly correlated
sites need many. \code{recommend_common_treatments()} reads that correlation
straight from the breeder's \strong{environmental kinship} (the environment
covariance \code{Sigma_E}, or a relationship matrix from
\code{\link[=build_environment_relationship]{build_environment_relationship()}}), so the breeder does not have to guess a
connectivity level. It reports the typical correlation \code{rho}, the analytic
\code{\link[=suggest_common_range]{suggest_common_range()}} band, and -- if \code{G} and \code{Sigma_E} are supplied --
the simulated optimum from \code{\link[=tune_common_treatments]{tune_common_treatments()}}.
}
\examples{
SigE <- matrix(c(1, .3, .1, .3, 1, .2, .1, .2, 1), 3)
recommend_common_treatments(SigE, n_test_entries_per_environment = 60,
                            n_treatments = 300, n_environments = 3)$rho
}
\seealso{
\code{\link[=suggest_common_range]{suggest_common_range()}}, \code{\link[=tune_common_treatments]{tune_common_treatments()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}.
}
