% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_historical_met.R
\name{fit_historical_met}
\alias{fit_historical_met}
\title{Fit a genetic environment covariance from historical MET responses}
\usage{
fit_historical_met(
  data,
  genotype_col = "Genotype",
  environment_col = "Environment",
  response_col = "Value",
  environments = NULL,
  model = c("fa", "unstructured", "diagonal"),
  rank = 2L,
  residual_variance = NULL,
  control = list(iter.max = 500L, eval.max = 1000L, rel.tol = 1e-08)
)
}
\arguments{
\item{data}{Long-format historical MET data.}

\item{genotype_col, environment_col, response_col}{Column names.}

\item{environments}{Optional environment order; defaults to observed order.}

\item{model}{Covariance model: \code{"fa"}, \code{"unstructured"}, or \code{"diagonal"}.}

\item{rank}{Factor-analytic rank, used for \code{model = "fa"}.}

\item{residual_variance}{Known residual variance: a scalar or one value per
environment. If \code{NULL}, it is estimated from within-cell replication;
this requires at least one replicated genotype-environment cell.}

\item{control}{Named list passed to \code{\link[stats:nlminb]{stats::nlminb()}}.}
}
\value{
A \code{historical_met_fit} object containing \code{Sigma_E}, its correlation
form, residual variances, FA loadings/specific variances when relevant,
REML log likelihood, information criteria, convergence diagnostics, and
the aggregated data used by the fit.
}
\description{
Fits an environment covariance directly from replicated or adjusted
genotype-by-environment responses by restricted maximum likelihood (REML).
Missing genotype-environment cells are handled in the likelihood, rather
than by pairwise correlations. Available covariance models are diagonal,
factor analytic, and unstructured.
}
