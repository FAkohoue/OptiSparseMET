% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment_data_qc.R
\name{qc_environmental_data}
\alias{qc_environmental_data}
\title{Quality-control environmental source data}
\usage{
qc_environmental_data(
  data,
  environments = NULL,
  environment_col = "environment",
  date_col = NULL,
  expected_dates = NULL,
  ranges = NULL,
  min_coverage = 0.8,
  duplicate_action = c("error", "aggregate"),
  missing_action = c("impute", "warn", "error"),
  impute = c("linear", "median", "none"),
  add_missing_indicators = TRUE,
  source = "environmental"
)
}
\arguments{
\item{data}{A data frame containing environmental observations.}

\item{environments}{Optional complete character vector of required
environments.}

\item{environment_col}{Name of the environment column.}

\item{date_col}{Optional date column. When supplied, duplicate and coverage
checks use environment-date keys and the output is ordered by them.}

\item{expected_dates}{Optional vector of expected dates, or a data frame with
\code{environment} and the column named by \code{date_col}. If omitted, coverage is
relative to the observed union of dates.}

\item{ranges}{Optional named list. Each element is a length-two valid range
for a numeric variable; values outside it are set to \code{NA}.}

\item{min_coverage}{Minimum acceptable observed fraction for each
environment-variable combination.}

\item{duplicate_action}{\code{"error"} (default) or \code{"aggregate"}. Aggregation
averages numeric values and takes the first non-missing non-numeric value.}

\item{missing_action}{\code{"impute"}, \code{"warn"}, or \code{"error"}.}

\item{impute}{Imputation used when \code{missing_action = "impute"}:
\code{"linear"} interpolates within environment and then uses the cross-
environment median, \code{"median"} uses the variable median, and \code{"none"}
leaves values missing.}

\item{add_missing_indicators}{Add \verb{<variable>__was_missing} columns.}

\item{source}{Short source label stored in the audit and provenance.}
}
\value{
A list with \code{data}, \code{audit}, \code{provenance}, and \code{imputation}.
}
\description{
Applies explicit, auditable quality control before weather, soil, management,
or other environmental data are converted to kernels. The function validates
environment coverage and duplicate keys, converts out-of-range observations
to missing, applies a declared imputation rule, and records every affected
environment-variable combination. It never silently replaces missing values.
}
