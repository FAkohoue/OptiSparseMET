% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment_kernels.R
\name{calibrate_environment_covariance}
\alias{calibrate_environment_covariance}
\title{Calibrate environmental covariance from historical MET information}
\usage{
calibrate_environment_covariance(
  kernels,
  target = NULL,
  historical = NULL,
  genotype_col = "genotype",
  environment_col = "environment",
  value_col = "value",
  year_col = NULL,
  prior_weights = NULL,
  identity_weight = 0.1,
  ridge = 0,
  interaction_kernels = NULL,
  eligible_interactions = NULL,
  interaction_policy = c("evidence", "exclude", "include"),
  interaction_alpha = 0.05,
  n_boot = 200L,
  seed = NULL
)
}
\arguments{
\item{kernels}{Named list of aligned environmental kernels.}

\item{target}{Optional empirical environment genetic-correlation matrix or
list of such matrices.}

\item{historical}{Optional long data frame containing genotype, environment,
and genetic-value columns.}

\item{genotype_col, environment_col, value_col}{Column names in \code{historical}.}

\item{year_col}{Optional year column. When supplied, a target is built per
year and leave-year-out validation is added.}

\item{prior_weights}{Optional named modality weights used only as a weak
ridge target when a historical target is supplied. They are ignored when
\code{target} and \code{historical} are both \code{NULL}.}

\item{identity_weight}{Prior identity/shrinkage weight in \verb{[0,1)}, used only
to construct the ridge target when historical MET evidence is supplied.}

\item{ridge}{Non-negative penalty toward \code{prior_weights}. Set to zero for
wholly data-calibrated weights.}

\item{interaction_kernels}{Optional character vector naming interaction
kernels. \code{NULL} identifies names containing \code{"_x_"}; \code{character(0)}
declares that none are interactions.}

\item{eligible_interactions}{Optional character vector restricting which
interaction kernels may enter the central covariance. Kernels excluded by
this screen remain in \code{candidates} as structural uncertainty scenarios.
Supply the \code{supported} component from
\code{\link[=assess_variable_interactions]{assess_variable_interactions()}} after component-wise blocked validation.
\code{NULL} makes every identified interaction eligible for the aggregate
evidence gate.}

\item{interaction_policy}{\code{"evidence"} (default) activates interaction
components only when a paired blocked-validation sign test favours the
main-plus-interaction model over the main-only model. \code{"exclude"} fixes all
interaction weights at zero in the central covariance. \code{"include"} is an
explicit user override for the central covariance.
Leave-year/target-out folds are preferred when available; otherwise
leave-environment-out folds are used.
This policy never removes interaction kernels from the sensitivity set:
absence of response evidence is not evidence that physical interactions
are absent. Without a historical target, interaction kernels remain
separate, unweighted uncertainty scenarios and never enter the central
covariance.}

\item{interaction_alpha}{Significance level for the one-sided paired sign
test used by \code{interaction_policy = "evidence"}.}

\item{n_boot}{Number of pair-bootstrap calibration draws.}

\item{seed}{Optional seed for bootstrap reproducibility.}
}
\value{
A list with \code{Sigma_E}, weights (or \code{NULL} without historical MET
evidence), status, interaction status/evidence, fitted target, diagnostics,
blocked validation, bootstrap draws, and sensitivity candidates.
}
\description{
Learns non-negative, sum-to-one weights for modality kernels against an
empirical genetic-correlation target. Calibration can use a target matrix, a
list of year-specific target matrices, or historical genotype-by-environment
values. Leave-environment-out predictions and bootstrap weight uncertainty
are returned. Without a historical target, weights are not defined:
\code{Sigma_E} is the identity matrix and all main-effect and interaction kernels
are returned as separate, unweighted sensitivity scenarios. Lack of
response evidence is not treated as absence of a physical interaction.
Environmental similarity is never converted into an assumed genetic
covariance by arbitrary prior weights.
}
