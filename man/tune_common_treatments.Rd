% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_common_treatments.R
\name{tune_common_treatments}
\alias{tune_common_treatments}
\title{Tune the number of common treatments across environments}
\usage{
tune_common_treatments(
  treatments,
  environments,
  n_test_entries_per_environment,
  G,
  Sigma_E,
  common_values,
  sparse_allocation = c("random", "disjoint"),
  sigma_g2 = 1,
  sigma_e2 = 1,
  n_sim = 40L,
  select_fraction = 0.1,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{treatments}{Character vector of candidate line IDs.}

\item{environments}{Character vector of environment names.}

\item{n_test_entries_per_environment}{Plots (entries) per environment, \code{k}.}

\item{G, Sigma_E}{Genomic relationship and environment covariance.}

\item{common_values}{Integer vector of common-treatment counts to evaluate
(each \verb{< n_test_entries_per_environment}).}

\item{sparse_allocation}{How the non-common lines are spread: \code{"random"}
(random per environment) or \code{"disjoint"} (non-overlapping blocks -- the
worst case for connectivity, where common treatments matter most).}

\item{sigma_g2, sigma_e2}{Variance components.}

\item{n_sim}{Simulation replicates per count.}

\item{select_fraction}{Top fraction for the gain statistic.}

\item{seed, max_dim}{Reproducibility and size controls.}
}
\value{
A list with \code{table} (one row per count: \code{n_common}, \code{accuracy_known},
\code{accuracy_estimated}, \code{gain_estimated}, \code{min_shared}, \code{shared_var},
\code{coverage}) and \code{optima} (the counts maximising \code{accuracy_known} and
\code{accuracy_estimated}, and the connectivity \code{knee}).
}
\description{
Common treatments (lines tested in every environment) trade off against unique
coverage: more of them stabilise the estimated genotype-by-environment
covariance and connect an otherwise-disconnected design, but consume plots
that could test unique lines. \code{tune_common_treatments()} sweeps the number of
common treatments and reports four curves so the optimum can be read off
directly rather than guessed.
}
\details{
The crucial point is that a design criterion which assumes the environment
covariance \eqn{\Sigma_E} is \emph{known} almost always prefers zero common
treatments -- their value is that they let you \emph{estimate} \eqn{\Sigma_E} and
connect the design. The function therefore reports, for each candidate count:
\describe{
\item{\code{accuracy_known}}{Simulated accuracy with \eqn{\Sigma_E} assumed known
(via \code{\link[=simulate_met]{simulate_met()}}); typically decreasing in the count -- the coverage
cost.}
\item{\code{accuracy_estimated}}{Simulated accuracy with \eqn{\Sigma_E}
re-estimated from the data each replicate; this is the objective that can
show an interior optimum, and it rises when correlations are weak (weak
correlations are noisier to estimate, so more common treatments help).}
\item{\code{min_shared}, \code{shared_var}}{Connectivity: the minimum and variance of
the number of genotypes shared between environment pairs (the G x E
estimation objective; increasing, with a knee).}
\item{\code{coverage}}{Number of unique lines actually tested (decreasing).}
}
The gap between \code{accuracy_known} and \code{accuracy_estimated} is the diagnostic:
it is widest when environments are weakly correlated, and a
\code{best_estimated} that runs to the top of \code{common_values} is a signal to
partition the environments into mega-environments rather than add more common
treatments.
}
\seealso{
\code{\link[=simulate_met]{simulate_met()}}, \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, \code{\link[=select_environments]{select_environments()}}.
}
