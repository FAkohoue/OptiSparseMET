% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/design_objective.R
\name{design_objective}
\alias{design_objective}
\title{Evaluate a sparse MET design under a combined objective}
\usage{
design_objective(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  reps = NULL,
  env_efficiency = NULL,
  prop = 0.1,
  sigma_g = 1,
  trait_weights = NULL,
  trait_gencov = NULL,
  R_T = NULL,
  multitrait = c("exact", "approx"),
  cost_per_plot = 1,
  weights = list(gain = 1, reliability = 0, cost = 0),
  ref = NULL,
  budget = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocation_matrix}{Genotype-by-environment 0/1 (or replication-count)
matrix with dimnames.}

\item{G, Sigma_E, sigma_g2, sigma_e2, reps, env_efficiency, max_dim}{Passed to
\code{\link[=met_information]{met_information()}} (across-TPE target).}

\item{prop, sigma_g, trait_weights, trait_gencov}{Passed to
\code{\link[=expected_genetic_gain]{expected_genetic_gain()}}. Supplying \code{trait_weights} (e.g. from
\code{\link[=desired_gain_weights]{desired_gain_weights()}}) with \code{trait_gencov} (the trait genetic covariance
\eqn{\Sigma_T}) turns on the multi-trait index objective.}

\item{R_T}{Trait residual covariance for the exact multi-trait path (defaults
to \code{sigma_e2 * I}).}

\item{multitrait}{\code{"exact"} (default) uses the full trait-covariance Kronecker
MME via \code{\link[=met_information_mt]{met_information_mt()}} for the index reliability; \code{"approx"} uses the
single-trait reliability times the index SD.}

\item{cost_per_plot}{Cost of one physical plot.
Default 1.}

\item{weights}{Named list of non-negative weights \code{gain}, \code{reliability},
\code{cost}. Default \code{list(gain = 1, reliability = 0, cost = 0)}.}

\item{ref}{Named list of reference values (\code{gain}, \code{reliability}, \code{cost}) used
to normalise the components; \code{NULL} (default) uses 1 for each.}

\item{budget}{Optional maximum number of plots; designs above it are
infeasible.}
}
\value{
A list with \code{score} and the raw components \code{reliability}, \code{mean_PEV},
\code{gain}, \code{plots}, \code{cost}, and \code{feasible}.
}
\description{
A single, deterministic scoring function for a candidate allocation that
combines the three things a breeder trades off: statistical quality
(reliability / CDmean, or mean PEV), expected genetic gain (single-trait or a
multi-trait economic index), and resource cost (plots). It is built on the
coupled two-level \code{\link[=met_information]{met_information()}} matrix, so no Monte-Carlo simulation is
needed inside an optimisation loop -- the objective is exact given the
variance parameters. This is the objective maximised by \code{\link[=optimize_design]{optimize_design()}}.
}
\details{
The scalar score (higher is better) is a weighted combination
\deqn{s = w_{gain}\,\tilde{\Delta G} + w_{rel}\,\tilde{r^2} - w_{cost}\,\tilde{c},}
where each component is divided by a reference value (\code{ref}) so the terms are
on a comparable scale; \code{\link[=optimize_design]{optimize_design()}} supplies \code{ref} from the starting
design automatically. A design whose plot count exceeds \code{budget} is infeasible
(\code{score = -Inf}). For a single trait, gain and reliability are monotonically
related, so weighting both is redundant; the distinct trade-off is
benefit-vs-cost (and the multi-trait index vs cost).
}
\examples{
set.seed(1)
G <- crossprod(matrix(rnorm(30 * 60), 60, 30)) / 60 + diag(30) * 0.3
dimnames(G) <- list(paste0("L", 1:30), paste0("L", 1:30))
SigE <- diag(3) * 0.5 + 0.5
dimnames(SigE) <- list(paste0("E", 1:3), paste0("E", 1:3))
M <- matrix(0L, 30, 3, dimnames = list(rownames(G), colnames(SigE)))
for (e in 1:3) M[sample(30, 15), e] <- 1L
design_objective(M, G, SigE,
                 weights = list(gain = 1, reliability = 0, cost = 0.2))$score
}
\seealso{
\code{\link[=optimize_design]{optimize_design()}}, \code{\link[=expected_genetic_gain]{expected_genetic_gain()}}, \code{\link[=met_information]{met_information()}}.
}
