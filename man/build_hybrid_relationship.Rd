% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_hybrid_relationship.R
\name{build_hybrid_relationship}
\alias{build_hybrid_relationship}
\title{Genomic relationship among hybrids/testcrosses from parental G}
\usage{
build_hybrid_relationship(G_parents, crosses, hybrid_ids = NULL)
}
\arguments{
\item{G_parents}{Parental genomic relationship matrix (row/column names are
parent ids), e.g. from \code{\link[=build_relationship_matrix]{build_relationship_matrix()}}.}

\item{crosses}{A data frame or matrix with two columns naming the two parents
of each hybrid (both must be rows of \code{G_parents}).}

\item{hybrid_ids}{Optional names for the hybrids (length \code{nrow(crosses)});
defaults to \code{crosses} row names, else \code{parent1_x_parent2}.}
}
\value{
The hybrid-by-hybrid additive genomic relationship matrix (dimnames =
\code{hybrid_ids}). Pass it through \code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}} if you need a
guaranteed-invertible version.
}
\description{
Hybrids (single crosses, testcrosses) are usually not genotyped directly --
only their parents are. And tools like \pkg{AGHmatrix} require integer 0/1/2
marker dosages, which a hybrid's parent-mean genotype (0, 0.5, 1, ...) is not.
\code{build_hybrid_relationship()} sidesteps both problems: build the \strong{parental}
genomic relationship matrix from integer marker data (e.g.
\code{build_relationship_matrix(method = "AGHmatrix")}), then derive the additive
genomic relationship among the hybrids from the cross design.
}
\details{
For a single cross \eqn{H = a \times b}, the additive genomic value is the sum
of the parental gamete contributions, so the additive relationship between two
hybrids \eqn{a\times b} and \eqn{c\times d} is
\deqn{G_H[ab, cd] = \tfrac{1}{4}\,(G[a,c] + G[a,d] + G[b,c] + G[b,d]).}
This equals VanRaden's G computed on the parent-mean hybrid dosages \strong{when
both are centred on the parental (base) allele frequencies} -- the
appropriate centring for hybrids (Technow et al. 2014). It is therefore not an
approximation: it derives the hybrid GRM on the parental frequency scale while
avoiding non-integer dosages in the estimator, rather than recomputing
allele frequencies from the specific set of hybrids. Applies to testcrosses
(one common tester/female) and full factorials alike.
}
\examples{
set.seed(1)
Mpar <- matrix(sample(0:2, 6 * 300, TRUE), 6, 300,
               dimnames = list(c("A","B","C","D","T1","T2"), NULL))
Gp <- build_relationship_matrix(markers = Mpar, type = "genomic")
crosses <- data.frame(p1 = c("A","B","C","D"), p2 = c("T1","T1","T2","T2"))
Ghyb <- build_hybrid_relationship(Gp, crosses)
dim(Ghyb)
}
\references{
Technow, F. et al. (2014). Genome properties and prospects of genomic
prediction of hybrid performance. \emph{Genetics}, 197, 1343-1355.
}
\seealso{
\code{\link[=build_relationship_matrix]{build_relationship_matrix()}}, \code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}}.
}
