% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sparsity_grid.R
\name{sparsity_grid}
\alias{sparsity_grid}
\title{Explore the sparse-testing trade-off (individuals x per-environment x environments)}
\usage{
sparsity_grid(
  G,
  Sigma_E,
  ia_values,
  ipf_values,
  noe_values,
  plot_budget = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  n_sim = 30L,
  select_fraction = 0.1,
  bv_target = "across_tpe",
  target_envs = NULL,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{G}{Genomic relationship matrix (row/column names) with at least
\code{max(ia_values)} genotypes.}

\item{Sigma_E}{Environment covariance with at least \code{max(noe_values)}
environments (a broad-TPE structure makes the sweep informative).}

\item{ia_values}{Numbers of individuals \emph{available} to sample from the TPG.}

\item{ipf_values}{Numbers of individuals \emph{tested per environment}.}

\item{noe_values}{Numbers of environments (each >= 2).}

\item{plot_budget}{Optional cap on total plots (\code{ipf * noe}); cells exceeding
it are skipped.}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{n_sim}{Number of independent simulation replicates.}

\item{select_fraction}{Top fraction selected (on predicted BV) for the
genetic-gain statistic.}

\item{bv_target}{Which breeding value the design is scored against (Colmant
et al. 2026): \code{"across_tpe"} (default; average of all environments),
\code{"environment_specific"} (a single environment), or \code{"mega_environment"}
(average of a subset). This can change which design wins, so broad- and
specific-adaptation strategies can be compared honestly.}

\item{target_envs}{Environment name(s) or index/indices defining the target
for \code{"environment_specific"} (exactly one) or \code{"mega_environment"} (a
subset). Ignored for \code{"across_tpe"}.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A data frame (sorted by \code{accuracy_mean}, best first) with one row per
feasible grid cell: \code{ia}, \code{ipf}, \code{noe}, \code{total_plots}, \code{sparsity_pct}
(relative to a fully balanced design), \code{accuracy_mean}, \code{gain_mean},
\code{common_selected_mean}, \code{mean_PEV}, and \code{CDmean}.
}
\description{
The single largest driver of accuracy in Colmant et al. (2026) was \emph{how} the
target populations are sampled: how many individuals are available, how many
are tested per environment, and how many environments are used, under a fixed
plot budget. \code{sparsity_grid()} sweeps that three-way grid, builds a random
sparse allocation for each cell, and scores realised accuracy and genetic gain
by simulation -- turning "how sparse should we go?" into a comparison you can
read off a table.
}
\references{
Colmant, A., Pita, F., & Covarrubias-Pazaran, G. (2026). Some objective
functions and ideas to optimize experimental designs in artificial selection
programs. \emph{Crop Science}, 66, e70337.
}
\seealso{
\code{\link[=simulate_met]{simulate_met()}}, \code{\link[=recommend_replication]{recommend_replication()}}.
}
