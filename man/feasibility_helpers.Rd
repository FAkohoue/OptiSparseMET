% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feasibility_helpers.R
\name{feasibility_helpers}
\alias{feasibility_helpers}
\title{Feasibility helpers for sparse MET allocation}
\description{
Utility functions for assessing whether a proposed sparse MET configuration
has sufficient per-environment capacity to assign every non-common treatment
at least once, and for deriving safe values for
\code{n_test_entries_per_environment} prior to calling \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.
}
\details{
These helpers address a specific failure mode: passing
\code{n_test_entries_per_environment} that is too small to cover all treatments,
which causes \code{\link[=allocate_sparse_met]{allocate_sparse_met()}} to either error, produce incomplete
allocation, or require \code{allow_approximate = TRUE} in ways that leave some
treatments unassigned. Calling these helpers before allocation lets the user
verify feasibility and choose a safe capacity without trial and error.

The three exported helpers serve distinct purposes.
\code{\link[=min_k_for_full_coverage]{min_k_for_full_coverage()}} computes the arithmetic minimum.
\code{\link[=suggest_safe_k]{suggest_safe_k()}} adds a user-defined buffer on top of that minimum and
returns a single ready-to-use integer. \code{\link[=warn_if_k_too_small]{warn_if_k_too_small()}} checks an
existing configuration and emits a warning with a corrective suggestion if
the capacity is insufficient. The internal function
\code{.check_full_coverage_feasibility()} is called by \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}
to enforce the same condition with a hard stop.
}
