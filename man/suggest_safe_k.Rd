% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feasibility_helpers.R
\name{suggest_safe_k}
\alias{suggest_safe_k}
\title{Suggest a safe uniform per-environment capacity for sparse MET allocation}
\usage{
suggest_safe_k(treatments, environments, common_treatments = NULL, buffer = 3L)
}
\arguments{
\item{treatments}{Character vector of treatment IDs. Duplicates are silently
removed.}

\item{environments}{Character vector of environment names. Duplicates are
silently removed.}

\item{common_treatments}{Optional character vector of common treatment IDs.
Values not present in \code{treatments} are silently dropped. If \code{NULL}, no
common treatments are assumed.}

\item{buffer}{Non-negative integer, default \code{3}. Extra slots added on top of
the strict minimum capacity. A small buffer reduces the risk of
infeasibility from rounding and makes the configuration more robust to
minor changes in trial dimensions.}
}
\value{
Integer scalar. A safe uniform value for
\code{n_test_entries_per_environment}.
}
\description{
The suggested value is:

\deqn{k_{\text{safe}} = \left\lceil J^* / I \right\rceil + C + b}

where \eqn{J^* = J - C} is the number of non-common treatments, \eqn{I} is
the number of environments, \eqn{C} is the number of common treatments, and
\eqn{b} is \code{buffer}. Setting \code{buffer = 0} returns the strict minimum from
\code{\link[=min_k_for_full_coverage]{min_k_for_full_coverage()}}. A buffer of 3 to 5 is typically sufficient to
absorb rounding and ensure \code{\link[=allocate_sparse_met]{allocate_sparse_met()}} runs without needing
\code{allow_approximate = TRUE}.
}
\details{
Returns a single integer suitable for passing to \code{n_test_entries_per_environment}
in \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}. The value is the minimum capacity that guarantees
full treatment coverage plus a user-defined buffer, making it appropriate for
examples, tests, and demonstration workflows where a comfortably feasible
setting is preferred over the exact minimum.
}
\examples{
trt <- paste0("L", sprintf("\%03d", 1:80))
env <- c("Env1", "Env2", "Env3")

## No common treatments, default buffer of 3:
## ceil(80 / 3) + 0 + 3 = 27 + 3 = 30
suggest_safe_k(
  treatments        = trt,
  environments      = env,
  common_treatments = NULL,
  buffer            = 3
)

## With 5 common treatments, buffer of 2:
## ceil(75 / 3) + 5 + 2 = 25 + 5 + 2 = 32
suggest_safe_k(
  treatments        = trt,
  environments      = env,
  common_treatments = trt[1:5],
  buffer            = 2
)

}
\seealso{
\code{\link[=min_k_for_full_coverage]{min_k_for_full_coverage()}} for the strict minimum without a buffer.
\code{\link[=warn_if_k_too_small]{warn_if_k_too_small()}} to check an existing capacity non-fatally.
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} for the allocation function this feeds into.
}
