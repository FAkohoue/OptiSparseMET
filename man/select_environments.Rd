% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select_environments.R
\name{select_environments}
\alias{select_environments}
\title{Select a representative subset of environments from a TPE (decision 1)}
\usage{
select_environments(
  D,
  n,
  method = c("representative", "optcontrib", "kmeans", "hclust", "random"),
  seed = NULL
)
}
\arguments{
\item{D}{Square environment relationship/similarity matrix (higher = more
similar), with row/column names.}

\item{n}{Number of environments to select.}

\item{method}{One of \code{"representative"}, \code{"optcontrib"}, \code{"kmeans"},
\code{"hclust"}, \code{"random"}.}

\item{seed}{Optional RNG seed (used by \code{"kmeans"} and \code{"random"}).}
}
\value{
A list with \code{selected} (environment names), \code{method}, and \code{score}
(coverage for \code{"representative"}, total within-set similarity for
\code{"optcontrib"}; \code{NA} otherwise).
}
\description{
\code{select_environments()} chooses \code{n} environments to represent a target
population of environments (TPE), given an environment relationship matrix
\code{D} (e.g. genetic correlations across environments, or a covariance derived
from enviromic data). It is a corrected, modified version of the
environment-selection idea in Colmant et al. (2026, decision 1).
}
\details{
Methods:
\describe{
\item{\code{"representative"} (default)}{Maximises coverage of the whole TPE:
the selected set \eqn{S} maximises \eqn{\sum_e \max_{s \in S} D_{es}}
(a k-medoid / facility-location criterion). This targets
\emph{representativeness} directly.}
\item{\code{"optcontrib"}}{The modified optimal-contribution / spread criterion
\eqn{-q'Dq}: greedily minimises the total similarity among selected
environments, maximising diversity. Note (per the package critique) that
spread is not the same as representativeness -- a maximally diverse set can
be a set of mutually atypical outliers.}
\item{\code{"kmeans"}, \code{"hclust"}}{Clustering baselines (as in the paper):
partition environments into \code{n} groups and return the medoid of each.}
\item{\code{"random"}}{Uniform random subset (control).}
}
}
\seealso{
\code{\link[=select_individuals]{select_individuals()}}, \code{\link[=met_information]{met_information()}}.
}
