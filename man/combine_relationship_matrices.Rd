% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_hybrid_relationship.R
\name{combine_relationship_matrices}
\alias{combine_relationship_matrices}
\title{Combine relationship matrices (e.g. additive + dominance) for total merit}
\usage{
combine_relationship_matrices(matrices, weights = NULL)
}
\arguments{
\item{matrices}{A named list of relationship matrices over the same
individuals (e.g. \code{list(additive = G_A, dominance = G_D)}).}

\item{weights}{One finite non-negative weight per matrix (variance components
or proportions), normalised to sum to 1. Named weights are aligned to a
named \code{matrices} list. Defaults to equal weights.}
}
\value{
The combined relationship matrix (dimnames from the first matrix).
}
\description{
When more than one genetic effect matters -- classically \strong{additive} and
\strong{dominance} for hybrids -- the relevant relationship for predicting \emph{total}
genetic merit is a variance-weighted combination of the component matrices.
\code{combine_relationship_matrices()} aligns the matrices by name and returns
\eqn{\sum_k w_k K_k / \sum_k w_k}, where the weights are the variance
proportions of each component (e.g. \code{c(additive = sigma2_A, dominance = sigma2_D)}). The result is a single relationship matrix that the design/
evaluation functions can use so the design accounts for dominance, not just
additive effects.
}
\examples{
set.seed(1)
M <- matrix(sample(0:2, 20 * 300, TRUE), 20, 300); rownames(M) <- paste0("H", 1:20)
G_A <- build_relationship_matrix(markers = M, type = "genomic")
G_D <- build_relationship_matrix(markers = M, type = "genomic",
                                 relationship = "dominance")
G_tot <- combine_relationship_matrices(list(additive = G_A, dominance = G_D),
                                       weights = c(2, 1))   # sigma2_A : sigma2_D
}
\seealso{
\code{\link[=build_relationship_matrix]{build_relationship_matrix()}}, \code{\link[=build_hybrid_relationship]{build_hybrid_relationship()}},
\code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}}.
}
