% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/environment_kernels.R
\name{combine_environment_kernels}
\alias{combine_environment_kernels}
\title{Combine modality-specific environmental kernels}
\usage{
combine_environment_kernels(
  kernels,
  weights = NULL,
  identity_weight = 0,
  interactions = NULL,
  interaction_mode = c("anova", "product")
)
}
\arguments{
\item{kernels}{Named list of aligned square kernels.}

\item{weights}{Non-negative named weights, required when more than one kernel
is supplied. They are normalised to sum to one. Use
\code{\link[=consensus_environment_kernels]{consensus_environment_kernels()}} for descriptive integration without
empirically justified weights.}

\item{identity_weight}{Optional shrinkage weight on an identity kernel.}

\item{interactions}{Optional named list of products such as
\code{list(weather_management = c("weather", "management"))}.}

\item{interaction_mode}{Interaction construction passed to
\code{\link[=add_environment_kernel_interactions]{add_environment_kernel_interactions()}}.}
}
\value{
A normalised positive-semidefinite environment relationship matrix
carrying the realised weights as an attribute.
}
\description{
Forms one positive-semidefinite environmental relationship matrix from
aligned modality-specific kernels. When more than one kernel is supplied,
the function requires explicit, non-negative weights; it does not interpret
equal weights as a scientific default. Users without empirical weights
should retain the kernels as separate uncertainty scenarios or use
\code{\link[=consensus_environment_kernels]{consensus_environment_kernels()}} for descriptive integration.
}
