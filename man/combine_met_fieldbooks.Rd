% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine_met_fieldbooks.R
\name{combine_met_fieldbooks}
\alias{combine_met_fieldbooks}
\title{Combine environment-level field books into a single MET field book}
\usage{
combine_met_fieldbooks(
  field_books,
  local_designs = NULL,
  replication_modes = NULL,
  sparse_method = NULL,
  common_treatments = NULL
)
}
\arguments{
\item{field_books}{Named list of data frames, one per environment. List
names are used as the values of the \code{Environment} column in the output
and must therefore be unique and non-empty. Each element must be a data
frame; the function stops with an error if any element is not.}

\item{local_designs}{Optional named character vector or named list. Gives
the local design type used in each environment, typically
\code{"met_prep_famoptg"} or \code{"met_alpha_rc_stream"} for built-in engines.
Names must match the names of \code{field_books}. Environments not present in
\code{local_designs} receive \code{NA} in the \code{LocalDesign} column. If \code{NULL}, all
environments receive \code{NA}.}

\item{replication_modes}{Optional named character vector or named list.
Gives the replication mode used in each environment, corresponding to the
\code{replication_mode} argument passed to \code{\link[=assign_replication_by_seed]{assign_replication_by_seed()}} for
that environment. Names must match the names of \code{field_books}.
Environments not present in \code{replication_modes} receive \code{NA} in the
\code{ReplicationMode} column. If \code{NULL}, all environments receive \code{NA}.}

\item{sparse_method}{Optional character scalar. The across-environment
allocation strategy used in \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, e.g.
\code{"balanced_incomplete"} or \code{"random_balanced"}. Applied uniformly to all
rows of the combined field book. If \code{NULL}, the \code{SparseMethod} column is
filled with \code{NA}.}

\item{common_treatments}{Optional character vector of treatment IDs forced
into all environments. Used to populate the \code{IsCommonTreatment} logical
column by matching each row's \code{Treatment} value against this vector. If
\code{NULL}, \code{IsCommonTreatment} is \code{FALSE} for all rows.}
}
\value{
A data frame with one row per plot across all environments. Columns
appear in the following order: \code{Environment}, \code{LocalDesign},
\code{ReplicationMode}, \code{SparseMethod}, \code{IsCommonTreatment}, \code{Treatment},
\code{Family}, \code{Gcluster}, \code{Block}, \code{Plot}, \code{Row}, \code{Column}, followed by any
additional columns present in the input field books in the order they are
encountered. Row names are reset to \code{NULL}. Columns absent in a given
environment's field book are filled with \code{NA} for all rows belonging to
that environment.
}
\description{
Environment-level field books produced by \code{\link[=met_prep_famoptg]{met_prep_famoptg()}} or
\code{\link[=met_alpha_rc_stream]{met_alpha_rc_stream()}} may differ in their column sets depending on the
design options used in each environment. A direct \code{rbind()} over such a list
fails unless all data frames share identical columns.
\code{combine_met_fieldbooks()} resolves this by computing the union of all
column names across environments, padding missing columns with \code{NA} in each
data frame before stacking, and placing a standard set of columns at the
front of the result regardless of the order in which they appear in the
individual field books.

The following columns are guaranteed to be present in the output even when
absent in the input, filled with \code{NA} where the source field book did not
contain them: \code{Treatment}, \code{Family}, \code{Gcluster}, \code{Block}, \code{Plot}, \code{Row},
\code{Column}. All other columns from any input field book are preserved and
appear after the standard column set.

MET-level metadata is added to every row before stacking:
\itemize{
\item \code{Environment}: taken from the name of the list element.
\item \code{LocalDesign}: the design type used in that environment, from
\code{local_designs} if supplied, otherwise \code{NA}.
\item \code{ReplicationMode}: the replication mode used in that environment, from
\code{replication_modes} if supplied, otherwise \code{NA}.
\item \code{SparseMethod}: the across-environment allocation strategy, from
\code{sparse_method} if supplied, otherwise \code{NA}.
\item \code{IsCommonTreatment}: logical flag indicating whether the treatment in each
row appears in \code{common_treatments}.
}
}
\details{
\code{combine_met_fieldbooks()} stacks a named list of environment-level field
books into one combined multi-environment trial (MET) field book. Each
environment's field book is augmented with MET-level metadata columns before
stacking, and columns that are present in some environments but absent in
others are filled with \code{NA} rather than causing an error. The result is a
single flat data frame suitable for export as a field book or as input to
downstream mixed-model analysis pipelines.
}
\examples{
## Minimal example: two environments with the same columns.
fb_E1 <- data.frame(
  Treatment = c("L001", "L002", "CHK1"),
  Family    = c("F1", "F2", "CHECK"),
  Block     = c(1L, 1L, 1L),
  Plot      = 1:3,
  Row       = c(1L, 1L, 1L),
  Column    = 1:3,
  stringsAsFactors = FALSE
)

fb_E2 <- data.frame(
  Treatment = c("L003", "L004", "CHK1"),
  Family    = c("F3", "F4", "CHECK"),
  Block     = c(1L, 1L, 1L),
  Plot      = 1:3,
  Row       = c(1L, 1L, 1L),
  Column    = 1:3,
  stringsAsFactors = FALSE
)

met <- combine_met_fieldbooks(
  field_books        = list(E1 = fb_E1, E2 = fb_E2),
  local_designs      = c(E1 = "met_prep_famoptg", E2 = "met_prep_famoptg"),
  replication_modes  = c(E1 = "augmented", E2 = "augmented"),
  sparse_method      = "balanced_incomplete",
  common_treatments  = "CHK1"
)

nrow(met)                        # 6 -- three plots per environment
unique(met$Environment)          # "E1" "E2"
met$IsCommonTreatment            # TRUE only for CHK1 rows
head(met[, 1:8])

## Heterogeneous columns: E2 has an extra column absent in E1.
## combine_met_fieldbooks() fills the missing column with NA for E1 rows.
fb_E2$SpatialResidual <- rnorm(3)

met2 <- combine_met_fieldbooks(
  field_books = list(E1 = fb_E1, E2 = fb_E2)
)

"SpatialResidual" \%in\% names(met2)                         # TRUE
is.na(met2$SpatialResidual[met2$Environment == "E1"])      # all TRUE

}
\seealso{
\code{\link[=met_prep_famoptg]{met_prep_famoptg()}} and \code{\link[=met_alpha_rc_stream]{met_alpha_rc_stream()}} which produce the
environment-level field books that serve as input to this function.
\code{\link[=plan_sparse_met_design]{plan_sparse_met_design()}} which calls this function internally as the
final step of the two-stage MET pipeline.
}
