% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_environment_relationship.R
\name{build_environment_relationship}
\alias{build_environment_relationship}
\title{Build one environment relationship matrix for exploratory selection}
\usage{
build_environment_relationship(
  x,
  source = c("genetic_correlation", "enviromic"),
  kernel = c("correlation", "gaussian"),
  bandwidth = NULL,
  variables = NULL,
  weights = NULL,
  geo = FALSE,
  missing = c("error", "median")
)
}
\arguments{
\item{x}{Input data.
\describe{
\item{\code{source = "genetic_correlation"}}{A genotype-by-environment matrix
of genetic values or best linear unbiased estimates (BLUEs; genotypes
in rows, environments in
columns). \code{D} is the correlation between environment columns.}
\item{\code{source = "enviromic"}}{An environment-by-covariate matrix
(environments in rows, standardised or raw covariates in columns).}
}}

\item{source}{Either \code{"genetic_correlation"} or \code{"enviromic"}.}

\item{kernel}{For \code{source = "enviromic"}: \code{"correlation"} (correlation of
environments across standardised covariates) or \code{"gaussian"} (Gaussian
kernel on covariate distance).

\strong{Interactions and the choice of kernel.} The Gaussian radial basis
function kernel is non-linear, but that does not identify a biologically
interpretable weather-by-soil or weather-by-management contribution.
Explicit scientific hypotheses should be represented by separate
cross-modality kernels from \code{\link[=build_environment_kernels]{build_environment_kernels()}} or
\code{\link[=add_environment_kernel_interactions]{add_environment_kernel_interactions()}}. The \code{"correlation"} kernel is
linear and contains no implicit interaction feature space.}

\item{bandwidth}{Optional Gaussian-kernel bandwidth; defaults to the median
pairwise distance.}

\item{variables}{For \code{source = "enviromic"}: optional character vector (column
names) or integer indices selecting which covariates to use. This is how a
breeder restricts the kinship to the \emph{limiting factors} they judge relevant
(e.g. only \code{mean_temp}, \code{total_precip}, \code{clay}) instead of every assembled
covariate. The valid names are exactly \code{colnames(x)}; call
\code{\link[=enviromic_variable_catalog]{enviromic_variable_catalog()}} to see every fetchable variable with its
meaning and units. Defaults to all columns.}

\item{weights}{For \code{source = "enviromic"}: optional numeric weights for the
selected covariates (a named vector is matched by name, otherwise taken in
column order). Higher weights make a variable count more toward the
similarity. Weighting is
applied as \eqn{\sqrt{w}} on the standardised columns, so both the
correlation and Gaussian kernels honour it. The default gives equal weight
to the selected columns. Non-equal weights require external agronomic or
empirical justification. They must not be used to manufacture
trait-specific modality weights when historical MET responses are absent.}

\item{geo}{Logical. If \code{TRUE}, \code{x} is treated as site coordinates and
great-circle (haversine) distance is used instead of Euclidean -- correct
for GPS latitude/longitude at continental scale. The columns are taken as
\code{latitude}, \code{longitude} (by those names if present, else the first two).
Only used with \verb{source = "enviromic", kernel = "gaussian"}.}

\item{missing}{Missing-covariate policy for \code{source = "enviromic"}:
\code{"error"} (default) or explicit column-median imputation. Prefer
\code{\link[=qc_environmental_data]{qc_environmental_data()}} when an audit ledger is required.}
}
\value{
A square environment relationship matrix with row/column names, ready
to pass to \code{\link[=select_environments]{select_environments()}}.
}
\description{
\code{\link[=select_environments]{select_environments()}} needs an environment-by-environment similarity matrix
\code{D}. \code{build_environment_relationship()} constructs one such matrix from
historical multi-environment trial responses or one matrix of environmental
covariates. The environmental route can include unseen sites for which
covariates, but no phenotypes, are available. It describes exposure
similarity; it is not automatically a trait-specific genetic covariance.
Use \code{\link[=build_environment_kernels]{build_environment_kernels()}} when weather, soil, management, geography,
and their interactions must remain separate.
}
\seealso{
\code{\link[=select_environments]{select_environments()}}.
}
