% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_common_range.R
\name{suggest_common_range}
\alias{suggest_common_range}
\title{Suggest a range of common-treatment counts to evaluate}
\usage{
suggest_common_range(
  n_test_entries_per_environment,
  n_treatments,
  n_environments,
  sparse_allocation = c("random", "disjoint"),
  rho = 0.5,
  target_se = 0.15,
  n_points = 8L
)
}
\arguments{
\item{n_test_entries_per_environment}{Plots per environment, \code{k}.}

\item{n_treatments}{Total number of candidate lines, \code{J}.}

\item{n_environments}{Number of environments, \code{E}.}

\item{sparse_allocation}{\code{"random"} (sparse overlap ~ \eqn{k^2/J}) or
\code{"disjoint"} (no sparse overlap; common treatments carry all connectivity).}

\item{rho}{Typical genetic correlation between environments (use a low value
for a broad/heterogeneous TPE). Default 0.5.}

\item{target_se}{Target standard error for the estimated correlation. Default
0.15. Lower values demand more connectivity.}

\item{n_points}{Approximate number of grid points. Default 8.}
}
\value{
A list with \code{common_values} (integer vector for
\code{\link[=tune_common_treatments]{tune_common_treatments()}}) and \code{rationale} (the target connectivity, the
expected sparse overlap, the connectivity-driven and coverage bounds, and
the hard cap).
}
\description{
The number of common treatments has three governing bounds: a hard cap
(\code{C < k}, the per-environment capacity), a connectivity-driven upper bound
(enough genotypes shared between environment pairs to estimate their genetic
correlations, net of what the sparse overlap already provides), and a coverage
reference (where unique lines can no longer be covered). \code{suggest_common_range()}
turns these into a grid to pass as \code{common_values} to
\code{\link[=tune_common_treatments]{tune_common_treatments()}}.
}
\details{
The connectivity target follows the sampling error of a correlation: to
estimate a genetic correlation \code{rho} with standard error \code{target_se},
\deqn{\text{target\_shared} \approx \left(\frac{1-\rho^2}{\text{target\_se}}\right)^2}
connecting genotypes are needed per environment pair. Random sparse allocation
already supplies about \eqn{k^2/J} shared genotypes per pair; disjoint
(non-overlapping) designs supply none, so common treatments must provide the
whole amount. Weak correlations (\code{rho} near 0) need more, which is why the
optimum rises as environments become weakly correlated.

The coverage reference \eqn{(kE - J)/(E-1)} is reported but is \emph{not} used as a
hard cap: under weak correlation the optimum can lie beyond it, trading
coverage for connectivity.
}
\seealso{
\code{\link[=tune_common_treatments]{tune_common_treatments()}}, \code{\link[=suggest_safe_k]{suggest_safe_k()}}.
}
