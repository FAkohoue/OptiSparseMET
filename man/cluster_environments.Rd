% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_environments.R
\name{cluster_environments}
\alias{cluster_environments}
\title{Partition environments into a specified number of mega-environments}
\usage{
cluster_environments(
  D,
  n_clusters,
  method = c("hclust", "kmeans"),
  Sigma_E = NULL,
  seed = NULL
)
}
\arguments{
\item{D}{Square, finite, named environment relationship/similarity matrix.}

\item{n_clusters}{Number of mega-environments fixed by external evidence.}

\item{method}{\code{"hclust"} (default) or \code{"kmeans"}. Hierarchical clustering
operates on kernel-induced distances. K-means operates on positive-eigen
kernel principal coordinates, not directly on raw similarities, and uses
a finite-descent relocation algorithm that always terminates.}

\item{Sigma_E}{Optional environment covariance to subset per cluster
(defaults to \code{D}).}

\item{seed}{Optional integer used by k-means.}
}
\value{
A list with \code{membership} (a named integer cluster vector), \code{clusters}
(a list of environment-name vectors), and \code{Sigma_E} (a list of
within-cluster covariance matrices).
}
\description{
\code{cluster_environments()} is the low-level function for situations where the
number of groups is fixed by external evidence. In routine use,
\code{\link[=infer_mega_environments]{infer_mega_environments()}} should be preferred because it estimates the
number of supported groups and refuses to impose unstable hard groupings.
}
\seealso{
\code{\link[=infer_mega_environments]{infer_mega_environments()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}, \code{\link[=simulate_met]{simulate_met()}}.
}
