% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_management_interactions.R
\name{add_management_interactions}
\alias{add_management_interactions}
\title{Build management interaction covariates}
\usage{
add_management_interactions(management, interactions, drop_na = TRUE)
}
\arguments{
\item{management}{A data frame with an \code{environment} column plus management
covariates.}

\item{interactions}{A list, each element a character vector of two or more
column names in \code{management} to interact.}

\item{drop_na}{Replace non-finite products (from missing inputs) with 0.
Default \code{TRUE}.}
}
\value{
The \code{management} data frame with the interaction columns appended
(names joined by \code{":"}).
}
\description{
Some management effects only make sense in combination -- fertiliser \emph{dose}
matters differently under each fertiliser \emph{type}, or planting \emph{mode} interacts
with \emph{irrigation}. \code{add_management_interactions()} adds those interaction
columns to a management data frame before it is passed to
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}, so the environmental covariate matrix can
capture them.
}
\details{
Each interaction is the element-wise product of its variables after expansion:
a numeric variable is used as-is; a categorical variable is expanded to its
one-hot indicators. So \code{list(c("fert_type", "fert_dose"))} produces one column
per fertiliser type holding the dose within that type (0 otherwise), e.g.
\code{fert_type_NPK:fert_dose} and \code{fert_type_urea:fert_dose}. Two categorical
variables give the combined-level indicators; two numeric variables give their
product. Higher-order interactions (three or more variables) are supported by
listing them together. The original main-effect columns are kept.
}
\section{When to use (and when it is redundant)}{

Explicit interactions are \strong{not always needed}. If the environmental kinship
uses the Gaussian (RBF) kernel in \code{\link[=build_environment_relationship]{build_environment_relationship()}}, that
kernel already \emph{represents} covariate interactions implicitly, so adding them
here is usually redundant and can even degrade the kernel (more dimensions,
distance concentration). Reach for this function when you (i) use the linear
\code{"correlation"} kernel, which captures no implicit interactions; (ii) want to
emphasise or select a specific agronomic combination as a limiting factor
(via the \code{variables}/\code{weights} arguments of
\code{\link[=build_environment_relationship]{build_environment_relationship()}}); or (iii) need interpretable interaction
features for reporting or a reaction-norm model. Keep the list short and
agronomy- or \code{\link[=screen_interactions]{screen_interactions()}}-driven. Management \strong{doses} are a
separate matter -- always encode them with \code{\link[=nest_dose_within_type]{nest_dose_within_type()}}, which
corrects a mis-encoded variable (100 kg NPK is not 100 kg urea) rather than
adding a modelling interaction.
}

\examples{
mg <- data.frame(environment = c("E1", "E2", "E3"),
                 fert_type = c("NPK", "urea", "NPK"),
                 fert_dose = c(120, 80, 100),
                 planting_mode = c("transplanting", "direct", "direct"))
add_management_interactions(mg, list(c("fert_type", "fert_dose")))
}
\seealso{
\code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}.
}
