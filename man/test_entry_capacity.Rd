% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_budget.R
\name{test_entry_capacity}
\alias{test_entry_capacity}
\title{Test-entry capacity of an offered plot budget}
\usage{
test_entry_capacity(
  total_plots,
  n_checks = 0,
  n_blocks = 1,
  avg_reps_per_entry = 1
)
}
\arguments{
\item{total_plots}{Total plots offered at the site.}

\item{n_checks}{Number of check treatments (each appears in every block).}

\item{n_blocks}{Number of blocks (checks contribute \code{n_checks * n_blocks}
plots).}

\item{avg_reps_per_entry}{Average plots per test entry: 1 for an
augmented/unreplicated design, higher for p-rep or replicated entries
(e.g. 1.3 if 30 percent of entries are duplicated). Default 1.}
}
\value{
Integer number of distinct test entries.
}
\description{
Field partners offer space in \strong{total plots}, but the allocation capacity
(\code{n_test_entries_per_environment}) and \code{\link[=suggest_site_capacity]{suggest_site_capacity()}} count \strong{test
entries}, which exclude the check plots and replication a within-environment
design adds. Because checks appear in every block, a design uses
\eqn{b \times c} check plots (blocks times checks) plus the replicated and
unreplicated entry plots. \code{test_entry_capacity()} returns the largest number
of distinct test entries that fit in \code{total_plots}.
}
\examples{
test_entry_capacity(400, n_checks = 2, n_blocks = 4)   # 2 checks x 4 blocks -> 392
}
\seealso{
\code{\link[=required_plots]{required_plots()}}, \code{\link[=field_plot_accounting]{field_plot_accounting()}},
\code{\link[=suggest_site_capacity]{suggest_site_capacity()}}, \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.
}
