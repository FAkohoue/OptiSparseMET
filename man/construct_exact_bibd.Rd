% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construct_exact_bibd.R
\name{construct_exact_bibd}
\alias{construct_exact_bibd}
\title{Construct an exact balanced incomplete block design (when one exists)}
\usage{
construct_exact_bibd(treatments, environments, k)
}
\arguments{
\item{treatments}{Character vector of treatment (line) IDs, length \eqn{J}.}

\item{environments}{Character vector of environment names, length \eqn{I}
(the BIBD blocks).}

\item{k}{Integer. Number of treatments per environment (block size).}
}
\value{
A list with \code{allocation_matrix} (a \eqn{J \times I} 0/1 incidence
matrix with treatments as rows and environments as columns), \code{lambda}, and
\code{is_bibd} (logical; constant-lambda balance check). Errors if \code{crossdes} is not
installed or if the parameters admit no BIBD.
}
\description{
\code{construct_exact_bibd()} builds a strict balanced incomplete block design
(BIBD) -- every pair of treatments co-occurring in the same environment an
equal number of times (constant \eqn{\lambda}) -- by delegating to
\code{crossdes::find.BIB()} and verifying the balance property (constant pairwise
concurrence lambda and constant replication) directly from the concurrence
matrix.

A strict BIBD exists only when the necessary conditions hold: the count
identity \eqn{J r = I k}, integrality of
\eqn{\lambda = r(k-1)/(J-1)}, and Fisher's inequality \eqn{I \ge J}. For
sparse multi-environment testing the number of treatments \eqn{J} usually far
exceeds the number of environments \eqn{I}, so Fisher's inequality fails and
no BIBD exists; in that regime use \code{\link[=allocate_sparse_met]{allocate_sparse_met()}} with
\code{allocation_method = "equireplicate"} and \code{balance = "env_pair"} /
\code{"line_pair"} for the achievable near-balanced design.
}
\examples{
\dontrun{
## Symmetric BIBD: v = b = 7, r = k = 3, lambda = 1 (Fano plane)
bibd <- construct_exact_bibd(
  treatments   = paste0("L", 1:7),
  environments = paste0("E", 1:7),
  k            = 3
)
bibd$is_bibd     # TRUE
bibd$lambda      # 1
}

}
\seealso{
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} for the near-balanced equireplicate
allocation used when a strict BIBD does not exist;
\code{\link[=check_equireplicate_feasibility]{check_equireplicate_feasibility()}} to test \code{strict_bibd_possible} first.
}
