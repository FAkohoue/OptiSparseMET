% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/envirotype_features.R
\name{fetch_weather_series}
\alias{fetch_weather_series}
\title{Fetch a daily weather series from NASA POWER}
\usage{
fetch_weather_series(
  sites,
  pars = NULL,
  temporal = "daily",
  max_tries = 3L,
  pause = 3,
  cache_dir = NULL,
  workers = 1L
)
}
\arguments{
\item{sites}{Data frame with \code{environment}, \code{latitude}, \code{longitude},
\code{start_date}, \code{end_date}.}

\item{pars}{NASA POWER parameter codes. Defaults to the six core variables;
see \code{\link[=enviromic_variable_catalog]{enviromic_variable_catalog()}}.}

\item{temporal}{\code{"daily"} (default) or \code{"hourly"} where available.}

\item{max_tries}{Number of attempts per site before giving up (the POWER API
sometimes times out). Default 3.}

\item{pause}{Seconds to wait between retries. Default 3.}

\item{cache_dir}{Optional directory to cache each site's raw POWER response
(keyed by coordinates/dates/parameters). Re-runs read the cache instead of
re-downloading -- a large speed-up for planning. Default \code{NULL} (no cache).}

\item{workers}{Number of parallel workers for the fetch. \verb{> 1} uses
\pkg{future.apply} (install it) and is much faster for many site-years.
Default 1 (sequential).}
}
\value{
A long data frame with \code{environment}, the actual \code{date}, \code{day}
(0-based day-after-start computed from that date), and one column per
parameter; or \code{NULL} if nothing was retrieved. A \code{provenance} attribute
records request dates, returned dates, row counts, and cache use by site.
}
\description{
Returns the per-site \strong{daily} weather series (long format) rather than a
single aggregated value, so it can be turned into rich temporal features with
\code{\link[=envirotype_features]{envirotype_features()}}. Requires internet and the \pkg{nasapower} package.
}
\seealso{
\code{\link[=envirotype_features]{envirotype_features()}}, \code{\link[=build_enviromic_covariates]{build_enviromic_covariates()}}.
}
