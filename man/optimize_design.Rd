% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_design.R
\name{optimize_design}
\alias{optimize_design}
\title{Criterion-driven optimisation of a sparse MET allocation}
\usage{
optimize_design(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  objective = list(),
  sigma_g2 = 1,
  sigma_e2 = 1,
  tpe_weights = NULL,
  env_efficiency = NULL,
  design_evaluator = NULL,
  preserve = c("margins", "replication", "none"),
  allocation_criterion = c("weighted", "mean_pev", "cdmean", "expected_gain"),
  search_method = c("annealing", "exchange", "genetic", "exact", "mip"),
  common_set = c("fixed", "optimize_jointly", "none"),
  common_treatments = NULL,
  common_count_range = NULL,
  common_weight = 0.1,
  minimum_treatment_environments = 0L,
  maximum_treatment_environments = NULL,
  robust = NULL,
  robust_aggregate = c("mean", "cvar", "min"),
  cvar_alpha = 0.25,
  seed_available = NULL,
  seed_required_per_environment = NULL,
  minimum_seed_buffer = 0,
  environment_capacities = NULL,
  minimum_environment_entries = 1L,
  n_starts = 5L,
  iters = 200L,
  cooling = 0.97,
  exact_max_cells = 16L,
  seed = NULL,
  max_dim = 6000L,
  verbose = FALSE
)
}
\arguments{
\item{allocation_matrix}{Starting genotype-by-environment 0/1 matrix
(dimnames required), e.g. from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{G, Sigma_E}{Relationship inputs for \code{\link[=met_information]{met_information()}}.}

\item{objective}{A list of objective settings: \code{weights} (list with \code{gain},
\code{reliability}, \code{cost}), \code{prop}, \code{sigma_g}, \code{cost_per_plot}, \code{trait_weights},
\code{trait_gencov}, \code{budget}.}

\item{sigma_g2, sigma_e2}{Nominal variance components (used when \code{robust} is
\code{NULL}, and to normalise the objective reference).}

\item{tpe_weights}{Target-population weights passed to \code{\link[=met_information]{met_information()}}.}

\item{env_efficiency}{Optional fixed site-efficiency vector. For genuine
allocation-replication-layout optimisation, use \code{design_evaluator}.}

\item{design_evaluator}{Optional function taking one candidate allocation
matrix and returning a list with any of \code{reps}, \code{env_efficiency},
\code{local_information}, \code{cost_per_plot}, \code{fixed_plot_overhead}, \code{feasible},
\code{fieldbooks}, and \code{metadata}. It is called once per unique candidate (results are cached),
allowing the annealing search to score actual plantable local layouts.}

\item{preserve}{\code{"margins"}, \code{"replication"}, or \code{"none"} (see Details).}

\item{allocation_criterion}{\code{"weighted"}, \code{"mean_pev"}, \code{"cdmean"}, or
\code{"expected_gain"}; passed to \code{\link[=design_objective]{design_objective()}}.}

\item{search_method}{Search engine. \code{"annealing"} is the scalable default;
\code{"exchange"} accepts improving moves only; \code{"genetic"} uses an elitist
mutation-selection population; and \code{"exact"} exhaustively evaluates all
feasible binary designs for very small problems. \code{"mip"} is an alias for
the dependency-free exact binary search and is intentionally guarded by
\code{exact_max_cells}.}

\item{common_set}{Whether globally common treatments are \code{"fixed"}, forbidden
(\code{"none"}), or allowed to change in size and identity
(\code{"optimize_jointly"}).}

\item{common_treatments}{Optional fixed common-treatment IDs. With
\code{common_set = "fixed"}, \code{NULL} infers them from all-environment rows of the
starting design.}

\item{common_count_range}{Allowed minimum and maximum number of global common
treatments during joint optimisation. Defaults to the feasible range.}

\item{common_weight}{Non-negative weight for a connectivity/diversity utility
during joint common-set search. It is constant or zero outside
\code{common_set = "optimize_jointly"}.}

\item{minimum_treatment_environments, maximum_treatment_environments}{Scalar or
treatment-specific bounds on the number of environments assigned to each
treatment.}

\item{robust}{Optional list from \code{\link[=robust_scenarios]{robust_scenarios()}}; if given, designs are
scored by \code{\link[=robust_design_score]{robust_design_score()}}.}

\item{robust_aggregate, cvar_alpha}{Aggregation for robust scoring.}

\item{seed_available}{Optional network-wide seed inventory, as a named
numeric vector or a data frame with columns \code{Treatment} and
\code{SeedAvailable}. Optimisation moves that would overspend a treatment are
rejected.}

\item{seed_required_per_environment}{Seed consumed by one allocation in each
environment: a positive scalar, a named numeric vector, or a data frame
with columns \code{Environment} and \code{SeedRequiredPerPlot}.}

\item{minimum_seed_buffer}{Non-negative seed reserve retained for every
treatment. Default 0.}

\item{environment_capacities}{Optional scalar or per-environment upper bounds
on the number of allocated entries. This prevents unconstrained add/relocate
moves from exceeding contracted site capacity.}

\item{minimum_environment_entries}{Scalar or per-environment lower bounds.
Default 1 keeps every environment connected.}

\item{n_starts}{Number of random restarts. Default 5.}

\item{iters}{Annealing iterations per restart. Default 200.}

\item{cooling}{Geometric cooling factor per iteration. Default 0.97.}

\item{exact_max_cells}{Maximum \code{nrow * ncol} accepted by exact/\code{"mip"}
enumeration.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Guard passed to \code{\link[=met_information]{met_information()}}.}

\item{verbose}{Print per-restart progress.}
}
\value{
A list with the best \code{allocation_matrix}, \code{score}, raw objective
\code{components}, starting score, restart \code{trace}, full \code{trajectory}, move and
evaluator \code{diagnostics}, preservation and robustness settings, optional
\code{seed_summary}, the best \code{design_evaluation}, and a validated
\code{sparse_met_design} in \code{design}.
}
\description{
Searches for the genotype-by-environment allocation that maximises a
\code{\link[=design_objective]{design_objective()}} -- any weighted combination of statistical quality
(reliability / CDmean), expected genetic gain (single-trait or multi-trait
index), and resource cost -- by exchange, simulated annealing, evolutionary
mutation-selection, or guarded exact binary search. Unlike the greedy
constructors in \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, the
allocation itself is optimised against the criterion on the coupled
\code{\link[=met_information]{met_information()}} matrix. The scoring can be made robust to uncertain
variance components by passing \code{\link[=robust_scenarios]{robust_scenarios()}}.
}
\details{
The move set is chosen by \code{preserve}, which controls what is held fixed:
\describe{
\item{\code{"margins"}}{Margin-preserving swaps only: both the replication of
every genotype and the size of every environment stay fixed, so a fixed
resource budget and (if started from an M4 design) equal replication are
retained while quality is improved.}
\item{\code{"replication"}}{Relocations: each genotype keeps its number of
environments (equal replication preserved, M4-style) but environment
sizes may change -- criterion-driven allocation with site-specific
capacities.}
\item{\code{"none"}}{Relocate/add/remove: plots may change (bounded by
\code{objective$budget}), so cost is optimised jointly with quality and gain --
the fully simultaneous mode.}
}
All evaluations are deterministic; components are normalised by the starting
design so the objective weights are on a comparable scale.
}
\examples{
set.seed(1)
G <- crossprod(matrix(rnorm(24 * 60), 60, 24)) / 60 + diag(24) * 0.3
dimnames(G) <- list(paste0("L", 1:24), paste0("L", 1:24))
SigE <- diag(3) * 0.5 + 0.5
dimnames(SigE) <- list(paste0("E", 1:3), paste0("E", 1:3))
M <- matrix(0L, 24, 3, dimnames = list(rownames(G), colnames(SigE)))
for (e in 1:3) M[sample(24, 12), e] <- 1L
opt <- optimize_design(M, G, SigE, preserve = "margins",
                       n_starts = 2, iters = 40, seed = 1)
opt$score >= opt$score_start
}
\seealso{
\code{\link[=design_objective]{design_objective()}}, \code{\link[=robust_design_score]{robust_design_score()}}, \code{\link[=pareto_designs]{pareto_designs()}},
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.
}
