% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical_envirotype.R
\name{historical_envirotype}
\alias{historical_envirotype}
\title{Characterise candidate sites from past seasons (planning-stage envirotyping)}
\usage{
historical_envirotype(
  sites = NULL,
  years = NULL,
  window = NULL,
  pars = NULL,
  envirotype = list(),
  variability = c("sd", "cv"),
  daily_by_year = NULL,
  max_tries = 3L,
  cache_dir = NULL,
  workers = 1L,
  station_daily = NULL,
  station_correction = c("none", "mean_bias", "linear"),
  summary_components = c("mean", "sd", "q10", "q50", "q90", "trend"),
  min_years = 3L,
  feature_function = c("generic", "rice"),
  min_daily_coverage = 0.8,
  daily_missing_action = c("impute", "warn", "error"),
  weather_ranges = .default_weather_ranges()
)
}
\arguments{
\item{sites}{Data frame with \code{environment}, \code{latitude}, \code{longitude} (network
path). May be \code{NULL} when \code{daily_by_year} is given.}

\item{years}{Integer vector of past years to characterise the site with.}

\item{window}{The growing window, as \strong{month-day} strings. Either a length-2
vector \code{c(start_md, end_md)} applied to every site, OR a data frame with
columns \code{environment}, \code{start_md}, \code{end_md} for \strong{site-specific} windows
(e.g. Peru's season differs from Colombia's), OR supply \code{start_md}/\code{end_md}
columns directly on \code{sites}. A window whose \code{end_md} precedes its \code{start_md}
is treated as crossing into the next calendar year (e.g. \code{"11-01"}->\code{"02-28"}).}

\item{pars}{NASA POWER parameter codes (see \code{\link[=enviromic_variable_catalog]{enviromic_variable_catalog()}}).}

\item{envirotype}{A list of arguments passed to \code{\link[=envirotype_features]{envirotype_features()}}
(\code{windows}, \code{stats}, \code{thresholds}, \code{gdd}, \code{functional}).}

\item{variability}{\code{"sd"} (default) or \code{"cv"} for the interannual-variability
summary.}

\item{daily_by_year}{Optional named list (by year) of long daily data frames
to use instead of fetching -- enables offline use and reproducibility.}

\item{max_tries}{Retries per site-year for the NASA POWER fetch (the service
intermittently times out). Default 3.}

\item{cache_dir, workers}{Passed to \code{\link[=fetch_weather_series]{fetch_weather_series()}} to cache responses
(fast re-runs) and fetch in parallel.}

\item{station_daily}{Optional on-station (manually collected) daily weather to
merge with the downloaded series: a long data frame with \code{environment},
\code{day} (0-based day-after-start), optionally \code{year}, and weather columns.
Station values take precedence; downloaded data fills the variables and days
the station did not measure.}

\item{station_correction}{Optional correction of downloaded weather against
overlapping station records: \code{"none"}, \code{"mean_bias"}, or \code{"linear"}.}

\item{summary_components}{Historical summaries included in \code{combined}.
Available components are \code{"mean"}, \code{"sd"} (or \code{"cv"} according to
\code{variability}), \code{"q10"}, \code{"q50"}, \code{"q90"}, \code{"trend"}, and
\code{"n_observed"}.}

\item{min_years}{Minimum observed seasons required per environment-feature.
Summaries with fewer seasons are set to missing and recorded in
\code{year_coverage}.}

\item{feature_function}{\code{"generic"} uses \code{\link[=envirotype_features]{envirotype_features()}};
\code{"rice"} uses \code{\link[=rice_weather_features]{rice_weather_features()}} and therefore adds rice-specific
stress physiology before across-year summarisation.}

\item{min_daily_coverage}{Minimum within-season coverage required for every
weather variable at every environment.}

\item{daily_missing_action}{\code{"impute"} (linear within site, then median),
\code{"warn"}, or \code{"error"}.}

\item{weather_ranges}{Named physical plausibility ranges for daily weather.
Defaults cover common NASA POWER variables.}
}
\value{
A list with \code{typical} (environment-by-feature matrix, across-year
mean), \code{variability} (across-year SD or CV), \code{combined} (typical +
variability columns, suffixed \verb{_iav}, so the between-year variation can
enter the relationship matrix), \code{n_years}, and \code{per_year} (the list of
per-year feature matrices). Build \code{D} from \code{typical} (mean conditions),
\code{combined} (mean + interannual risk), or the consensus of the per-year
matrices from \code{\link[=assess_envirotype_stability]{assess_envirotype_stability()}}.
}
\description{
At the design stage the upcoming season has not happened, so its weather
cannot be fetched. \code{historical_envirotype()} characterises each candidate site
from \strong{several past seasons} over the intended calendar window: it builds
\code{\link[=envirotype_features]{envirotype_features()}} for every year and summarises them into (i) the
\strong{typical} environmental profile (the across-year mean, used to predict the
environment relationship and select sites) and (ii) the \strong{interannual
variability} of each feature (the across-year standard deviation or CV -- the
site's environmental \emph{risk}, which feeds robust design). This is the
planning-appropriate substitute for this-season data.
}
\details{
For the network path, supply \code{sites} (with \code{latitude}/\code{longitude}) and a
calendar \code{window} of month-day strings; the growing window is applied to each
year in \code{years}, the daily series fetched with \code{\link[=fetch_weather_series]{fetch_weather_series()}}, and
features computed with \code{\link[=envirotype_features]{envirotype_features()}}. For offline use (or your own
records) pass \code{daily_by_year}, a named list of long daily data frames (one per
year). The typical profile plugs into \code{\link[=build_environment_relationship]{build_environment_relationship()}}; the
variability profile flags sites whose environment swings year to year, which
informs descriptive mega-environment stability and which separate weather
structures should enter \code{\link[=robust_scenarios]{robust_scenarios()}}. It does not identify genetic
\code{Sigma_E} without historical MET values.
}
\examples{
# Offline: three past seasons of daily data supplied directly.
mk <- function(base) rbind(
  data.frame(environment = "E1", day = 0:59,
             T2M = base + rnorm(60), T2M_MAX = base + 6 + rnorm(60)),
  data.frame(environment = "E2", day = 0:59,
             T2M = base + 4 + rnorm(60), T2M_MAX = base + 10 + rnorm(60)))
hist <- historical_envirotype(
  years = 2020:2022,
  daily_by_year = list(`2020` = mk(24), `2021` = mk(26), `2022` = mk(22)),
  envirotype = list(windows = 2, stats = "mean"))
hist$typical
hist$variability
}
\seealso{
\code{\link[=envirotype_features]{envirotype_features()}}, \code{\link[=fetch_weather_series]{fetch_weather_series()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}, \code{\link[=robust_scenarios]{robust_scenarios()}}.
}
