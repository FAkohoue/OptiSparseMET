% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate_design_criteria.R
\name{validate_design_criteria}
\alias{validate_design_criteria}
\title{Validate design-stage criteria against realised selection outcomes}
\usage{
validate_design_criteria(
  allocations,
  G,
  Sigma_E = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  env_efficiency = NULL,
  n_sim = 30L,
  select_fraction = 0.1,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocations}{A named list of \eqn{J \times E} allocation matrices
(for example the \code{allocation_matrix} outputs of \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}).
Use a diverse set -- balanced, sparse, and optimised -- so the correlation
is informative.}

\item{G}{Genomic (or pedigree) relationship matrix with row/column names
covering the lines in \code{allocation_matrix}.}

\item{Sigma_E}{Optional \eqn{E \times E} environment covariance (genetic
correlations across environments). Defaults to the identity (independent
environments, unit variance).}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{env_efficiency}{Optional length-\eqn{E} vector of within-environment
design efficiency factors \eqn{\epsilon_e \in (0,1]} (the coupling to the
local design). Defaults to 1 for every environment.}

\item{n_sim}{Number of independent simulation replicates.}

\item{select_fraction}{Top fraction selected for the outcome metrics.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A list with:
\describe{
\item{\code{table}}{One row per design: \code{design}, the design-based \code{mean_PEV}
and \code{CDmean}, and the realised \code{accuracy}, \code{gain}, \code{common_selected}, and
\code{avg_rank}.}
\item{\code{correlations}}{A data frame of Pearson correlations between each
design-stage criterion and each realised outcome across the designs (with
the number of designs, \code{n}).}
}
}
\description{
Cheap design-stage criteria (mean PEV, CDmean, ...) are only useful if they
actually track the outcomes a breeder cares about -- selection accuracy and
genetic gain. Mothukuri et al. (2025) show that this link, while real, is
often weak and programme-specific, so it should be measured rather than assumed.

\code{validate_design_criteria()} takes a set of candidate allocations, computes
for each both the design-based criteria (via \code{\link[=met_information]{met_information()}}) and the
realised outcomes (via \code{\link[=simulate_met]{simulate_met()}}), and reports their correlation across
designs. A strong negative correlation between \code{mean_PEV} and accuracy (or
positive between \code{CDmean} and accuracy) means the cheap criterion is a
trustworthy proxy for your programme; a weak one is a warning that you should
rely on simulation, not the criterion, when choosing a design.
}
\references{
Mothukuri, S. R., Beyene, Y., Gultas, M., Burgueno, J., & Griebel, S. (2025).
Optimization of sparse phenotyping strategy in multi-environmental trials in
maize. \emph{Theoretical and Applied Genetics}, 138, 62.
}
\seealso{
\code{\link[=met_information]{met_information()}}, \code{\link[=simulate_met]{simulate_met()}}, \code{\link[=sensitivity_varcomp]{sensitivity_varcomp()}}.
}
