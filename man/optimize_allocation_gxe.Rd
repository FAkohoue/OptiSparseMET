% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_allocation_gxe.R
\name{optimize_allocation_gxe}
\alias{optimize_allocation_gxe}
\title{Optimise entry-across-environment allocation for G x E prediction (decision 3)}
\usage{
optimize_allocation_gxe(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  env_efficiency = NULL,
  iter = 200L,
  seed = NULL,
  max_dim = 2000L
)
}
\arguments{
\item{allocation_matrix}{A \eqn{J \times E} 0/1 incidence matrix (lines as
rows, environments as columns) with row and column names. Typically
\code{alloc$allocation_matrix} from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{G}{Genomic (or pedigree) relationship matrix with row/column names
covering the lines in \code{allocation_matrix}.}

\item{Sigma_E}{Optional \eqn{E \times E} environment covariance (genetic
correlations across environments). Defaults to the identity (independent
environments, unit variance).}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{env_efficiency}{Optional length-\eqn{E} vector of within-environment
design efficiency factors \eqn{\epsilon_e \in (0,1]} (the coupling to the
local design). Defaults to 1 for every environment.}

\item{iter}{Number of swap iterations.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A list with the refined \code{allocation_matrix}, \code{mean_PEV_before},
\code{mean_PEV_after}, and \code{CDmean_after}.
}
\description{
\code{optimize_allocation_gxe()} refines a sparse allocation to maximise the
across-TPE prediction reliability (minimise mean PEV) under the coupled G x E
information matrix of \code{\link[=met_information]{met_information()}}. It is the corrected, reproducible
form of decision 3 in Colmant et al. (2026): instead of the unbounded spread
proxy \eqn{-q'Dq} (with \eqn{D = G \otimes E}) or the underspecified
connectedness objective functions, it optimises the quantity that actually
governs prediction accuracy.
}
\details{
Swaps exchange a treatment between two environments (treatment \code{a} from \code{e1}
to \code{e2}, treatment \code{b} from \code{e2} to \code{e1}), so every treatment keeps its
replication count and every environment keeps its size -- the equireplicate
margins are preserved. A swap is accepted when it lowers the across-TPE mean
PEV. Each evaluation solves the (\eqn{JE \times JE}) information matrix, so
keep \code{J * E} within \code{max_dim}.
}
\seealso{
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} for the initial equireplicate allocation;
\code{\link[=met_information]{met_information()}} for the objective.
}
