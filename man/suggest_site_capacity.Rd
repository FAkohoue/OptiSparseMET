% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_site_capacity.R
\name{suggest_site_capacity}
\alias{suggest_site_capacity}
\title{Suggest the optimal number of plots for an unconstrained site}
\usage{
suggest_site_capacity(
  G,
  Sigma_E,
  candidate_plots,
  scope = c("single", "all", "subset"),
  n_env = NULL,
  other_n_env = 2L,
  other_capacity = NULL,
  focal_envs = NULL,
  site_capacities = NULL,
  plots_per_entry = 1,
  check_plots_per_site = 0,
  cost_per_plot = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  n_sim = 30L,
  select_fraction = 0.1,
  min_gain = 0.002,
  select = c("max", "diminishing", "target"),
  target_accuracy = 0.7,
  seed = NULL,
  max_dim = 6000L,
  robust = NULL,
  robust_aggregate = c("min", "mean", "cvar"),
  cvar_alpha = 0.25
)
}
\arguments{
\item{G}{Genomic relationship matrix (row/column names) with at least
\code{max(candidate_plots)} genotypes.}

\item{Sigma_E}{Environment covariance with at least \code{1 + other_n_env}
environments.}

\item{candidate_plots}{Integer vector of candidate entry counts to evaluate
per site (e.g. \code{seq(20, 200, by = 20)}).}

\item{scope}{\code{"single"} (default) optimises one generous site with the others
fixed; \code{"all"} optimises every site together; \code{"subset"} varies only
\code{focal_envs}.}

\item{n_env}{Total number of sites, used when \code{scope = "all"}. Defaults to
\code{1 + other_n_env}.}

\item{other_n_env}{For \code{scope = "single"}: number of other sites in the trial
(each at \code{other_capacity}). Default 2.}

\item{other_capacity}{For \code{scope = "single"}: entries tested at each of the
other sites. Defaults to the median of \code{candidate_plots}; set it to the real
capacities of the constrained sites for a realistic answer.}

\item{focal_envs}{For \code{scope = "subset"}, environment names whose capacity is
set to each candidate value.}

\item{site_capacities}{For \code{scope = "subset"}, a named vector covering every
environment. Values for non-focal environments remain fixed.}

\item{plots_per_entry}{Plots consumed per entry (for cost/plot accounting):
a scalar or a named/vector value per environment. Default 1.}

\item{check_plots_per_site}{Check plots added at each site (e.g.
\code{n_checks * n_blocks}), so \code{total_plots} reflects the real field size
including checks, not just test entries. May be scalar or per-environment.
Default 0. See
\code{\link[=test_entry_capacity]{test_entry_capacity()}} to convert an offered plot budget.}

\item{cost_per_plot}{Optional cost of one plot; enables a \code{gain_per_cost}
column. Default \code{NULL}.}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{n_sim}{Number of independent simulation replicates.}

\item{select_fraction}{Top fraction selected (on predicted BV) for the
genetic-gain statistic.}

\item{min_gain}{Minimum marginal accuracy gain per added plot required to keep
growing the site (used by \code{select = "diminishing"}). Default \code{0.002}.}

\item{select}{How to pick the optimum \strong{within the candidate interval}.
\code{"max"} (default) chooses the capacity that \strong{maximises accuracy} within the
interval -- for an unrestricted site, the breeder just supplies the interval
and the package returns the best size in it, with no further choice needed.
\code{"diminishing"} returns the knee (where marginal gain drops below \code{min_gain})
if you would rather not over-invest, and \code{"target"} the smallest capacity
reaching \code{target_accuracy}. \code{candidate_plots} \emph{is} the interval.}

\item{target_accuracy}{Target for \code{select = "target"}. Default 0.7.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}

\item{robust}{Optional list from \code{\link[=robust_scenarios]{robust_scenarios()}}. When supplied, every
candidate capacity is simulated under every variance/covariance scenario.
This allows unweighted environmental interaction kernels to affect the
capacity recommendation even when no historical MET covariance is
available.}

\item{robust_aggregate}{Aggregation across \code{robust} scenarios: \code{"min"}
(default; maximin), \code{"mean"}, or \code{"cvar"}.}

\item{cvar_alpha}{Lower-tail fraction used when
\code{robust_aggregate = "cvar"}.}
}
\value{
A list with \code{table} (one row per candidate: \code{plots} = per-site
entries, \code{total_plots} across the trial, \code{accuracy_mean}, \code{gain_mean},
\code{marginal_accuracy_per_plot}, and optionally \code{gain_per_cost}) and
\code{recommended_plots} (the diminishing-returns optimum; the per-site capacity
to use everywhere when \code{scope = "all"}, at the focal site when
\code{scope = "single"}, or at all focal sites when \code{scope = "subset"}), plus
robust-analysis status, aggregation rule, and scenario count.
}
\description{
When a location is offered with no hard limit on field capacity ("use as much
space as you like"), more plots test more of the target population and raise
prediction accuracy and genetic gain -- but with diminishing returns, and
every extra plot costs resources. \code{suggest_site_capacity()} sweeps candidate
plot counts, simulates realised accuracy and gain, and recommends the plot
number at the point of diminishing returns, so the breeder maximises gain
while not wasting resources. Three scopes are supported:
\describe{
\item{\code{scope = "single"}}{One generous site varies while the other sites
are held at their real capacities. Returns the plot number for that site.}
\item{\code{scope = "all"}}{\emph{Every} site is unconstrained: the candidate capacity
is applied to all \code{n_env} sites at once, and the recommendation is the
common per-site capacity to use everywhere.}
\item{\code{scope = "subset"}}{Only \code{focal_envs} vary; every other environment
is held at its named value in \code{site_capacities}. This is the appropriate
mode when several sites are unconstrained but partner sites have fixed
plot commitments.}
}
}
\details{
For each candidate capacity \code{n}, a nested sparse design is built: the focal
environment(s) test \code{n} entries while all non-focal capacities stay fixed.
\code{\link[=simulate_met]{simulate_met()}} then returns realised selection accuracy and genetic gain
for the whole design under a common Monte Carlo stream. The marginal gain in
accuracy per additional \emph{trial-wide physical plot} is tracked; the
recommendation is the largest consecutive candidate whose marginal gain
still meets \code{min_gain}. Beyond that point extra plots buy little accuracy
and may be better spent elsewhere. When \code{cost_per_plot} is supplied,
gain-per-cost is also reported.

When \code{robust} is supplied, accuracy and gain are aggregated across its
variance/covariance scenarios before capacity selection. Maximin aggregation
does not use scenario probabilities and is therefore suitable for separate
main-effect and interaction structures whose relative weights are unknown.

Each plot holds one entry unless \code{plots_per_entry > 1} (e.g. replicated
checks), which scales the plot and cost accounting but not the number of
distinct entries.
}
\examples{
set.seed(1)
G <- crossprod(matrix(rnorm(80 * 150), 150, 80)) / 150 + diag(80) * 0.3
dimnames(G) <- list(paste0("L", 1:80), paste0("L", 1:80))
SigE <- diag(3) * 0.5 + 0.5
dimnames(SigE) <- list(paste0("E", 1:3), paste0("E", 1:3))
res <- suggest_site_capacity(G, SigE,
                             candidate_plots = c(20, 40, 60, 80),
                             other_n_env = 2, other_capacity = 30,
                             n_sim = 10, seed = 1)
res$recommended_plots
}
\references{
Colmant, A., Pita, F., & Covarrubias-Pazaran, G. (2026). Some objective
functions and ideas to optimize experimental designs in artificial selection
programs. \emph{Crop Science}, 66, e70337.
}
\seealso{
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, \code{\link[=sparsity_grid]{sparsity_grid()}}, \code{\link[=recommend_replication]{recommend_replication()}}.
}
