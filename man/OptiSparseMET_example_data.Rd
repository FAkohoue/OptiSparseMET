% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_example.R
\docType{data}
\name{OptiSparseMET_example_data}
\alias{OptiSparseMET_example_data}
\title{Example data for OptiSparseMET}
\format{
A named list with the following components:
\describe{
\item{\code{treatments}}{Character vector of test treatment IDs. These are the
candidate lines to be allocated across environments. Does not include
check treatments.}
\item{\code{environments}}{Character vector of environment names. Used as the
\code{environments} argument in \code{allocate_sparse_met()} and as keys in
\code{env_design_specs}.}
\item{\code{treatment_info}}{Data frame with one row per treatment in
\code{treatments}. Contains at minimum columns \code{Treatment} and \code{Family}.
Used as the \code{treatment_info} argument in \code{allocate_sparse_met()} when
\code{allocation_group_source = "Family"}.}
\item{\code{common_treatments}}{Character vector of treatment IDs forced into
all environments before sparse allocation. A subset of \code{treatments}.
Used as the \code{common_treatments} argument in \code{allocate_sparse_met()} and
\code{combine_met_fieldbooks()}.}
\item{\code{seed_info}}{Data frame with columns \code{Treatment} and
\code{SeedAvailable}. Contains one row per treatment in \code{treatments} and
gives the seed quantity available for each line. Used as the
\code{seed_available} argument in \code{assign_replication_by_seed()}.}
\item{\code{seed_required_per_plot}}{Data frame with columns \code{Environment} and
\code{SeedRequiredPerPlot}. Gives the per-plot seed requirement for each
environment, reflecting that seed consumption may differ across
locations. Used as the \code{seed_required_per_plot} argument in
\code{assign_replication_by_seed()} after subsetting to the environment of
interest.}
\item{\code{OptiSparseMET_GRM}}{Numeric matrix. Genomic relationship matrix
with row and column names matching the IDs in \code{treatments}. Used as the
\code{GRM} argument in \code{allocate_sparse_met()} when
\code{allocation_group_source = "GRM"}, and in \code{alpha_rc_stream()} or
\code{prep_famoptg()} when \code{cluster_source = "GRM"} or
\code{dispersion_source = "GRM"}.}
\item{\code{OptiSparseMET_A}}{Numeric matrix. Pedigree-based numerator
relationship matrix with row and column names matching the IDs in
\code{treatments}. Used as the \code{A} argument when
\code{allocation_group_source = "A"}, \code{cluster_source = "A"}, or
\code{dispersion_source = "A"}.}
\item{\code{OptiSparseMET_K}}{Numeric matrix. Relationship matrix intended for
mixed-model efficiency evaluation and dispersion optimization. Used as
the \code{K} argument in \code{alpha_rc_stream()} and \code{prep_famoptg()} when
\code{prediction_type \%in\% c("GBLUP", "PBLUP")} or
\code{dispersion_source = "K"}.}
\item{\code{sparse_example_args_random_balanced}}{Named list of arguments ready
to pass to \code{allocate_sparse_met()} with
\code{allocation_method = "random_balanced"}. All list names correspond
directly to function parameter names.}
\item{\code{sparse_example_args_balanced_incomplete}}{Named list of arguments
ready to pass to \code{allocate_sparse_met()} with
\code{allocation_method = "balanced_incomplete"}. All list names correspond
directly to function parameter names.}
\item{\code{env_design_specs}}{Named list with one element per environment in
\code{environments}. Each element is itself a named list of arguments
specifying the local field design for that environment, suitable for
passing to \code{prep_famoptg()} or \code{alpha_rc_stream()}.}
}
}
\source{
Synthetically generated for package documentation and testing.
}
\usage{
OptiSparseMET_example_data
}
\description{
Synthetic dataset for demonstrating and testing sparse multi-environment
trial allocation and within-environment field design construction across the
functions of \code{OptiSparseMET}. All components are internally consistent:
treatment IDs, family labels, seed inventories, relationship matrices, and
environment specifications refer to the same set of synthetic lines and
environments, so any combination of components can be passed to the package
functions without modification.
}
\details{
All data are synthetic and generated solely for use in package examples,
unit tests, and vignettes. No real breeding trial data are included.
Relationship matrices (\code{OptiSparseMET_GRM}, \code{OptiSparseMET_A},
\code{OptiSparseMET_K}) are positive semi-definite by construction and have
row and column names consistent with \code{treatments}, so they can be used
directly without an \code{id_map}. The \code{seed_info} and \code{seed_required_per_plot}
components are calibrated so that a range of replication outcomes (fully
replicated, partially replicated, and excluded treatments) arise naturally
when passed to \code{assign_replication_by_seed()}, making the data useful for
demonstrating all three replication modes.
}
\keyword{datasets}
