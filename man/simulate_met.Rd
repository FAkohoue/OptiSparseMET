% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_met.R
\name{simulate_met}
\alias{simulate_met}
\title{Simulate a sparse MET and report realized accuracy and genetic gain}
\usage{
simulate_met(
  allocation_matrix,
  G,
  Sigma_E = NULL,
  sigma_g2 = 1,
  sigma_e2 = 1,
  reps = NULL,
  env_efficiency = NULL,
  n_sim = 50L,
  select_fraction = 0.1,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{allocation_matrix}{A \eqn{J \times E} 0/1 incidence matrix (lines as
rows, environments as columns) with row and column names. Typically
\code{alloc$allocation_matrix} from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{G}{Genomic (or pedigree) relationship matrix with row/column names
covering the lines in \code{allocation_matrix}.}

\item{Sigma_E}{Optional \eqn{E \times E} environment covariance (genetic
correlations across environments). Defaults to the identity (independent
environments, unit variance).}

\item{sigma_g2, sigma_e2}{Genetic and residual variance scalars.}

\item{reps}{Optional \eqn{J \times E} matrix of plot counts per cell.
Defaults to \code{allocation_matrix} (one plot per present cell).}

\item{env_efficiency}{Optional length-\eqn{E} vector of within-environment
design efficiency factors \eqn{\epsilon_e \in (0,1]} (the coupling to the
local design). Defaults to 1 for every environment.}

\item{n_sim}{Number of independent simulation replicates.}

\item{select_fraction}{Top fraction selected (on predicted BV) for the
genetic-gain statistic.}

\item{seed}{Optional RNG seed.}

\item{max_dim}{Integer guard on \eqn{J \times E}; above it the dense inverse
is refused with an informative error.}
}
\value{
A list with \code{accuracy_mean}, \code{accuracy_sd}, \code{gain_mean}, \code{gain_sd},
\code{n_sim}, and the design-based \code{mean_PEV} / \code{CDmean} from \code{\link[=met_information]{met_information()}}.
}
\description{
\code{simulate_met()} closes the loop between design and outcome. It simulates
genotype-by-environment effects with a genuine across-environment genetic
correlation structure (\eqn{u \sim \mathrm{MVN}(0, \sigma_g^2\,\Sigma_E
\otimes G)}), generates plot means for the cells an allocation actually
tests, predicts breeding values by GBLUP through the coupled MET information
matrix (see \code{\link[=met_information]{met_information()}}), and reports the realized \strong{selection
accuracy} (correlation between predicted and true across-TPE breeding
values) and \strong{genetic gain} (mean true merit of the top fraction selected on
prediction). This is the quantity breeders care about, and it lets a design's
A / D / CDmean proxies be checked against the outcome they are meant to
improve.

Unlike a no-G×E simulation, the correlation structure in \code{Sigma_E} means
cross-environment connectivity genuinely affects accuracy, so balanced,
sparse, and near-balanced allocations can be compared fairly.
}
\seealso{
\code{\link[=met_information]{met_information()}}, \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.
}
