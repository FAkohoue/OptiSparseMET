% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_famoptg.R
\name{prep_famoptg}
\alias{prep_famoptg}
\title{Construct a repeated-check block design with flexible replication structure}
\usage{
prep_famoptg(
  check_treatments,
  check_families,
  p_rep_treatments,
  p_rep_reps,
  p_rep_families,
  unreplicated_treatments,
  unreplicated_families,
  n_blocks,
  n_rows,
  n_cols,
  order = "column",
  serpentine = FALSE,
  seed = NULL,
  attempts = 1000,
  warn_and_correct = TRUE,
  fix_rows = TRUE,
  cluster_source = c("Family", "GRM", "A"),
  GRM = NULL,
  A = NULL,
  id_map = NULL,
  cluster_method = c("kmeans", "hclust"),
  cluster_seed = 1,
  cluster_attempts = 25,
  n_pcs_use = Inf,
  eval_efficiency = FALSE,
  treatment_effect = c("random", "fixed"),
  prediction_type = c("none", "IID", "GBLUP", "PBLUP"),
  K = NULL,
  line_id_map = NULL,
  varcomp = list(sigma_e2 = 1, sigma_g2 = 1, sigma_b2 = 1, sigma_r2 = 1, sigma_c2 = 1),
  check_as_fixed = TRUE,
  residual_structure = c("IID", "AR1", "AR1xAR1"),
  rho_row = 0,
  rho_col = 0,
  spatial_engine = c("auto", "sparse", "dense"),
  dense_max_n = 5000,
  eff_trace_samples = 80,
  eff_full_max = 400,
  check_placement = c("random", "systematic", "optimal"),
  check_opt_attempts = 200,
  use_dispersion = FALSE,
  dispersion_source = c("K", "A", "GRM"),
  dispersion_radius = 1,
  dispersion_iters = 2000,
  dispersion_seed = 1
)
}
\arguments{
\item{check_treatments}{Character vector of check treatment IDs. Checks
appear exactly once in every block and define the repeated reference
structure of the design. Required for all design classes. Must not overlap
with \code{p_rep_treatments} or \code{unreplicated_treatments}.}

\item{check_families}{Character vector of the same length as
\code{check_treatments}. Family or group labels for check treatments. Required
regardless of \code{cluster_source} because checks always carry a group label
in the output field book. Must align element-wise with \code{check_treatments}.}

\item{p_rep_treatments}{Character vector of replicated non-check treatment
IDs. Each treatment in this vector appears \code{p_rep_reps[i]} times in the
full design, distributed across that many distinct blocks. Supply
\code{character(0)} or \code{NULL} for an augmented design with no replicated
non-check entries. Must align element-wise with \code{p_rep_reps} and
\code{p_rep_families}.}

\item{p_rep_reps}{Integer vector of replication counts, one per element of
\code{p_rep_treatments}. Each value must satisfy \code{p_rep_reps[i] <= n_blocks}.
A treatment with replication \eqn{r} is placed in \eqn{r} distinct blocks.
Supply \code{integer(0)} when \code{p_rep_treatments} is empty.}

\item{p_rep_families}{Character vector of the same length as
\code{p_rep_treatments}. Family or group labels for replicated non-check
treatments. Used directly when \code{cluster_source = "Family"}. When
\code{cluster_source \%in\% c("GRM", "A")}, still used to anchor the number of
clusters to the number of distinct families among non-check treatments.
Supply \code{character(0)} when \code{p_rep_treatments} is empty.}

\item{unreplicated_treatments}{Character vector of non-check treatments
assigned exactly one plot each. Supply \code{character(0)} or \code{NULL} when all
non-check treatments are replicated. In combination with empty
\code{p_rep_treatments} and repeated checks, these entries define an augmented
repeated-check design. Must align element-wise with
\code{unreplicated_families}.}

\item{unreplicated_families}{Character vector of the same length as
\code{unreplicated_treatments}. Family or group labels for unreplicated
treatments. Required whenever \code{unreplicated_treatments} is non-empty.
Supply \code{character(0)} when \code{unreplicated_treatments} is empty.}

\item{n_blocks}{Positive integer. Number of experimental blocks. Determines
how many times checks are repeated and sets the upper bound on replication
for any non-check treatment: \code{p_rep_reps[i] <= n_blocks}. When a
replicated treatment should appear in every block, its replication must
equal \code{n_blocks}.}

\item{n_rows}{Positive integer. Number of rows in the field grid. Together
with \code{n_cols}, determines total field capacity and the 2D coordinates in
\code{layout_matrix} and \code{field_book}. When \code{warn_and_correct = TRUE} and
\code{fix_rows = TRUE}, \code{n_rows} is held fixed and \code{n_cols} is adjusted.}

\item{n_cols}{Positive integer. Number of columns in the field grid. When
\code{warn_and_correct = TRUE} and \code{fix_rows = FALSE}, \code{n_cols} is held fixed
and \code{n_rows} is adjusted.}

\item{order}{Character scalar. Direction in which the field grid is
traversed when mapping the ordered treatment sequence to spatial
coordinates. \code{"row"} fills row by row; \code{"column"} fills column by column.
Should reflect the physical planting or harvesting direction. Interacts
with \code{serpentine}.}

\item{serpentine}{Logical. If \code{TRUE}, alternate rows (when \code{order = "row"})
or alternate columns (when \code{order = "column"}) reverse traversal direction,
producing a boustrophedon mapping from treatment sequence to field
coordinates. Does not affect block composition; affects only the spatial
positions assigned to each treatment.}

\item{seed}{Optional integer. Random seed controlling block assignment of
replicated treatments, within-block shuffling, check placement candidates,
and dispersion optimization when \code{dispersion_seed} is \code{NULL}. If \code{NULL},
a seed is generated internally and returned as \code{seed_used}.}

\item{attempts}{Positive integer. Maximum number of shuffle proposals per
block used to reduce same-group adjacency in the 1D block ordering.
Larger values increase search effort without changing the algorithm. Most
relevant when group structure is highly imbalanced within blocks or when
many similar entries occur together.}

\item{warn_and_correct}{Logical. If \code{FALSE}, the function stops when
\code{n_rows * n_cols} does not equal the total required plot count. If \code{TRUE},
one dimension is expanded upward to accommodate all plots, with surplus
cells left as \code{NA} in \code{layout_matrix}. Which dimension is adjusted is
controlled by \code{fix_rows}.}

\item{fix_rows}{Logical. Active only when \code{warn_and_correct = TRUE}. If
\code{TRUE}, \code{n_rows} is held fixed and \code{n_cols} is adjusted. If \code{FALSE},
\code{n_cols} is held fixed and \code{n_rows} is adjusted. Use \code{TRUE} when the
number of field rows is physically constrained.}

\item{cluster_source}{Character scalar. Source of group labels for
within-block adjacency control. \code{"Family"} uses the supplied family
vectors. \code{"GRM"} derives cluster labels from the eigenstructure of \code{GRM}.
\code{"A"} derives cluster labels from the eigenstructure of \code{A}. Determines
which of \code{GRM}, \code{A}, \code{id_map}, \code{cluster_method}, \code{cluster_seed},
\code{cluster_attempts}, and \code{n_pcs_use} become active.}

\item{GRM}{Optional numeric matrix. Genomic relationship matrix. Required
when \code{cluster_source = "GRM"} or when \code{use_dispersion = TRUE} and
\code{dispersion_source = "GRM"}. Must be square with row and column names
matching treatment IDs or reachable through \code{id_map}.}

\item{A}{Optional numeric matrix. Pedigree-based numerator relationship
matrix. Required when \code{cluster_source = "A"} or when
\code{use_dispersion = TRUE} and \code{dispersion_source = "A"}. Same naming
requirements as \code{GRM}.}

\item{id_map}{Optional data frame with columns \code{Treatment} and \code{LineID}.
Required only when \code{cluster_source \%in\% c("GRM", "A")} and treatment IDs
in the design do not match the row names of the selected relationship
matrix.}

\item{cluster_method}{Character scalar. Clustering algorithm applied to PCA
scores in matrix-based grouping. \code{"kmeans"} uses k-means with
\code{cluster_attempts} random restarts seeded by \code{cluster_seed}. \code{"hclust"}
uses Ward's criterion hierarchical clustering. Ignored when
\code{cluster_source = "Family"}.}

\item{cluster_seed}{Integer. Seed for k-means initialization. Active only
when \code{cluster_source \%in\% c("GRM", "A")} and \code{cluster_method = "kmeans"}.}

\item{cluster_attempts}{Integer. Number of random restarts for k-means.
Active only when \code{cluster_source \%in\% c("GRM", "A")} and
\code{cluster_method = "kmeans"}. Larger values reduce the risk of poor local
optima.}

\item{n_pcs_use}{Integer or \code{Inf}. Number of leading principal components
retained for PCA-based clustering. \code{Inf} retains all components
corresponding to positive eigenvalues. Smaller values preserve only broad
genetic structure. Must be at least 2. Ignored when
\code{cluster_source = "Family"}.}

\item{eval_efficiency}{Logical. If \code{TRUE}, mixed-model efficiency metrics
are computed on the final design. If \code{FALSE}, all efficiency arguments are
ignored and \code{efficiency} is returned as \code{NULL}.}

\item{treatment_effect}{Character scalar. Specifies whether non-check
treatments enter the efficiency model as fixed or random effects. \code{"fixed"}
computes precision of pairwise treatment contrasts. \code{"random"} computes
average prediction error variance. Active only when
\code{eval_efficiency = TRUE}.}

\item{prediction_type}{Character scalar. Random-effect model for efficiency
evaluation. \code{"none"} skips. \code{"IID"} assumes independent entry effects.
\code{"GBLUP"} and \code{"PBLUP"} use the relationship matrix \code{K}. Active only when
\code{eval_efficiency = TRUE} and \code{treatment_effect = "random"}.}

\item{K}{Optional numeric matrix. Relationship matrix serving two purposes:
(1) random-effect efficiency when \code{prediction_type \%in\% c("GBLUP", "PBLUP")}; (2) dispersion optimization when \code{use_dispersion = TRUE} and
\code{dispersion_source = "K"}. Ignored otherwise. Row and column names must
match treatment IDs or be reachable through \code{line_id_map}.}

\item{line_id_map}{Optional data frame with columns \code{Treatment} and
\code{LineID}. Required only when \code{K} is active and treatment labels differ
from \code{rownames(K)}. Serves the same role for \code{K} that \code{id_map} serves for
\code{GRM} and \code{A}.}

\item{varcomp}{Named list of variance components for efficiency evaluation.
Must contain \code{sigma_e2}, \code{sigma_g2}, \code{sigma_b2}, \code{sigma_r2}, and
\code{sigma_c2}. All values except \code{sigma_g2} must be strictly positive.
\code{sigma_g2} must be positive when \code{treatment_effect = "random"}. Active
only when \code{eval_efficiency = TRUE}. The ratio of \code{sigma_b2} to \code{sigma_e2}
has the largest effect on efficiency metrics for block designs.}

\item{check_as_fixed}{Logical. If \code{TRUE}, check treatments are included as
fixed indicator columns in the mixed-model coefficient matrix during
efficiency evaluation. Active only when \code{eval_efficiency = TRUE}.}

\item{residual_structure}{Character scalar. Residual covariance structure
for efficiency evaluation. \code{"IID"} assumes independent residuals. \code{"AR1"}
introduces row-wise autocorrelation controlled by \code{rho_row}. \code{"AR1xAR1"}
introduces both row-wise and column-wise autocorrelation controlled by
\code{rho_row} and \code{rho_col}. Active only when \code{eval_efficiency = TRUE}.}

\item{rho_row}{Numeric. AR1 autocorrelation parameter in the row direction.
Must satisfy \eqn{|\rho_{\text{row}}| < 1}. Active when
\code{eval_efficiency = TRUE} and \code{residual_structure \%in\% c("AR1", "AR1xAR1")}.}

\item{rho_col}{Numeric. AR1 autocorrelation parameter in the column
direction. Must satisfy \eqn{|\rho_{\text{col}}| < 1}. Active when
\code{eval_efficiency = TRUE} and \code{residual_structure = "AR1xAR1"}.}

\item{spatial_engine}{Character scalar. Computational strategy for the
residual precision matrix when \code{residual_structure != "IID"}. \code{"auto"}
selects \code{"dense"} when the number of observed plots is at most
\code{dense_max_n} and \code{"sparse"} otherwise. \code{"dense"} and \code{"sparse"} force
the respective implementation. Active only when \code{eval_efficiency = TRUE}
and \code{residual_structure != "IID"}.}

\item{dense_max_n}{Integer. Plot count threshold used by \code{spatial_engine = "auto"} to switch between dense and sparse computation. Active only when
\code{eval_efficiency = TRUE}, \code{residual_structure != "IID"}, and
\code{spatial_engine = "auto"}.}

\item{eff_trace_samples}{Integer. Number of Hutchinson trace samples used
when the target treatment dimension exceeds \code{eff_full_max} and exact
efficiency extraction is replaced by stochastic approximation. Larger
values reduce approximation variance at the cost of runtime. Active only
when \code{eval_efficiency = TRUE}.}

\item{eff_full_max}{Integer. Maximum treatment dimension for which exact
efficiency extraction via linear solve is attempted. Above this threshold
the function switches to stochastic trace estimation. Active only when
\code{eval_efficiency = TRUE}.}

\item{check_placement}{Character scalar. Strategy for positioning checks
within blocks. \code{"random"} draws check positions freely within each block.
\code{"systematic"} spaces checks approximately evenly in the 1D block
ordering. \code{"optimal"} evaluates \code{check_opt_attempts} candidate layouts
and selects the one maximizing the mean minimum Euclidean distance between
check positions in the 2D grid.}

\item{check_opt_attempts}{Positive integer. Number of candidate layouts
evaluated when \code{check_placement = "optimal"}. Larger values increase the
chance of finding a spatially well-dispersed check arrangement at higher
runtime cost. Ignored unless \code{check_placement = "optimal"}.}

\item{use_dispersion}{Logical. If \code{TRUE}, a swap-based local search is
applied after layout construction to reduce spatial concentration of
genetically similar non-check entries. The pairwise relatedness matrix
is selected by \code{dispersion_source}. If \code{FALSE}, all dispersion arguments
are ignored.}

\item{dispersion_source}{Character scalar. Relationship matrix used for
pairwise relatedness scoring in dispersion optimization. \code{"K"} uses \code{K}
(with optional \code{line_id_map}), \code{"A"} uses \code{A}, \code{"GRM"} uses \code{GRM}.
Active only when \code{use_dispersion = TRUE}.}

\item{dispersion_radius}{Positive integer. Chebyshev neighborhood radius
for dispersion scoring. Two plots are neighbors when
\eqn{\max(|\Delta r|, |\Delta c|) \leq \texttt{dispersion\_radius}}.
\code{1} considers immediate neighbors only; larger values extend the local
window over which relatedness is penalized. Active only when
\code{use_dispersion = TRUE}.}

\item{dispersion_iters}{Non-negative integer. Number of swap proposals in
the dispersion search. Larger values allow more extensive search at higher
runtime cost. Active only when \code{use_dispersion = TRUE}.}

\item{dispersion_seed}{Optional integer. Seed for the dispersion swap
search, applied independently of \code{seed}. When \code{NULL}, \code{seed_used} is
applied. Use when the initial layout and the dispersion refinement should
be reproducible independently.}
}
\value{
A named list with the following components:
\describe{
\item{\code{layout_matrix}}{Character matrix of dimension \verb{n_rows x n_cols}.
Each cell contains a treatment ID or \code{NA} for surplus unused positions.}
\item{\code{field_book}}{Data frame with one row per assigned plot. Columns:
\code{Treatment}, \code{Family}, \code{Gcluster}, \code{Block}, \code{Plot}, \code{Row}, \code{Column}.
\code{Gcluster} is \code{NA} for check treatments and for all treatments when
\code{cluster_source = "Family"}.}
\item{\code{efficiency}}{\code{NULL} if \code{eval_efficiency = FALSE}; otherwise a
named list of efficiency metrics including \code{mode}, \code{A}, \code{D},
\code{mean_PEV} or \code{mean_VarDiff}, \code{n_trt} or \code{n_lines}, \code{residual_structure_used},
\code{spatial_engine_used}, and \code{notes}.}
\item{\code{seed_used}}{Integer. The random seed used internally, returned for
reproducibility records.}
}
}
\description{
The allocation rules that govern all three design classes are:
\itemize{
\item Check treatments appear exactly once in every block.
\item Replicated non-check treatments appear at most once per block; their
replicates are distributed across distinct blocks.
\item Unreplicated treatments appear exactly once in the full design.
}

A treatment with replication \eqn{r} and \eqn{b \geq r} blocks therefore
occupies \eqn{r} distinct blocks. When \eqn{r = b}, the treatment appears in
every block, which is the RCBD analogue within this framework.

Within each block, the 1D ordering of treatments is shuffled to reduce
adjacency between entries from the same genetic group, using up to \code{attempts}
shuffle proposals per block. This adjacency control operates on group labels
derived from \code{cluster_source}. Optional dispersion optimization applies a
swap-based local search after the full layout is placed, using pairwise
relatedness from a relationship matrix to penalize spatial proximity of
similar entries in the 2D field grid.
}
\details{
\code{prep_famoptg()} builds a block field design in which check treatments
appear in every block and non-check treatments are allocated across blocks
according to specified replication levels. The same function produces three
design classes depending on the supplied treatment structure: an augmented
repeated-check design when all non-check treatments are unreplicated, a
partially replicated (p-rep) design when some non-check treatments are
replicated and others are not, and an RCBD-type repeated-check design when
all non-check treatments receive a common replication greater than 1.
Optionally, the function applies post-layout dispersion optimization to
reduce local relatedness among non-check entries, and evaluates the
resulting design under a mixed-model framework.

Serpentine traversal uses \code{mod()} from the \strong{pracma} package. When the
adjusted field size exceeds the plot count, surplus cells appear as \code{NA} in
\code{layout_matrix}; the \code{field_book} contains only assigned plots.

The function separates construction logic, grouping logic, dispersion logic,
and efficiency logic. This means that, for example, family labels can drive
adjacency control while a genomic matrix drives dispersion optimization, and
efficiency evaluation can use a third, dedicated relationship matrix \code{K}.
None of these layers need to use the same source.
}
\section{Design classes}{


\strong{Augmented repeated-check design}

Obtained when \code{p_rep_treatments} is empty or \code{NULL} and all non-check
entries are supplied through \code{unreplicated_treatments}. Checks are repeated
in every block; all test entries appear once. Appropriate for early-stage
screening when the number of candidates exceeds what can be replicated.

\strong{Partially replicated (p-rep) repeated-check design}

Obtained when some non-check treatments are in \code{p_rep_treatments} with
replication greater than 1 and others remain in \code{unreplicated_treatments}.
Checks are repeated in every block; selected candidates are replicated while
others receive a single plot.

\strong{RCBD-type repeated-check design}

Obtained when all non-check treatments are in \code{p_rep_treatments} with a
common replication greater than 1 and \code{unreplicated_treatments} is empty. If
the common replication equals \code{n_blocks}, every non-check treatment appears
once in every block alongside the checks, producing the closest analogue to
a classical RCBD within this repeated-check framework. If the common
replication is less than \code{n_blocks}, balance is maintained but the design is
no longer a strict classical RCBD.
}

\section{Construction sequence}{

\enumerate{
\item Inputs are validated and field dimensions are reconciled with the total
plot count required.
\item Group labels are derived from family labels or from PCA-based clustering
of \code{GRM} or \code{A}.
\item Replicated non-check entries are assigned to blocks, subject to the
constraint that no treatment appears twice in the same block.
\item Unreplicated entries are distributed across blocks.
\item Checks are inserted into each block using the chosen placement strategy.
\item Within each block, treatment order is shuffled to reduce same-group
adjacency in the 1D block sequence.
\item The ordered treatment sequence is mapped to the field grid according to
\code{order} and \code{serpentine}.
\item Optionally, a swap-based dispersion search reduces local relatedness in
the 2D layout.
\item Optionally, mixed-model efficiency metrics are computed on the final
design.
}
}

\section{Grouping and why it matters}{


Group labels serve two purposes. During block construction, they define which
entries are considered similar for the purpose of adjacency control in the 1D
block ordering. During optional dispersion optimization, a relationship matrix
is used to score pairwise relatedness between neighboring plots in the 2D
grid and a swap search reduces that score.

Three grouping modes are available. \code{"Family"} reads labels directly from the
supplied family vectors. \code{"GRM"} derives cluster labels from the eigenstructure
of the genomic relationship matrix via PCA followed by k-means or hierarchical
clustering. \code{"A"} applies the same procedure to the pedigree relationship
matrix. Matrix-based grouping is preferable when family labels are too coarse
to capture the relevant genetic structure, or when dispersing genomically
similar materials more precisely is a priority.
}

\section{Argument dependencies}{


\strong{Grouping mode}

When \code{cluster_source = "Family"}, labels come from \code{check_families},
\code{p_rep_families}, and \code{unreplicated_families}; \code{GRM}, \code{A}, \code{id_map},
\code{cluster_method}, \code{cluster_seed}, \code{cluster_attempts}, and \code{n_pcs_use} are
ignored. When \code{cluster_source = "GRM"}, \code{GRM} is required; \code{id_map} is
needed only if treatment IDs differ from \code{rownames(GRM)}. When
\code{cluster_source = "A"}, the same applies with \code{A}. In both matrix-based
modes, \code{cluster_method}, \code{cluster_seed}, \code{cluster_attempts}, and \code{n_pcs_use}
are active.

\strong{Efficiency evaluation}

When \code{eval_efficiency = FALSE}, all efficiency arguments are ignored. When
\code{eval_efficiency = TRUE} and \code{treatment_effect = "fixed"}, precision of
fixed-effect treatment contrasts is computed and \code{prediction_type}, \code{K}, and
\code{line_id_map} are ignored. When \code{treatment_effect = "random"},
\code{prediction_type} becomes active: \code{"none"} skips random-effect efficiency;
\code{"IID"} treats entry effects as independent; \code{"GBLUP"} and \code{"PBLUP"} require
\code{K}, with \code{line_id_map} needed when treatment IDs differ from \code{rownames(K)}.

\strong{Residual structure}

\code{"IID"} ignores \code{rho_row} and \code{rho_col}. \code{"AR1"} uses \code{rho_row} only.
\code{"AR1xAR1"} uses both. All residual arguments are inactive when
\code{eval_efficiency = FALSE}.

\strong{Check placement}

\code{"optimal"} activates \code{check_opt_attempts} and evaluates multiple candidate
layouts, selecting the one that maximizes the mean minimum distance between
check positions in the 2D grid. \code{"random"} and \code{"systematic"} ignore
\code{check_opt_attempts}.

\strong{Dispersion optimization}

When \code{use_dispersion = FALSE}, all dispersion arguments are ignored. When
\code{use_dispersion = TRUE}, \code{dispersion_source} selects the matrix used for
pairwise relatedness scoring: \code{"K"} requires \code{K} (with optional
\code{line_id_map}), \code{"A"} requires \code{A}, \code{"GRM"} requires \code{GRM}.

\strong{Field dimension correction}

When \code{warn_and_correct = FALSE}, the function stops if \code{n_rows * n_cols}
does not match the total required plot count. When \code{warn_and_correct = TRUE},
one dimension is adjusted upward to accommodate all plots; \code{fix_rows}
determines which dimension is held fixed.
}

\examples{
data("OptiSparseMET_example_data", package = "OptiSparseMET")
x <- OptiSparseMET_example_data

env_trt <- x$treatments[1:24]
env_fam <- x$treatment_info$Family[match(env_trt, x$treatment_info$Treatment)]

## Example 1: p-rep repeated-check design — first 8 entries replicated
## twice, remaining 16 unreplicated. Checks in every block.
out_prep <- prep_famoptg(
  check_treatments         = c("CHK1", "CHK2"),
  check_families           = c("CHECK", "CHECK"),
  p_rep_treatments         = env_trt[1:8],
  p_rep_reps               = rep(2L, 8),
  p_rep_families           = env_fam[1:8],
  unreplicated_treatments  = env_trt[9:24],
  unreplicated_families    = env_fam[9:24],
  n_blocks                 = 4,
  n_rows                   = 6,
  n_cols                   = 8,
  order                    = "row",
  serpentine               = TRUE,
  seed                     = 123,
  cluster_source           = "Family",
  eval_efficiency          = FALSE,
  use_dispersion           = FALSE
)

dim(out_prep$layout_matrix)   # 6 x 8
head(out_prep$field_book)
out_prep$seed_used

\dontrun{
## Example 2: augmented repeated-check design — no replicated entries,
## all 24 test entries unreplicated.
out_aug <- prep_famoptg(
  check_treatments         = c("CHK1", "CHK2"),
  check_families           = c("CHECK", "CHECK"),
  p_rep_treatments         = character(0),
  p_rep_reps               = integer(0),
  p_rep_families           = character(0),
  unreplicated_treatments  = env_trt,
  unreplicated_families    = env_fam,
  n_blocks                 = 4,
  n_rows                   = 5,
  n_cols                   = 6,
  order                    = "row",
  serpentine               = FALSE,
  seed                     = 123,
  cluster_source           = "Family",
  eval_efficiency          = FALSE,
  use_dispersion           = FALSE
)

dim(out_aug$layout_matrix)
head(out_aug$field_book)

## Example 3: RCBD-type repeated-check design — all 24 entries replicated
## 4 times (once per block). Equivalent to a classical RCBD with repeated
## checks.
out_rcbd <- prep_famoptg(
  check_treatments         = c("CHK1", "CHK2"),
  check_families           = c("CHECK", "CHECK"),
  p_rep_treatments         = env_trt,
  p_rep_reps               = rep(4L, length(env_trt)),
  p_rep_families           = env_fam,
  unreplicated_treatments  = character(0),
  unreplicated_families    = character(0),
  n_blocks                 = 4,
  n_rows                   = 14,
  n_cols                   = 8,
  order                    = "row",
  serpentine               = TRUE,
  seed                     = 123,
  cluster_source           = "Family",
  eval_efficiency          = FALSE,
  use_dispersion           = FALSE
)

dim(out_rcbd$layout_matrix)
head(out_rcbd$field_book)
}

}
