% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_selection.R
\name{list_candidate_interactions}
\alias{list_candidate_interactions}
\title{Enumerate candidate interaction terms among covariates}
\usage{
list_candidate_interactions(x, order = 2L, exclude = character(0))
}
\arguments{
\item{x}{A data frame of covariates (column classes are used), or a named list
giving, per column, either \code{"numeric"} or the character vector of factor
levels.}

\item{order}{Maximum interaction order (2 = pairwise, 3 = three-way, ...).
Default 2.}

\item{exclude}{Column names to exclude (e.g. an \code{environment} id, or doses
already handled by \code{\link[=nest_dose_within_type]{nest_dose_within_type()}}). \code{"environment"} is always
excluded.}
}
\value{
A data frame with one row per candidate: \code{term} (colon-joined
variables), \code{order}, \code{type} (\code{"numeric"}, \code{"categorical"}, or \code{"mixed"}),
and \code{est_columns} (columns it would generate), sorted cheapest first.
}
\description{
Lists every possible interaction among management/environmental covariate
columns so a breeder can inspect the options before defining a short,
pre-specified set with \code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}} or adding
explicit covariate columns with \code{\link[=add_management_interactions]{add_management_interactions()}}. Each candidate is
labelled by type (categorical, numeric, or mixed) and by the number of columns
it would generate -- the dimensionality cost -- so cheap, low-risk terms are
obvious and the explosion of high-order terms is visible. Enumeration is not a
recommendation: with few environments, most interactions add noise, so pair
this with agronomic judgement (\code{\link[=agronomic_interaction_hypotheses]{agronomic_interaction_hypotheses()}}) or the
data-driven \code{\link[=screen_interactions]{screen_interactions()}}. Exhaustive enumeration is not a
recommendation to construct every listed kernel.
}
\examples{
df <- data.frame(temp = 1:3, rain = 4:6,
                 planting = c("a", "b", "a"), irrigation = c("x", "y", "x"))
list_candidate_interactions(df)
}
\seealso{
\code{\link[=build_variable_interaction_kernels]{build_variable_interaction_kernels()}},
\code{\link[=add_management_interactions]{add_management_interactions()}}, \code{\link[=screen_interactions]{screen_interactions()}},
\code{\link[=agronomic_interaction_hypotheses]{agronomic_interaction_hypotheses()}}.
}
