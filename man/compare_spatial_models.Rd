% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_spatial_models.R
\name{compare_spatial_models}
\alias{compare_spatial_models}
\title{Score one design under several spatial models}
\usage{
compare_spatial_models(
  field_book,
  n_rows,
  n_cols,
  check_treatments,
  models = c("IID", "AR1", "AR1xAR1", "AR1xAR1_nugget", "exponential", "pspline"),
  evaluator = c("alpha", "famoptg"),
  rho_row = 0.3,
  rho_col = 0.3,
  nugget = 0.2,
  kernel_range = 3,
  matern_nu = 1.5,
  spline_lambda_row = 1,
  spline_lambda_col = 1,
  ...
)
}
\arguments{
\item{field_book}{Field book to evaluate, as passed to the evaluator.}

\item{n_rows, n_cols}{Field dimensions.}

\item{check_treatments}{Character vector of check identifiers.}

\item{models}{Character vector of models to evaluate (see Details).}

\item{evaluator}{Which engine to use: \code{"alpha"} for
\code{\link[=met_evaluate_alpha_efficiency]{met_evaluate_alpha_efficiency()}} or \code{"famoptg"} for
\code{\link[=met_evaluate_famoptg_efficiency]{met_evaluate_famoptg_efficiency()}}.}

\item{rho_row, rho_col}{AR1 parameters used by the AR1-type models.}

\item{nugget}{Nugget proportion used by \code{"AR1xAR1_nugget"} and the kernels.}

\item{kernel_range, matern_nu}{Parameters for the distance kernels.}

\item{spline_lambda_row, spline_lambda_col}{Smoothing parameters for
\code{"pspline"}.}

\item{...}{Further arguments passed to the evaluator (for example
\code{varcomp}, \code{treatment_effect}, \code{prediction_type}, \code{K}).}
}
\value{
A data frame with one row per model and columns \code{model},
\code{A_criterion}, \code{D_criterion}, \code{CDmean} and \code{mean_PEV} (\code{NA} where a
criterion does not apply), plus \code{error} for any model that failed.
}
\description{
A design that is best under one spatial assumption is not guaranteed to be
best under another, and at design time the spatial structure is rarely known.
\code{compare_spatial_models()} evaluates a single field layout under a set of
spatial models and returns the criteria side by side, so a design can be
checked for robustness rather than tuned to one assumption. It is the spatial
analogue of \code{\link[=sensitivity_varcomp]{sensitivity_varcomp()}}.
}
\details{
Available models:
\describe{
\item{\code{"IID"}}{Independent residuals -- no spatial structure.}
\item{\code{"AR1"}, \code{"AR1xAR1"}}{Separable autoregressive residual correlation
along rows, or along rows and columns.}
\item{\code{"AR1xAR1_nugget"}}{Separable AR1xAR1 plus an independent
measurement-error (nugget) component -- the standard applied model, since
pure AR1xAR1 often fits field data poorly.}
\item{\code{"exponential"}, \code{"gaussian"}, \code{"matern"}}{Isotropic correlation as a
function of Euclidean plot distance, useful for irregular layouts.}
\item{\code{"pspline"}}{A SpATS-style two-dimensional penalised-spline surface
fitted as a random effect with IID residuals (Rodriguez-Alvarez et al.
2018), rather than as a residual covariance.}
}

Because the criteria are conditional on the assumed parameters, compare
designs on the same settings. A design that wins across every model is a safe
choice; one that wins under a single model is a warning.
}
\references{
Rodriguez-Alvarez, M. X., Boer, M. P., van Eeuwijk, F. A., & Eilers, P. H. C.
(2018). Correcting for spatial heterogeneity in plant breeding experiments
with P-splines. \emph{Spatial Statistics}, 23, 52-71.
}
\seealso{
\code{\link[=sensitivity_varcomp]{sensitivity_varcomp()}}, \code{\link[=met_evaluate_alpha_efficiency]{met_evaluate_alpha_efficiency()}}.
}
