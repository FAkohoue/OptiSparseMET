% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adaptive_met_allocation.R
\name{adaptive_met_allocation}
\alias{adaptive_met_allocation}
\title{Sequentially add the most informative MET observations}
\usage{
adaptive_met_allocation(
  observed_allocation,
  G,
  Sigma_E = NULL,
  target_environment_sizes,
  batch_size = NULL,
  allocation_criterion = c("mean_pev", "cdmean", "expected_gain"),
  robust = NULL,
  robust_aggregate = c("mean", "cvar", "min"),
  cvar_alpha = 0.25,
  candidate_mask = NULL,
  required_common_treatments = NULL,
  minimum_treatment_environments = 0L,
  sigma_g2 = 1,
  sigma_e2 = 1,
  tpe_weights = NULL,
  env_efficiency = NULL,
  cost_per_plot = 1,
  seed_available = NULL,
  seed_required_per_environment = NULL,
  minimum_seed_buffer = 0,
  seed = NULL,
  max_dim = 6000L
)
}
\arguments{
\item{observed_allocation}{Named genotype-by-environment 0/1 matrix describing
observations already collected or irrevocably committed.}

\item{G, Sigma_E}{Relationship matrices used by \code{\link[=design_objective]{design_objective()}}.}

\item{target_environment_sizes}{Scalar or named vector giving the maximum
final number of allocated treatments in each environment.}

\item{batch_size}{Number of new cells to recommend. \code{NULL} fills every
environment to its target size.}

\item{allocation_criterion}{\code{"mean_pev"}, \code{"cdmean"}, or
\code{"expected_gain"}.}

\item{robust}{Optional scenario list from \code{\link[=robust_scenarios]{robust_scenarios()}}.}

\item{robust_aggregate, cvar_alpha}{Robust aggregation controls.}

\item{candidate_mask}{Optional named 0/1 matrix identifying cells that may be
added. Already observed cells need not be marked.}

\item{required_common_treatments}{Treatments that must be present in every
environment before discretionary additions are selected.}

\item{minimum_treatment_environments}{Minimum final environment coverage per
treatment. Coverage deficits are filled before discretionary additions.}

\item{sigma_g2, sigma_e2, tpe_weights, env_efficiency}{Statistical assumptions.}

\item{cost_per_plot}{Optional scalar or environment-specific plot cost.}

\item{seed_available}{Optional named treatment-level seed inventory.}

\item{seed_required_per_environment}{Optional scalar or environment-specific
seed cost for one new observation; required with \code{seed_available}.}

\item{minimum_seed_buffer}{Seed reserve that cannot be spent.}

\item{seed, max_dim}{Reproducibility and information-matrix size control.}
}
\value{
A list with the updated \code{allocation_matrix}, ordered
\code{recommendations}, criterion scores before and after, an audit \code{trajectory},
seed use, and a validated \code{sparse_met_design} object.
}
\description{
Builds or updates a sparse multi-environment allocation one
genotype-by-environment cell at a time. At each step it evaluates every
feasible unobserved cell and adds the one with the largest marginal gain in
the requested prediction criterion. This supports rolling multi-year plans
and within-season augmentation after observations, seed, or site capacity
change.
}
\seealso{
\code{\link[=optimize_design]{optimize_design()}}, \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, \code{\link[=met_information]{met_information()}}
}
