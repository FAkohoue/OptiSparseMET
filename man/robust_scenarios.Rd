% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_design.R
\name{robust_scenarios}
\alias{robust_scenarios}
\title{Build a prior over variance components for robust design}
\usage{
robust_scenarios(
  sigma_e2 = c(0.5, 1, 2),
  sigma_g2 = 1,
  sigmaE_shrink = 0,
  probs = NULL,
  Sigma_E_candidates = NULL
)
}
\arguments{
\item{sigma_e2}{Vector of residual variances to consider (with \code{sigma_g2},
these set the heritability regime). Default \code{c(0.5, 1, 2)}.}

\item{sigma_g2}{Genetic variance(s). Default 1.}

\item{sigmaE_shrink}{Vector of shrinkage weights in [0, 1]; a scenario uses
\code{(1 - s) * Sigma_E + s * diag(diag(Sigma_E))}, so \code{s = 0} trusts the
covariance fully and \code{s = 1} assumes independent environments. Default 0.}

\item{probs}{Optional prior probabilities (recycled/normalised); defaults to
uniform.}

\item{Sigma_E_candidates}{Optional named list of alternative environment
covariance matrices (for example calibration bootstrap or modality
sensitivity candidates). Each is crossed with the variance and shrinkage
grid. If omitted, the \code{Sigma_E} supplied to the scoring function is used.}
}
\value{
A list of scenarios, each a list with \code{sigma_g2}, \code{sigma_e2},
\code{sigmaE_shrink}, and \code{prob}.
}
\description{
Optimal designs are usually \emph{locally} optimal -- best only at the assumed
variance parameters. A robust (Bayesian) design instead performs well across a
plausible range of those parameters. \code{robust_scenarios()} builds that range as
a weighted set of scenarios varying the residual/genetic variance ratio (the
heritability regime) and, optionally, the trust placed in the assumed
environment covariance \code{Sigma_E} (shrinking it toward independence to reflect
uncertain G x E correlations).
}
\examples{
robust_scenarios(sigma_e2 = c(0.5, 1, 2), sigmaE_shrink = c(0, 0.5))
}
\seealso{
\code{\link[=robust_design_score]{robust_design_score()}}, \code{\link[=optimize_design]{optimize_design()}}.
}
