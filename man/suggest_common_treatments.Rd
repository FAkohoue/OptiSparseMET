% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_common_treatments.R
\name{suggest_common_treatments}
\alias{suggest_common_treatments}
\title{Fast heuristic screen for common (connectivity) treatments}
\usage{
suggest_common_treatments(
  G,
  treatment_info,
  seed_info,
  seed_required_per_plot,
  n_common = NULL,
  reps_per_site = 1,
  Sigma_E = NULL,
  n_test_entries = NULL,
  target_se = 0.15,
  min_per_family = 1,
  n_groups = NULL,
  gxe_floor = NULL,
  seed = NULL
)
}
\arguments{
\item{G}{Genomic relationship matrix over the treatments (row/col names).}

\item{treatment_info}{Data frame with \code{Treatment} and \code{Family}.}

\item{seed_info}{Data frame with \code{Treatment} and \code{SeedAvailable}.}

\item{seed_required_per_plot}{Data frame (\code{Environment},
\code{SeedRequiredPerPlot}) or a numeric vector of per-site seed-per-plot.}

\item{n_common}{If supplied, the number to select (representative,
seed-feasible). If \code{NULL}, the number is suggested (see Description).}

\item{reps_per_site}{Replicates of a common at each site (scalar or per-site
vector) -- used for the seed requirement.}

\item{Sigma_E}{Optional environment covariance; sets the connectivity target
for the suggested number.}

\item{n_test_entries}{Typical entries per site (for the connectivity calc);
defaults to \code{n_treatments / n_sites}.}

\item{target_se}{Target standard error of an estimated site correlation.
Default 0.15.}

\item{min_per_family}{Minimum commons per family. Default 1.}

\item{n_groups}{Number of genetic groups to guarantee coverage of (defaults to
the number of families).}

\item{gxe_floor}{Minimum commons for G x E estimability (defaults to
\code{max(20, 2 * n_sites)}).}

\item{seed}{Optional RNG seed (group clustering).}
}
\value{
A list with \code{selected} (the common treatment ids), \code{n_common},
\code{seed_need}, \code{n_feasible}, \code{n_families}, \code{n_groups}, and \code{rationale}.
}
\description{
Common treatments are tested at \strong{every} site to connect the sparse trials so
that G x E, G x E x M and between-site genetic correlations can be estimated.
This function is a lightweight screening rule for early planning. It assumes
that every common treatment receives the replication supplied in
\code{reps_per_site}; it does not optimise replication.

For the statistically robust production workflow, use
\code{\link[=optimize_common_treatments]{optimize_common_treatments()}}. That function compares common-set sizes under
variance uncertainty and returns binary common presence. Local replication
is assigned afterwards from the remaining seed inventory and never increases
connectivity.

If the breeder supplies \code{n_common}, that number is used. Otherwise the package
suggests it from three requirements a good check set must meet:
\enumerate{
\item \strong{Seed feasibility} -- a common must have enough seed to be sown at
all sites (\eqn{\sum_s q_s r_s} grams); only those enter the candidate pool.
\item \strong{Representativeness} -- every family \emph{and} every genetic group is
included, so the connectivity anchor spans the whole target population.
\item \strong{Estimability} -- enough commons to estimate the site correlations
and G x E with acceptable precision (a connectivity target from the
environment correlation, with a floor scaled to the number of sites).
}
The chosen set is seed-feasible, covers all families/groups, and is filled out
with the most genetically \emph{central} (representative) candidates.
}
\seealso{
\code{\link[=optimize_common_treatments]{optimize_common_treatments()}}, \code{\link[=recommend_common_treatments]{recommend_common_treatments()}},
\code{\link[=tune_common_treatments]{tune_common_treatments()}}, \code{\link[=select_individuals]{select_individuals()}}.
}
