% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/historical_envirotype.R
\name{assess_envirotype_stability}
\alias{assess_envirotype_stability}
\title{Assess whether the environment relationship is stable across years}
\usage{
assess_envirotype_stability(
  hist,
  kernel = "gaussian",
  variables = NULL,
  weights = NULL,
  consensus_method = "rv_weighted",
  bandwidth_multipliers = c(0.5, 1, 2),
  n_boot = 500L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{hist}{A \code{\link[=historical_envirotype]{historical_envirotype()}} result (uses its \code{per_year}).}

\item{kernel, variables, weights}{Passed to \code{\link[=build_environment_relationship]{build_environment_relationship()}}
when building each year's \code{D}.}

\item{consensus_method}{How to combine the per-year matrices into
\code{consensus_D}; passed to \code{\link[=consensus_relationship]{consensus_relationship()}}. Default
\code{"rv_weighted"} (STATIS -- down-weights anomalous years), rather than a plain
mean.}

\item{bandwidth_multipliers}{Gaussian bandwidth ensemble around the median.}

\item{n_boot}{Number of environment-pair bootstrap replicates for the
confidence interval of mean stability.}

\item{conf_level}{Bootstrap confidence level.}

\item{seed}{Optional bootstrap seed.}
}
\value{
A list with \code{per_year_D} (a \code{D} per year), \code{stability} (year-by-year
correlation matrix of the off-diagonal similarities), \code{mean_stability} (the
mean across year pairs), \code{consensus_D} (a robust consensus relationship to
design on), and \code{consensus_weights} (per-year weights when applicable).
}
\description{
Is the environmental kinship a reliable planning input, or does it swing from
year to year? \code{assess_envirotype_stability()} builds the environment
relationship matrix \code{D} \strong{separately for each past year} (from
\code{historical_envirotype()$per_year}) and measures how consistent those yearly
matrices are -- a Mantel-type correlation between the off-diagonal similarities
of each pair of years. High mean stability means the site relationships (and
the mega-environment structure) are dependable; low stability warns that the
descriptive environmental structure is unstable and that distinct yearly
weather kernels should be retained as robust-design stress tests. It does not
by itself estimate genetic \code{Sigma_E}.
}
\seealso{
\code{\link[=historical_envirotype]{historical_envirotype()}}, \code{\link[=consensus_relationship]{consensus_relationship()}},
\code{\link[=build_environment_relationship]{build_environment_relationship()}}, \code{\link[=robust_scenarios]{robust_scenarios()}}.
}
