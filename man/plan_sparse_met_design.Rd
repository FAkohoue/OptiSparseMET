% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plan_sparse_met_design.R
\name{plan_sparse_met_design}
\alias{plan_sparse_met_design}
\title{Plan a sparse multi-environment trial design and assemble a combined field book}
\usage{
plan_sparse_met_design(
  treatments,
  environments,
  allocation_method = c("random_balanced", "balanced_incomplete", "M3", "M4"),
  n_test_entries_per_environment,
  target_replications = NULL,
  common_treatments = NULL,
  env_design_specs,
  treatment_info = NULL,
  seed_info = NULL,
  seed_required_per_plot = NULL,
  allocation_group_source = c("none", "Family", "GRM", "A"),
  GRM = NULL,
  A = NULL,
  id_map = NULL,
  group_method = c("kmeans", "hclust"),
  group_seed = 1,
  group_attempts = 25,
  n_pcs_use = Inf,
  min_groups_per_environment = NULL,
  min_env_per_group = NULL,
  balance_groups_across_env = TRUE,
  force_group_connectivity = TRUE,
  allow_approximate = FALSE,
  seed = NULL
)
}
\arguments{
\item{treatments}{Character vector of treatment IDs.}

\item{environments}{Character vector of environment names.}

\item{allocation_method}{Character scalar passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{n_test_entries_per_environment}{Integer scalar or integer vector passed
to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{target_replications}{Optional positive integer passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{common_treatments}{Optional character vector of common treatments.}

\item{env_design_specs}{Named list with one element per environment. Each
element must contain at least a \code{design} field selecting the local design
engine (\code{"prep_famoptg"} or \code{"alpha_rc_stream"}). All other fields are
passed through to the corresponding local design function, except a small
set of pipeline-consumed fields used internally for seed-aware replication.}

\item{treatment_info}{Optional data frame with at least \code{Treatment} and
optionally \code{Family}.}

\item{seed_info}{Optional data frame used by \code{\link[=assign_replication_by_seed]{assign_replication_by_seed()}}
when seed-aware replication is requested for a local design.}

\item{seed_required_per_plot}{Optional scalar, named vector, or named list
used by \code{\link[=assign_replication_by_seed]{assign_replication_by_seed()}} when seed-aware replication is
requested.}

\item{allocation_group_source}{Character scalar passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{GRM}{Optional genomic relationship matrix passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{A}{Optional pedigree relationship matrix passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{id_map}{Optional treatment-to-matrix ID map passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{group_method}{Character scalar passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{group_seed}{Integer seed passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{group_attempts}{Integer passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{n_pcs_use}{Integer or \code{Inf} passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{min_groups_per_environment}{Optional integer passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{min_env_per_group}{Optional integer passed to
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{balance_groups_across_env}{Logical passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{force_group_connectivity}{Logical passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{allow_approximate}{Logical passed to \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A named list with:
\describe{
\item{\code{sparse_allocation}}{Output from \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}.}
\item{\code{environment_designs}}{Named list of local design outputs, one per
environment.}
\item{\code{combined_field_book}}{Combined MET field book from
\code{\link[=combine_met_fieldbooks]{combine_met_fieldbooks()}}.}
\item{\code{environment_summary}}{Environment-level summary table including
design metadata, plot counts, treatment counts, and extracted efficiency
fields such as \code{eff_model}, \code{eff_A}, \code{eff_D}, and \code{eff_mean_PEV} when
available.}
\item{\code{group_environment_summary}}{Group-by-environment summary propagated
from \code{sparse_allocation$group_by_environment}.}
\item{\code{efficiency_summary}}{Long-format table of extracted efficiency
outputs with columns \code{Environment}, \code{LocalDesign}, \code{Metric}, \code{Value},
and \code{ValueType}. Empty when no efficiency outputs are detected.}
\item{\code{summary}}{High-level summary list for the overall MET design.}
\item{\code{seed_used}}{Seed used internally.}
}
}
\description{
\code{plan_sparse_met_design()} is the end-to-end workflow wrapper for sparse
multi-environment trial (MET) design. It first allocates treatments across
environments using \code{\link[=allocate_sparse_met]{allocate_sparse_met()}}, then builds a local field design
for each environment using the environment-specific specification in
\code{env_design_specs}, and finally stacks all local field books into one
combined MET field book using \code{\link[=combine_met_fieldbooks]{combine_met_fieldbooks()}}.
}
\details{
Compared with calling the lower-level functions manually,
\code{plan_sparse_met_design()} ensures that:
\itemize{
\item every environment has a design specification
\item allocation metadata are propagated into the combined field book
\item local design metadata are harmonized across heterogeneous design engines
\item seed-aware replication can be applied environment by environment
\item local design efficiency outputs, when present as \code{design$efficiency},
are automatically captured into \code{environment_summary} and
\code{efficiency_summary}
\item environment specs are validated before design construction
\item \code{prep_famoptg()} replication mode can be inferred automatically when not
supplied explicitly
\item a registry-based compatibility layer allows additional future design
engines to be added with minimal changes
}
}
