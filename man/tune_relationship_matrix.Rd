% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_relationship_matrix.R
\name{tune_relationship_matrix}
\alias{tune_relationship_matrix}
\title{Make a relationship matrix well-conditioned and invertible}
\usage{
tune_relationship_matrix(
  G,
  method = c("auto", "asrgenomics", "bend", "blend"),
  blend = TRUE,
  bend = FALSE,
  rcn = TRUE,
  blend_weight = 0.02,
  eps = 1e-06,
  ...
)
}
\arguments{
\item{G}{A relationship matrix (e.g. from \code{\link[=build_relationship_matrix]{build_relationship_matrix()}}).}

\item{method}{How to tune. \code{"auto"} (default) uses \code{ASRgenomics::G.tuneup()}
if the package is installed, otherwise the native \code{"bend"}. \code{"asrgenomics"}
forces ASRgenomics. \code{"bend"} is a native nearest-positive-definite
correction (eigenvalue flooring; needs no external package). \code{"blend"} is a
native diagonal blend.}

\item{blend, bend, rcn}{Passed to \code{ASRgenomics::G.tuneup()} when it is used.}

\item{blend_weight}{Weight of the identity blend for the native \code{"blend"}
method. Default 0.02.}

\item{eps}{Eigenvalue floor (as a fraction of the largest eigenvalue) for the
native \code{"bend"} method. Default 1e-6.}

\item{...}{Further arguments passed to \code{ASRgenomics::G.tuneup()}.}
}
\value{
The tuned relationship matrix (with the original dimnames).
}
\description{
Genomic relationship matrices are frequently singular or ill-conditioned
(more markers than individuals, clones/near-duplicates, redundant entries),
which breaks the mixed-model equations. \code{tune_relationship_matrix()} returns a
tuned, invertible version. When the \pkg{ASRgenomics} package is available it
uses its \code{G.tuneup()} (blending toward an identity/\code{A} matrix and/or bending
negative eigenvalues, optionally targeting a reciprocal condition number) --
the standard, expert-trusted procedure. Otherwise it applies a simple
diagonal blend so the pipeline still runs.
}
\examples{
set.seed(1)
M <- matrix(sample(0:2, 30 * 200, TRUE), 30, 200)
rownames(M) <- paste0("L", 1:30)
G <- build_relationship_matrix(markers = M, type = "genomic")
Gb <- tune_relationship_matrix(G)                       # auto
Gb2 <- tune_relationship_matrix(G, method = "bend")     # no external package
}
\references{
Gezan, S. A., Borgognone, M. G., et al. ASRgenomics. VSN International.
Higham, N. J. (2002). Computing the nearest correlation matrix.
}
\seealso{
\code{\link[=build_relationship_matrix]{build_relationship_matrix()}}, \code{\link[=kinship_pca]{kinship_pca()}}.
}
