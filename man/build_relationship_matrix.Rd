% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_relationship_matrix.R
\name{build_relationship_matrix}
\alias{build_relationship_matrix}
\title{Build a relationship matrix from markers, pedigree, or both}
\usage{
build_relationship_matrix(
  markers = NULL,
  pedigree = NULL,
  type = c("genomic", "pedigree", "hmatrix"),
  omega = 0.05,
  tau = 1,
  method = c("vanraden", "AGHmatrix"),
  relationship = c("additive", "dominance"),
  ploidy = 2,
  tuneup = FALSE,
  blend = TRUE,
  bend = FALSE,
  rcn = TRUE
)
}
\arguments{
\item{markers}{Numeric marker matrix (genotypes in rows, markers in columns),
coded 0/1/2. Required for \code{"genomic"} and \code{"hmatrix"}.}

\item{pedigree}{Data frame with columns \code{id}, \code{sire}, \code{dam} (unknown parents
as \code{0} or \code{NA}). Required for \code{"pedigree"} and \code{"hmatrix"}.}

\item{type}{\code{"genomic"}, \code{"pedigree"}, or \code{"hmatrix"}.}

\item{omega}{For \code{"hmatrix"}: weight on the pedigree block within the genomic
block (small, for invertibility). Default 0.05.}

\item{tau}{For \code{"hmatrix"}: overall weight on the genomic information.}

\item{method}{Genomic estimator: \code{"vanraden"} (built-in, default) or
\code{"AGHmatrix"} to use \code{AGHmatrix::Gmatrix()} when the package is installed
(falls back to the built-in with a warning otherwise).}

\item{relationship}{\code{"additive"} (default) or \code{"dominance"} (a Vitezica-2013
dominance genomic relationship, intended as an explicit option for
hybrid/SCA effects). Dominance is never added automatically and should not
be used for ordinary inbred-line additive analyses. AGHmatrix uses
\code{"VanRaden"} vs \code{"Vitezica"} accordingly; the built-in dominance needs
integer 0/1/2 genotypes and ploidy 2.}

\item{ploidy}{Ploidy passed to the VanRaden estimator / AGHmatrix. Default 2.}

\item{tuneup}{If \code{TRUE}, pass the result through \code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}}
(ASRgenomics \code{G.tuneup()}) to guarantee a well-conditioned, invertible
matrix. Default \code{FALSE}.}

\item{blend, bend, rcn}{Passed to \code{\link[=tune_relationship_matrix]{tune_relationship_matrix()}} when
\code{tuneup = TRUE}.
Defaults are \code{TRUE}, \code{FALSE}, and \code{TRUE}, respectively.}
}
\value{
A named, symmetric relationship matrix.
}
\description{
The prediction and evaluation tools in this package take a single relationship
matrix. \code{build_relationship_matrix()} constructs it from raw inputs:
\describe{
\item{\code{"genomic"}}{A VanRaden (2008) genomic relationship matrix from a
marker matrix coded 0/1/2.}
\item{\code{"pedigree"}}{The numerator relationship matrix \eqn{A} from a
three-column pedigree, via Henderson's recursion (captures inbreeding).}
\item{\code{"hmatrix"}}{A single-step \eqn{H} matrix (Legarra et al. 2009;
Aguilar et al. 2010) that blends the genomic matrix on the genotyped block
into the pedigree matrix -- the standard way to use markers \emph{and} pedigree
together and to include non-genotyped lines (predicted through pedigree).}
}
}
\references{
VanRaden, P. M. (2008). Efficient methods to compute genomic predictions.
\emph{Journal of Dairy Science}, 91, 4414-4423.
Legarra, A., Aguilar, I., & Misztal, I. (2009). A relationship matrix
including full pedigree and genomic information. \emph{Journal of Dairy Science},
92, 4656-4663.
}
\seealso{
\code{\link[=met_information]{met_information()}}, \code{\link[=select_individuals]{select_individuals()}}, \code{\link[=tune_common_treatments]{tune_common_treatments()}}.
}
