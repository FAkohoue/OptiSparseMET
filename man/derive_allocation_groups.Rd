% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/derive_allocation_groups.R
\name{derive_allocation_groups}
\alias{derive_allocation_groups}
\title{Derive allocation group labels for sparse MET treatment assignment}
\usage{
derive_allocation_groups(
  treatments,
  allocation_group_source = c("none", "Family", "GRM", "A"),
  treatment_info = NULL,
  GRM = NULL,
  A = NULL,
  id_map = NULL,
  group_method = c("kmeans", "hclust"),
  group_seed = 1,
  group_attempts = 25,
  n_pcs_use = Inf
)
}
\arguments{
\item{treatments}{Character vector of treatment IDs. Duplicate values are
silently deduplicated. Must contain at least one element.}

\item{allocation_group_source}{Character scalar. Grouping mode. One of:
\describe{
\item{\code{"none"}}{All treatments assigned to a single group \code{"ALL"}.}
\item{\code{"Family"}}{Group labels read from \code{treatment_info$Family}.}
\item{\code{"GRM"}}{Cluster labels derived from \code{GRM} via PCA and clustering.}
\item{\code{"A"}}{Cluster labels derived from \code{A} via PCA and clustering.}
}}

\item{treatment_info}{Optional data frame. Required when
\code{allocation_group_source = "Family"}. Must contain columns \code{Treatment}
and \code{Family}. When \code{allocation_group_source \%in\% c("GRM", "A")}, this
argument is optional but, if supplied with a \code{Family} column, is used to
anchor the number of clusters to the number of distinct families among
the supplied treatments. The function stops if any treatment in
\code{treatments} is absent from \code{treatment_info$Treatment} when
\code{allocation_group_source = "Family"}.}

\item{GRM}{Optional numeric matrix. Genomic relationship matrix. Required
when \code{allocation_group_source = "GRM"}. Must be square with row and
column names. Row names must match treatment IDs in \code{treatments} or be
reachable through \code{id_map}.}

\item{A}{Optional numeric matrix. Pedigree-based numerator relationship
matrix. Required when \code{allocation_group_source = "A"}. Same structural
requirements as \code{GRM}.}

\item{id_map}{Optional data frame with columns \code{Treatment} and \code{LineID}.
Required only when treatment IDs in \code{treatments} do not match the row
names of \code{GRM} or \code{A}. The function uses \code{LineID} to look up the
corresponding matrix rows. Ignored when
\code{allocation_group_source \%in\% c("none", "Family")}.}

\item{group_method}{Character scalar. Clustering algorithm applied to the
PCA scores. \code{"kmeans"} uses k-means with \code{group_attempts} random
restarts. \code{"hclust"} uses Ward's criterion hierarchical clustering.
Ignored when \code{allocation_group_source \%in\% c("none", "Family")}.}

\item{group_seed}{Integer. Random seed passed to k-means initialization.
Active only when \code{allocation_group_source \%in\% c("GRM", "A")} and
\code{group_method = "kmeans"}. Has no effect on hierarchical clustering.}

\item{group_attempts}{Integer. Number of random restarts for k-means.
Larger values reduce the risk of converging to a poor local optimum.
Active only when \code{allocation_group_source \%in\% c("GRM", "A")} and
\code{group_method = "kmeans"}.}

\item{n_pcs_use}{Integer or \code{Inf}. Number of leading principal components
retained for clustering. \code{Inf} retains all components corresponding to
positive eigenvalues, up to \eqn{n - 1}. Smaller integer values retain
only the leading components. Must be at least 2. Ignored when
\code{allocation_group_source \%in\% c("none", "Family")}.}
}
\value{
A data frame with one row per element of \code{treatments} (after
deduplication) and the following columns:
\describe{
\item{\code{Treatment}}{Character. Treatment ID, in the order they appear in
\code{treatments} after deduplication.}
\item{\code{AllocationGroup}}{Character. Derived group label. \code{"ALL"} under
\code{"none"}; the family label string under \code{"Family"}; a string of the
form \code{"GRP_G{k}"} under \code{"GRM"} or \code{"GRP_A{k}"} under \code{"A"}, where
\code{{k}} is the integer cluster index.}
}
}
\description{
Four grouping modes are supported. \code{"none"} assigns all treatments to a
single group labelled \code{"ALL"}, which disables group-guided allocation
without requiring any change to the allocation function call. \code{"Family"}
reads group labels directly from \code{treatment_info$Family}, one label per
treatment. \code{"GRM"} and \code{"A"} derive cluster labels from the eigenstructure
of the genomic or pedigree relationship matrix respectively, using PCA
followed by k-means or hierarchical clustering.

When the number of clusters is not determined by the user directly, it is
anchored to the number of distinct family labels among the supplied
treatments if \code{treatment_info} is available, or otherwise approximated as
\eqn{\max(2,\, \lfloor\sqrt{n}\rfloor)} where \eqn{n} is the number of
treatments.
}
\details{
\code{derive_allocation_groups()} assigns a group label to each treatment prior
to sparse allocation across environments. These labels are then used by
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} to guide the incidence structure so that genetic
groups -- defined by family membership or by clusters derived from a
relationship matrix -- are distributed across environments rather than
concentrated in a subset of them. The function is called internally by
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} when \code{allocation_group_source} is not \code{"none"},
but can also be called directly to inspect or audit the group structure
before running allocation.
\subsection{Matrix-based grouping}{

When \code{allocation_group_source \%in\% c("GRM", "A")}, the function extracts
the treatment-level submatrix, performs eigendecomposition, and retains
eigenvectors corresponding to positive eigenvalues (threshold
\eqn{> 10^{-10}}) as principal components. Component scores are scaled by
the square root of the corresponding eigenvalues before clustering, which
weights components proportionally to their contribution to variance in the
relationship matrix.

The number of components retained is controlled by \code{n_pcs_use}. Setting
\code{n_pcs_use = Inf} retains all positive-eigenvalue components up to
\eqn{\min(n_{\text{pos}},\, n - 1)}. Smaller values retain only the leading
components, preserving broad genetic structure at the cost of finer
differentiation. At least 2 informative components must be available;
the function stops with an error if this condition is not met.

K-means clustering uses \code{group_attempts} random restarts seeded by
\code{group_seed}. Hierarchical clustering uses Ward's minimum variance criterion
(\code{method = "ward.D2"}) and is not affected by \code{group_seed} or
\code{group_attempts}. Resulting cluster labels are prefixed \code{"GRP_G"} for GRM
clusters and \code{"GRP_A"} for pedigree clusters.
}

\subsection{ID matching}{

By default, treatment IDs in \code{treatments} are matched directly to row names
of the relationship matrix. When field-book treatment labels differ from
matrix row names, supply \code{id_map} with columns \code{Treatment} and \code{LineID};
the function uses this map to resolve the correspondence before extracting
the submatrix.
}
}
\examples{
treatments <- paste0("L", sprintf("\%03d", 1:12))

treatment_info <- data.frame(
  Treatment = treatments,
  Family    = rep(c("F1", "F2", "F3"), each = 4),
  stringsAsFactors = FALSE
)

## Example 1: family-based groups
grp_fam <- derive_allocation_groups(
  treatments              = treatments,
  allocation_group_source = "Family",
  treatment_info          = treatment_info
)
grp_fam
# AllocationGroup is "F1", "F2", or "F3"

## Example 2: no grouping
grp_none <- derive_allocation_groups(
  treatments              = treatments,
  allocation_group_source = "none"
)
unique(grp_none$AllocationGroup)  # "ALL"

## Example 3: GRM-based clustering
set.seed(1)
n   <- length(treatments)
raw <- matrix(rnorm(n * n), n, n)
GRM <- crossprod(raw) / n
diag(GRM) <- diag(GRM) + 0.1
rownames(GRM) <- colnames(GRM) <- treatments

grp_grm <- derive_allocation_groups(
  treatments              = treatments,
  allocation_group_source = "GRM",
  GRM                     = GRM,
  treatment_info          = treatment_info,
  group_method            = "kmeans",
  group_seed              = 42,
  group_attempts          = 25,
  n_pcs_use               = Inf
)
grp_grm
# AllocationGroup values are "GRP_G1", "GRP_G2", "GRP_G3"

}
\seealso{
\code{\link[=allocate_sparse_met]{allocate_sparse_met()}} which calls this function internally when
\code{allocation_group_source} is not \code{"none"}. Call \code{derive_allocation_groups()}
directly to inspect or audit the group structure before running allocation.
}
